<project toolsVersion="6.11">
  <!-- ovstage source-checkout hook (ADR-0002). Declares no dependency by default:
       ovstage is fetched as a released package into ../_build/target-deps/ovstage by
       ovphysx/scripts/fetch_ovstage_release.py.

       To build against a local ovstage checkout, add a dependency named "ovstage"
       here (linkPath ../_build/target-deps/ovstage) with a packman source-path entry;
       pull_dependencies detects it and links that build instead of fetching.
       (Do not write the literal source-path tag in a comment: pull_dependencies greps
       this file for it.) -->
</project>
