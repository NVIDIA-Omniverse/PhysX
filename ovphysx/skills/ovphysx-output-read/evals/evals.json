{
  "skill_name": "ovphysx-output-read",
  "evals": [
    {
      "id": 1,
      "prompt": "In Python, after each ovphysx step, read only the active rigid-body positions and orientations while preserving each group's ovstage prim identity. Keep the numeric columns for later analysis.",
      "expected_output": "A Python output-read implementation that waits for the step, uses a context-managed ACTIVE read, consumes ovstage identity only while the result is live, and retains the copied numeric columns.",
      "files": [],
      "expectations": [
        "Reads the bundled references/python.md without depending on an ovphysx source checkout",
        "Selects ovphysx-output-read because the task requires ovstage-native identity rather than caller-owned tensor buffers",
        "Keeps an ovstage Stage attached and waits for the intended simulation step before reading",
        "Uses PhysX.read(SimObjectType.RIGID_BODY, [\"position\", \"orientation\"], ObjectScope.ACTIVE) as a context manager",
        "Uses group.tensors as NumPy copies that may outlive the context while keeping prim_list, attribute, and dictionary use inside it",
        "Reopens the ACTIVE read on each frame"
      ]
    },
    {
      "id": 2,
      "prompt": "Using the ovphysx C API, write the latest rigid-body output back into the attached ovstage Stage without rebuilding paths or repacking tensors. Include the closed-loop ordinal scheme and cleanup.",
      "expected_output": "A C lifecycle that queries, reads, forwards borrowed ovstage groups before release, keeps control/output ordinals disjoint, and releases every native resource on all paths.",
      "files": [],
      "expectations": [
        "Reads the bundled references/c.md and references/closed_loop.md without depending on repository-relative files",
        "Uses ovphysx_query, optional discovery/shared-dictionary access, ovphysx_read, and ovphysx_fetch_read_next",
        "Treats OVPHYSX_API_END_OF_ITERATION as normal completion and other non-success statuses as errors",
        "Forwards prims.list, the emitted attribute token, semantic, is_array, and tensors while the borrowed group is live",
        "Completes each ovstage write before ovphysx_release_group and releases read/query handles on success and error paths",
        "Explains that odd/even is an application allocation rather than ovstage semantics and keeps every ordinal single-purpose",
        "Seals control ordinals before update_from_ovstage and never passes a closed range that contains an output ordinal"
      ]
    },
    {
      "id": 3,
      "prompt": "For an RL loop, read every rigid-body pose each frame into a preallocated PyTorch CUDA tensor that my code owns.",
      "expected_output": "Routes to the GPU TensorBindings skill/API rather than the ovstage-native output-read skill.",
      "files": [],
      "expectations": [
        "Does not select ovphysx-output-read for caller-owned bulk CUDA buffers",
        "Selects tensor-bindings-gpu and its DLPack tensor-binding read path"
      ]
    },
    {
      "id": 4,
      "prompt": "Read only active articulation-joint positions in Python. I also want to preallocate from the query's discovery count and keep the cached prim order until layout_generation changes.",
      "expected_output": "Rejects this ACTIVE/caching plan for the ovphysx 0.5.2 baseline, uses ALL for articulation joints, and rebuilds cached layout after known structural edits instead of waiting for layout_generation.",
      "files": [],
      "expectations": [
        "Reads references/python.md and references/scope_and_layout.md",
        "Explains that articulation-joint reads and discovery currently ignore ACTIVE",
        "Routes the articulation-joint query to ObjectScope.ALL",
        "Does not present layout_generation as a working invalidation signal",
        "Requires rebuilding cached query, prim order, and shapes after known structural edits"
      ]
    }
  ]
}
