// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file omniUsdPhysicsDeformableSchema/curvesDeformableSimAPI.h
#ifndef OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSCURVESDEFORMABLESIMAPI_API_H
#define OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSCURVESDEFORMABLESIMAPI_API_H

#include "omniUsdPhysicsDeformableSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI
///
/// The proposed curves deformable schema is a work in progress!
/// Identifies a UsdGeomBasisCurves or UsdGeomBasisCurvesNetwork to be
/// used as the simulation geometry for a curves deformable body, distinguishing
/// it from geometries used exclusively for graphics or collision.
/// A curves deformable body requires exactly one simulation curves geometry,
/// limited to only linear segments. The simulation geometry can either have
/// both DeformableBodyAPI and CurvesDeformableSimAPI applied, or, in
/// a hierarchical setup, be the direct child of the root with DeformableBodyAPI.
/// The simulation API also defines attributes to describe the rest shape of the
/// curves deformable body.
///
class OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsCurvesDeformableSimAPI);
        return t;
    }

    /// Construct a OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI on UsdPrim \p prim .
    /// Equivalent to OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestShapePoints,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestCrvVtxIndices,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestNormals,
        };
        return localNames;
    }

    /// Return a OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsCurvesDeformableSimAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniPhysicsCurvesDeformableSimAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI object is returned upon success.
    /// An invalid (or empty) OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsCurvesDeformableSimAPI)) { return OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI(prim); }
        return OmniUsdPhysicsDeformableSchemaCurvesDeformableSimAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // RESTSHAPEPOINTS
    // --------------------------------------------------------------------- //
    /// Describes the rest shape in (local) space for
    /// a UsdGeomBasisCurves or a UsdGeomBasisCurvesNetwork.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `point3f[] omniphysics:restShapePoints` |
    /// | C++ Type | VtArray<GfVec3f> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Point3fArray |
    UsdAttribute GetRestShapePointsAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestShapePoints); }

    /// See GetRestShapePointsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestShapePointsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestShapePoints,
                   SdfValueTypeNames->Point3fArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RESTCRVVTXINDICES
    // --------------------------------------------------------------------- //
    /// If the shape is a UsdGeomBasisCurvesNetwork, restCrvVtxIndices
    /// specifies a flat list of the index into the restShapePoints attribute of
    /// each vertex of each curve. This allows restCrvVtxIndices to describe
    /// a disjoint per-curve rest shape that differs from any coincident points
    /// described by curveVertexIndices. If the simulation geometry is
    /// a UsdGeomBasisCurves, then restCrvVtxIndices is not applicable.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int[] omniphysics:restCrvVtxIndices` |
    /// | C++ Type | VtArray<int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->IntArray |
    UsdAttribute GetRestCrvVtxIndicesAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestCrvVtxIndices); }

    /// See GetRestCrvVtxIndicesAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestCrvVtxIndicesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestCrvVtxIndices,
                   SdfValueTypeNames->IntArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RESTNORMALS
    // --------------------------------------------------------------------- //
    /// Describes the orientation of the rest curves.
    /// Like normals, restNormals is not a generic primvar but the number of
    /// elements in this attribute will be determined by its 'interpolation'.
    /// See \ref SetRestNormalsInterpolation(). For example, varying
    /// interpolation can be used to specify a normal per curve segment, as
    /// described by \ref UsdGeomBasisCurves_PrimvarInterpolation.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `normal3f[] omniphysics:restNormals` |
    /// | C++ Type | VtArray<GfVec3f> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Normal3fArray |
    UsdAttribute GetRestNormalsAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestNormals); }

    /// See GetRestNormalsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestNormalsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestNormals,
                   SdfValueTypeNames->Normal3fArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
