// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file omniUsdPhysicsDeformableSchema/deformableBodyAPI.h
#ifndef OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSDEFORMABLEBODYAPI_API_H
#define OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSDEFORMABLEBODYAPI_API_H

#include "omniUsdPhysicsDeformableSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class OmniUsdPhysicsDeformableSchemaDeformableBodyAPI
///
/// Applies physics deformable body attributes to a UsdGeomImageable
/// prim with PhysicsBodyAPI and marks that prim including its children to be
/// driven by a volume, surface or curves deformable simulation. The simulation
/// state of a deformable object is represented by a dedicated mesh.
/// Often, additional geometries within the deformable subtree are required for
/// collision (with PhysicsCollisionAPI) or for graphics purposes. In such
/// hierarchical setups the simulation mesh cannot be at the root, but needs to
/// be nested as an immediate child below the root, because USD prohibits
/// nesting of GPrims.
/// The simulation mesh is marked with a deformable type-specific API:
/// - Volume: UsdGeomTetMesh with VolumeDeformableSimAPI
/// - Surface: UsdGeomMesh with SurfaceDeformableSimAPI
/// - Curves: UsdGeomBasisCurves/BasisCurvesNetwork with CurvesDeformableSimAPI
/// During a simulation, the points and velocities of the given UsdGeomPointBased
/// simulation mesh are updated. All other UsdGeomPointBased geometries in the
/// hierarchy below the DeformableBodyAPI prim should move and deform
/// accordingly. The material properties of the deformable body are to be
/// governed by the applied materials with DeformableMaterialAPI,
/// SurfaceDeformableMaterialAPI or CurvesDeformableMaterialAPI.
/// The proposed curves deformable schema is a work in progress!
///
class OmniUsdPhysicsDeformableSchemaDeformableBodyAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsDeformableBodyAPI);
        return t;
    }

    /// Construct a OmniUsdPhysicsDeformableSchemaDeformableBodyAPI on UsdPrim \p prim .
    /// Equivalent to OmniUsdPhysicsDeformableSchemaDeformableBodyAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniUsdPhysicsDeformableSchemaDeformableBodyAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a OmniUsdPhysicsDeformableSchemaDeformableBodyAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniUsdPhysicsDeformableSchemaDeformableBodyAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniUsdPhysicsDeformableSchemaDeformableBodyAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~OmniUsdPhysicsDeformableSchemaDeformableBodyAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsDeformableBodyEnabled,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsMass,
        };
        return localNames;
    }

    /// Return a OmniUsdPhysicsDeformableSchemaDeformableBodyAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniUsdPhysicsDeformableSchemaDeformableBodyAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static OmniUsdPhysicsDeformableSchemaDeformableBodyAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return OmniUsdPhysicsDeformableSchemaDeformableBodyAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsDeformableBodyAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniPhysicsDeformableBodyAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid OmniUsdPhysicsDeformableSchemaDeformableBodyAPI object is returned upon success.
    /// An invalid (or empty) OmniUsdPhysicsDeformableSchemaDeformableBodyAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static OmniUsdPhysicsDeformableSchemaDeformableBodyAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsDeformableBodyAPI)) { return OmniUsdPhysicsDeformableSchemaDeformableBodyAPI(prim); }
        return OmniUsdPhysicsDeformableSchemaDeformableBodyAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // DEFORMABLEBODYENABLED
    // --------------------------------------------------------------------- //
    /// Determines if this PhysicsDeformableBodyAPI is enabled.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omniphysics:deformableBodyEnabled = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetDeformableBodyEnabledAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsDeformableBodyEnabled); }

    /// See GetDeformableBodyEnabledAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateDeformableBodyEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsDeformableBodyEnabled,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // MASS
    // --------------------------------------------------------------------- //
    /// If non-zero, directly specifies the mass of the deformable
    /// object and overrides the mass properties derived from material density.
    /// Note if mass is 0.0 it is ignored. PhysicsMassAPI is ignored for
    /// deformable bodies.
    /// Units: mass
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float omniphysics:mass = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetMassAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsMass); }

    /// See GetMassAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateMassAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsMass,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
