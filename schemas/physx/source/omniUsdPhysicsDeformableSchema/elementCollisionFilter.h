// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file omniUsdPhysicsDeformableSchema/elementCollisionFilter.h
#ifndef OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSELEMENTCOLLISIONFILTER_API_H
#define OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSELEMENTCOLLISIONFILTER_API_H

#include "omniUsdPhysicsDeformableSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>
#include <pxr/usd/usdGeom/imageable.h>

PXR_NAMESPACE_OPEN_SCOPE

/// \class OmniUsdPhysicsDeformableSchemaElementCollisionFilter
///
/// Describes an element wise collision filter for deformable body
/// collision prims with PhysicsCollisionAPI. The filter specifies two suitable
/// collision GPrims which are the source of the elements. The sources may be
/// indentical to control self collision filtering. At least one source prim
/// needs to represent a deformable body collider, and up to one can either
/// be a rigid body or static collider.
/// The filtering for deformable sources is based on element indices representing
/// - triangles for UsdGeomMesh
/// - surface triangles for UsdGeomTetMesh
/// - segments for UsdGeomBasisCurves/UsdGeomBasisCurvesNetwork
/// - points for UsdGeomPoints
/// Filtering a specific triangle of a mesh means omitting collision tests
/// against its face, vertex, and edge features. However, vertices and edges
/// shared with adjacent unfiltered triangles are not filtered. Similarly,
/// vertices of filtered curve segments are not filtered if they are shared with
/// adjacent unfiltered segments.
/// If the source is either a static or rigid collider, the whole geometry is
/// filtered and no elements need to be specified. Which elements are filtered
/// against which, is specified using pairs of element groups. Each pair of
/// groups defines the elements of both sources that should not collide with
/// each other.
/// E.g.: triangles [4, 5, 6] of one collider mesh are each not colliding
/// against any triangles [12, 13] of the other collider mesh.
/// The group sizes are specified with groupElemCounts0 and groupElemCounts1,
/// two arrays of matching size. The element indices of each group are
/// consecutively stored for all groups in groupElemIndices0 and
/// groupElemIndices1, similarly to how faceVertexCounts specifies the number of
/// vertices per face and faceVertexIndices specifies the vertex indices per
/// face in order.
/// Two special cases:
/// - An empty groupElemCounts array indicates that the
/// corresponding source is filtered as a whole, for example for rigid or static
/// colliders.
/// - A group element count of zero indicates that the corresponding source is
/// filtered as a whole with respect to the paired up group of the other source.
///
class OmniUsdPhysicsDeformableSchemaElementCollisionFilter : public UsdGeomImageable
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::ConcreteTyped;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsElementCollisionFilter);
        return t;
    }

    /// Construct a OmniUsdPhysicsDeformableSchemaElementCollisionFilter on UsdPrim \p prim .
    /// Equivalent to OmniUsdPhysicsDeformableSchemaElementCollisionFilter::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniUsdPhysicsDeformableSchemaElementCollisionFilter(const UsdPrim& prim=UsdPrim()) : UsdGeomImageable(prim) {}

    /// Construct a OmniUsdPhysicsDeformableSchemaElementCollisionFilter on the prim held by \p schemaObj .
    /// Should be preferred over OmniUsdPhysicsDeformableSchemaElementCollisionFilter(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniUsdPhysicsDeformableSchemaElementCollisionFilter(const UsdSchemaBase& schemaObj) : UsdGeomImageable(schemaObj) {}

    /// Destructor.
    virtual ~OmniUsdPhysicsDeformableSchemaElementCollisionFilter() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsFilterEnabled,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemCounts0,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemCounts1,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemIndices0,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemIndices1,
        };
        return localNames;
    }

    /// Return a OmniUsdPhysicsDeformableSchemaElementCollisionFilter holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniUsdPhysicsDeformableSchemaElementCollisionFilter(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static OmniUsdPhysicsDeformableSchemaElementCollisionFilter Get(const UsdStagePtr &stage, const SdfPath &path)
    { return OmniUsdPhysicsDeformableSchemaElementCollisionFilter(stage->GetPrimAtPath(path)); }

    /// Attempt to ensure a \a UsdPrim adhering to this schema at \p path
    /// is defined (according to UsdPrim::IsDefined()) on this stage.
    ///
    /// If a prim adhering to this schema at \p path is already defined on this
    /// stage, return that prim.  Otherwise author an \a SdfPrimSpec with
    /// \a specifier == \a SdfSpecifierDef and this schema's prim type name for
    /// the prim at \p path at the current EditTarget.  Author \a SdfPrimSpec s
    /// with \p specifier == \a SdfSpecifierDef and empty typeName at the
    /// current EditTarget for any nonexistent, or existing but not \a Defined
    /// ancestors.
    ///
    /// The given \a path must be an absolute prim path that does not contain
    /// any variant selections.
    ///
    /// If it is impossible to author any of the necessary PrimSpecs, (for
    /// example, in case \a path cannot map to the current UsdEditTarget's
    /// namespace) issue an error and return an invalid \a UsdPrim.
    ///
    /// Note that this method may return a defined prim whose typeName does not
    /// specify this schema class, in case a stronger typeName opinion overrides
    /// the opinion at the current EditTarget.
    static OmniUsdPhysicsDeformableSchemaElementCollisionFilter Define(const UsdStagePtr &stage, const SdfPath &path)
    { return OmniUsdPhysicsDeformableSchemaElementCollisionFilter(stage->DefinePrim(path, OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsElementCollisionFilter)); }

public:
    // --------------------------------------------------------------------- //
    // FILTERENABLED
    // --------------------------------------------------------------------- //
    /// Determines if this PhysicsElementCollisionFilter is enabled.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool omniphysics:filterEnabled = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetFilterEnabledAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsFilterEnabled); }

    /// See GetFilterEnabledAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateFilterEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsFilterEnabled,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // GROUPELEMCOUNTS0
    // --------------------------------------------------------------------- //
    /// Array of element counts per filter group for src0. Same size as
    /// groupElemCounts1 or empty.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omniphysics:groupElemCounts0` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    UsdAttribute GetGroupElemCounts0Attr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemCounts0); }

    /// See GetGroupElemCounts0Attr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateGroupElemCounts0Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemCounts0,
                   SdfValueTypeNames->UIntArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // GROUPELEMCOUNTS1
    // --------------------------------------------------------------------- //
    /// Array of element counts per filter group for src1. Same size as
    /// groupElemCounts0 or empty.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omniphysics:groupElemCounts1` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    UsdAttribute GetGroupElemCounts1Attr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemCounts1); }

    /// See GetGroupElemCounts1Attr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateGroupElemCounts1Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemCounts1,
                   SdfValueTypeNames->UIntArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // GROUPELEMINDICES0
    // --------------------------------------------------------------------- //
    /// Array of element indices ordered according to groupElemCounts0,
    /// referencing elements of src0. If the src is a curves prim,
    /// then each entry consists of two indices: (curve index, segment index).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omniphysics:groupElemIndices0` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    UsdAttribute GetGroupElemIndices0Attr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemIndices0); }

    /// See GetGroupElemIndices0Attr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateGroupElemIndices0Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemIndices0,
                   SdfValueTypeNames->UIntArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // GROUPELEMINDICES1
    // --------------------------------------------------------------------- //
    /// Array of element indices ordered according to groupElemCounts1,
    /// referencing elements of src1. If the src is a curves prim,
    /// then each entry consists of two indices: (curve index, segment index).
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint[] omniphysics:groupElemIndices1` |
    /// | C++ Type | VtArray<unsigned int> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UIntArray |
    UsdAttribute GetGroupElemIndices1Attr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemIndices1); }

    /// See GetGroupElemIndices1Attr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateGroupElemIndices1Attr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsGroupElemIndices1,
                   SdfValueTypeNames->UIntArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SRC0
    // --------------------------------------------------------------------- //
    /// Relationship to a single deformable body's collider which is
    /// the source of elements referenced by groupElemIndices0 or
    /// a rigid body/static collider.
    UsdRelationship GetSrc0Rel() const
    { return GetPrim().GetRelationship(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsSrc0); }

    /// See GetSrc0Rel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateSrc0Rel() const
    { return GetPrim().CreateRelationship(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsSrc0, /* custom = */ false); }

public:
    // --------------------------------------------------------------------- //
    // SRC1
    // --------------------------------------------------------------------- //
    /// Relationship to a single deformable body's collider which is
    /// the source of elements referenced by groupElemIndices1
    /// or a rigid body/static collider.
    UsdRelationship GetSrc1Rel() const
    { return GetPrim().GetRelationship(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsSrc1); }

    /// See GetSrc1Rel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateSrc1Rel() const
    { return GetPrim().CreateRelationship(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsSrc1, /* custom = */ false); }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
