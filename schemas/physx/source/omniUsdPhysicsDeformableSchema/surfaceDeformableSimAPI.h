// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file omniUsdPhysicsDeformableSchema/surfaceDeformableSimAPI.h
#ifndef OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSSURFACEDEFORMABLESIMAPI_API_H
#define OMNIUSDPHYSICSDEFORMABLESCHEMA_GENERATED_OMNIPHYSICSSURFACEDEFORMABLESIMAPI_API_H

#include "omniUsdPhysicsDeformableSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI
///
/// Identifies a UsdGeomMesh to be used as the simulation mesh for
/// a surface deformable body, distinguishing it from geometries used
/// exclusively for graphics or collision.
/// A surface deformable body requires exactly one simulation mesh, limited to
/// only triangular faces. The simulation mesh can either have both
/// DeformableBodyAPI and SurfaceDeformableSimAPI applied, or, in a hierarchical
/// setup, be the direct child of the root with DeformableBodyAPI.
/// The simulation API also defines attributes to describe the rest shape of the
/// surface deformable body.
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref OmniUsdPhysicsDeformableSchemaTokens.
/// So to set an attribute to the value "rightHanded", use OmniUsdPhysicsDeformableSchemaTokens->rightHanded
/// as the value.
///
class OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsSurfaceDeformableSimAPI);
        return t;
    }

    /// Construct a OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI on UsdPrim \p prim .
    /// Equivalent to OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI on the prim held by \p schemaObj .
    /// Should be preferred over OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestShapePoints,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestTriVtxIndices,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestBendAnglesDefault,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestAdjTriPairs,
            OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestBendAngles,
        };
        return localNames;
    }

    /// Return a OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsSurfaceDeformableSimAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "OmniPhysicsSurfaceDeformableSimAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI object is returned upon success.
    /// An invalid (or empty) OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(OmniUsdPhysicsDeformableSchemaTokens->OmniPhysicsSurfaceDeformableSimAPI)) { return OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI(prim); }
        return OmniUsdPhysicsDeformableSchemaSurfaceDeformableSimAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // RESTSHAPEPOINTS
    // --------------------------------------------------------------------- //
    /// Describes the per triangle planar shape in (local) space for a
    /// surface deformable body. While panel based cloth systems may only
    /// require UV coordinates and not require a third component, doing so makes
    /// it convenient for an application to support non-planar shells with
    /// a rest shape described in 3D.
    /// One can still use this array for per-face UVs with a little wasted space.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `point3f[] omniphysics:restShapePoints` |
    /// | C++ Type | VtArray<GfVec3f> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Point3fArray |
    UsdAttribute GetRestShapePointsAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestShapePoints); }

    /// See GetRestShapePointsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestShapePointsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestShapePoints,
                   SdfValueTypeNames->Point3fArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RESTTRIVTXINDICES
    // --------------------------------------------------------------------- //
    /// Per triangle element indices into restShapePoints to describe
    /// the rest shape of each triangle face. This allows for
    /// restTriVtxIndices to have length up to 3*len(restShapePoints) to
    /// allow for a disjoint per-element rest shape description, or a
    /// more compact array when all triangles share conincident points
    /// to describe the rest shape of the surface deformable body triangle mesh.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int3[] omniphysics:restTriVtxIndices` |
    /// | C++ Type | VtArray<GfVec3i> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int3Array |
    UsdAttribute GetRestTriVtxIndicesAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestTriVtxIndices); }

    /// See GetRestTriVtxIndicesAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestTriVtxIndicesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestTriVtxIndices,
                   SdfValueTypeNames->Int3Array, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RESTBENDANGLESDEFAULT
    // --------------------------------------------------------------------- //
    /// Default method for defining rest dihedral angles between
    /// adjacent triangles, which are not explicitely specified with
    /// restAdjTriPairs and restBendAngles:
    /// "flatDefault" - Default rest dihedral bending angles are zero.
    /// "restShapeDefault" - Default rest dihedral bending angles are defined by
    /// the normals of the adjacent triangles in the configuration given by the
    /// restShapePoints. The adjacency is defined by the topology of the
    /// simulation mesh: GeomMesh:faceVertexIndices.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uniform token omniphysics:restBendAnglesDefault = "flatDefault"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref SdfVariability "Variability" | SdfVariabilityUniform |
    /// | \ref OmniUsdPhysicsDeformableSchemaTokens "Allowed Values" | flatDefault, restShapeDefault |
    UsdAttribute GetRestBendAnglesDefaultAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestBendAnglesDefault); }

    /// See GetRestBendAnglesDefaultAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestBendAnglesDefaultAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestBendAnglesDefault,
                   SdfValueTypeNames->Token, /* custom = */ false,
                   SdfVariabilityUniform, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RESTADJTRIPAIRS
    // --------------------------------------------------------------------- //
    /// Array of adjacent triangle pairs to define dihedral rest bending
    /// angles explicitely, overriding the default rest angles. Each entry
    /// corresponds to an angle specified in restBendAngles.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `int2[] omniphysics:restAdjTriPairs` |
    /// | C++ Type | VtArray<GfVec2i> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Int2Array |
    UsdAttribute GetRestAdjTriPairsAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestAdjTriPairs); }

    /// See GetRestAdjTriPairsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestAdjTriPairsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestAdjTriPairs,
                   SdfValueTypeNames->Int2Array, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RESTBENDANGLES
    // --------------------------------------------------------------------- //
    /// Rest angles in degrees. Length of restBendAngles should match
    /// length of restAdjTriPairs. For all adjacent triangle pairs not specified
    /// via restAdjTriPairs and restBendAngles, the default dihedral angle given
    /// by restBendAnglesDefault is assumed.
    /// Range: [-180.0, 180.0)
    /// Units: degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] omniphysics:restBendAngles` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    UsdAttribute GetRestBendAnglesAttr() const
    { return GetPrim().GetAttribute(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestBendAngles); }

    /// See GetRestBendAnglesAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRestBendAnglesAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(OmniUsdPhysicsDeformableSchemaTokens->omniphysicsRestBendAngles,
                   SdfValueTypeNames->FloatArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
