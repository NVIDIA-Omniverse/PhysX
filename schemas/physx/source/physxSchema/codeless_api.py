# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
"""GENERATED by tools/gen_codeless_api.py -- codeless PhysxSchema Python API.

Preserves the PhysxSchema.<Class> surface (Apply/Get/Get<Attr>Attr/Create<Attr>Attr)
for a codeless schema, backed by raw USD access. No compiled _physxSchema bindings.
"""
from pxr import Usd, Sdf, Tf

__all__ = ['PhysxSceneAPI', 'PhysxRigidBodyAPI', 'PhysxContactReportAPI', 'PhysxCollisionAPI', 'PhysxConvexHullCollisionAPI', 'PhysxConvexDecompositionCollisionAPI', 'PhysxSphereFillCollisionAPI', 'PhysxTriangleMeshSimplificationCollisionAPI', 'PhysxTriangleMeshCollisionAPI', 'PhysxSDFMeshCollisionAPI', 'PhysxSurfaceVelocityAPI', 'PhysxMeshMergeCollisionAPI', 'PhysxSceneQuasistaticAPI', 'PhysxMaterialAPI', 'PhysxPhysicsInstancer', 'PhysxPhysicsJointInstancer', 'PhysxPhysicsGearJoint', 'PhysxPhysicsRackAndPinionJoint', 'PhysxJointAPI', 'PhysxPhysicsDistanceJointAPI', 'PhysxLimitAPI', 'JointStateAPI', 'PhysxArticulationAPI', 'PhysxForceAPI', 'PhysxCharacterControllerAPI', 'PhysxTriggerAPI', 'PhysxTriggerStateAPI', 'PhysxCookedDataAPI', 'PhysxTendonAxisAPI', 'PhysxTendonAxisRootAPI', 'PhysxTendonAttachmentAPI', 'PhysxTendonAttachmentRootAPI', 'PhysxTendonAttachmentLeafAPI', 'PhysxMimicJointAPI', 'PhysxVehicleContextAPI', 'PhysxVehicleTireFrictionTable', 'PhysxVehicleWheelAPI', 'PhysxVehicleTireAPI', 'PhysxVehicleSuspensionAPI', 'PhysxVehicleWheelAttachmentAPI', 'PhysxVehicleSuspensionComplianceAPI', 'PhysxVehicleEngineAPI', 'PhysxVehicleGearsAPI', 'PhysxVehicleAutoGearBoxAPI', 'PhysxVehicleClutchAPI', 'PhysxVehicleDriveBasicAPI', 'PhysxVehicleDriveStandardAPI', 'PhysxVehicleMultiWheelDifferentialAPI', 'PhysxVehicleTankDifferentialAPI', 'PhysxVehicleBrakesAPI', 'PhysxVehicleSteeringAPI', 'PhysxVehicleAckermannSteeringAPI', 'PhysxVehicleAPI', 'PhysxVehicleControllerAPI', 'PhysxVehicleTankControllerAPI', 'PhysxVehicleWheelControllerAPI', 'PhysxVehicleNonlinearCommandResponseAPI', 'PhysxCameraAPI', 'PhysxCameraFollowAPI', 'PhysxCameraFollowLookAPI', 'PhysxCameraFollowVelocityAPI', 'PhysxCameraDroneAPI', 'PhysxParticleSystem', 'PhysxParticleIsosurfaceAPI', 'PhysxParticleSmoothingAPI', 'PhysxParticleAnisotropyAPI', 'PhysxDiffuseParticlesAPI', 'PhysxPBDMaterialAPI', 'PhysxParticleAPI', 'PhysxParticleSetAPI', 'PhysxParticleSamplingAPI', 'PhysxJointAxisAPI', 'PhysxDrivePerformanceEnvelopeAPI', 'PhysxSplinesSurfaceVelocityAPI', 'PhysxBaseDeformableBodyAPI', 'PhysxSurfaceDeformableBodyAPI', 'PhysxDeformableMaterialAPI', 'PhysxSurfaceDeformableMaterialAPI', 'PhysxAutoDeformableBodyAPI', 'PhysxAutoDeformableHexahedralMeshAPI', 'PhysxAutoDeformableMeshSimplificationAPI', 'PhysxAutoDeformableAttachmentAPI']


class _Base:
    def __init__(self, prim=None, name=''):
        if prim is not None and hasattr(prim, 'GetPrim') and not hasattr(prim, 'IsValid'):
            prim = prim.GetPrim()
        self._prim = prim
        self._name = name
    def GetPrim(self): return self._prim
    def GetPath(self): return self._prim.GetPath() if self._prim else Sdf.Path()
    def GetName(self): return self._name
    def _IsCompatible(self): return True
    def __bool__(self):
        return bool(self._prim and hasattr(self._prim, 'IsValid')
                    and self._prim.IsValid() and self._IsCompatible())


def _coerce_to_type(value, sdf_type):
    try:
        return sdf_type.arrayType.type.pythonClass([value])[0]
    except Exception:
        return value

class PhysxSceneAPI(_Base):
    _schemaName = "PhysxSceneAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxScene:bounceThreshold', 'physxScene:frictionOffsetThreshold', 'physxScene:frictionCorrelationDistance', 'physxScene:maxBiasCoefficient', 'physxScene:collisionSystem', 'physxScene:solverType', 'physxScene:broadphaseType', 'physxScene:frictionType', 'physxScene:enableCCD', 'physxScene:enableStabilization', 'physxScene:updateType', 'physxScene:enableGPUDynamics', 'physxScene:enableEnhancedDeterminism', 'physxScene:enableExternalForcesEveryIteration', 'physxScene:solveArticulationContactLast', 'physxScene:enableSceneQuerySupport', 'physxScene:timeStepsPerSecond', 'physxScene:gpuTempBufferCapacity', 'physxScene:gpuMaxRigidContactCount', 'physxScene:gpuMaxRigidPatchCount', 'physxScene:gpuHeapCapacity', 'physxScene:gpuFoundLostPairsCapacity', 'physxScene:gpuFoundLostAggregatePairsCapacity', 'physxScene:gpuTotalAggregatePairsCapacity', 'physxScene:gpuMaxDeformableVolumeContacts', 'physxScene:gpuMaxDeformableSurfaceContacts', 'physxScene:gpuMaxParticleContacts', 'physxScene:gpuMaxNumPartitions', 'physxScene:gpuCollisionStackSize', 'physxScene:invertCollisionGroupFilter', 'physxScene:reportKinematicKinematicPairs', 'physxScene:reportKinematicStaticPairs', 'physxScene:minPositionIterationCount', 'physxScene:maxPositionIterationCount', 'physxScene:minVelocityIterationCount', 'physxScene:maxVelocityIterationCount', 'physxScene:disableSleeping']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetBounceThresholdAttr(self):
        return self._prim.GetAttribute("physxScene:bounceThreshold")
    def CreateBounceThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:bounceThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:bounceThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFrictionOffsetThresholdAttr(self):
        return self._prim.GetAttribute("physxScene:frictionOffsetThreshold")
    def CreateFrictionOffsetThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:frictionOffsetThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:frictionOffsetThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFrictionCorrelationDistanceAttr(self):
        return self._prim.GetAttribute("physxScene:frictionCorrelationDistance")
    def CreateFrictionCorrelationDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:frictionCorrelationDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:frictionCorrelationDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxBiasCoefficientAttr(self):
        return self._prim.GetAttribute("physxScene:maxBiasCoefficient")
    def CreateMaxBiasCoefficientAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:maxBiasCoefficient")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:maxBiasCoefficient", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCollisionSystemAttr(self):
        return self._prim.GetAttribute("physxScene:collisionSystem")
    def CreateCollisionSystemAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:collisionSystem")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:collisionSystem", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolverTypeAttr(self):
        return self._prim.GetAttribute("physxScene:solverType")
    def CreateSolverTypeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:solverType")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:solverType", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBroadphaseTypeAttr(self):
        return self._prim.GetAttribute("physxScene:broadphaseType")
    def CreateBroadphaseTypeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:broadphaseType")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:broadphaseType", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFrictionTypeAttr(self):
        return self._prim.GetAttribute("physxScene:frictionType")
    def CreateFrictionTypeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:frictionType")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:frictionType", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableCCDAttr(self):
        return self._prim.GetAttribute("physxScene:enableCCD")
    def CreateEnableCCDAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:enableCCD")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:enableCCD", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableStabilizationAttr(self):
        return self._prim.GetAttribute("physxScene:enableStabilization")
    def CreateEnableStabilizationAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:enableStabilization")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:enableStabilization", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpdateTypeAttr(self):
        return self._prim.GetAttribute("physxScene:updateType")
    def CreateUpdateTypeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:updateType")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:updateType", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableGPUDynamicsAttr(self):
        return self._prim.GetAttribute("physxScene:enableGPUDynamics")
    def CreateEnableGPUDynamicsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:enableGPUDynamics")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:enableGPUDynamics", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableEnhancedDeterminismAttr(self):
        return self._prim.GetAttribute("physxScene:enableEnhancedDeterminism")
    def CreateEnableEnhancedDeterminismAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:enableEnhancedDeterminism")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:enableEnhancedDeterminism", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableExternalForcesEveryIterationAttr(self):
        return self._prim.GetAttribute("physxScene:enableExternalForcesEveryIteration")
    def CreateEnableExternalForcesEveryIterationAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:enableExternalForcesEveryIteration")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:enableExternalForcesEveryIteration", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolveArticulationContactLastAttr(self):
        return self._prim.GetAttribute("physxScene:solveArticulationContactLast")
    def CreateSolveArticulationContactLastAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:solveArticulationContactLast")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:solveArticulationContactLast", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableSceneQuerySupportAttr(self):
        return self._prim.GetAttribute("physxScene:enableSceneQuerySupport")
    def CreateEnableSceneQuerySupportAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:enableSceneQuerySupport")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:enableSceneQuerySupport", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTimeStepsPerSecondAttr(self):
        return self._prim.GetAttribute("physxScene:timeStepsPerSecond")
    def CreateTimeStepsPerSecondAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:timeStepsPerSecond")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:timeStepsPerSecond", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuTempBufferCapacityAttr(self):
        return self._prim.GetAttribute("physxScene:gpuTempBufferCapacity")
    def CreateGpuTempBufferCapacityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt64
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuTempBufferCapacity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuTempBufferCapacity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuMaxRigidContactCountAttr(self):
        return self._prim.GetAttribute("physxScene:gpuMaxRigidContactCount")
    def CreateGpuMaxRigidContactCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuMaxRigidContactCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuMaxRigidContactCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuMaxRigidPatchCountAttr(self):
        return self._prim.GetAttribute("physxScene:gpuMaxRigidPatchCount")
    def CreateGpuMaxRigidPatchCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuMaxRigidPatchCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuMaxRigidPatchCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuHeapCapacityAttr(self):
        return self._prim.GetAttribute("physxScene:gpuHeapCapacity")
    def CreateGpuHeapCapacityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuHeapCapacity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuHeapCapacity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuFoundLostPairsCapacityAttr(self):
        return self._prim.GetAttribute("physxScene:gpuFoundLostPairsCapacity")
    def CreateGpuFoundLostPairsCapacityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuFoundLostPairsCapacity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuFoundLostPairsCapacity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuFoundLostAggregatePairsCapacityAttr(self):
        return self._prim.GetAttribute("physxScene:gpuFoundLostAggregatePairsCapacity")
    def CreateGpuFoundLostAggregatePairsCapacityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuFoundLostAggregatePairsCapacity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuFoundLostAggregatePairsCapacity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuTotalAggregatePairsCapacityAttr(self):
        return self._prim.GetAttribute("physxScene:gpuTotalAggregatePairsCapacity")
    def CreateGpuTotalAggregatePairsCapacityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuTotalAggregatePairsCapacity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuTotalAggregatePairsCapacity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuMaxDeformableVolumeContactsAttr(self):
        return self._prim.GetAttribute("physxScene:gpuMaxDeformableVolumeContacts")
    def CreateGpuMaxDeformableVolumeContactsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuMaxDeformableVolumeContacts")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuMaxDeformableVolumeContacts", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuMaxDeformableSurfaceContactsAttr(self):
        return self._prim.GetAttribute("physxScene:gpuMaxDeformableSurfaceContacts")
    def CreateGpuMaxDeformableSurfaceContactsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuMaxDeformableSurfaceContacts")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuMaxDeformableSurfaceContacts", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuMaxParticleContactsAttr(self):
        return self._prim.GetAttribute("physxScene:gpuMaxParticleContacts")
    def CreateGpuMaxParticleContactsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuMaxParticleContacts")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuMaxParticleContacts", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuMaxNumPartitionsAttr(self):
        return self._prim.GetAttribute("physxScene:gpuMaxNumPartitions")
    def CreateGpuMaxNumPartitionsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuMaxNumPartitions")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuMaxNumPartitions", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGpuCollisionStackSizeAttr(self):
        return self._prim.GetAttribute("physxScene:gpuCollisionStackSize")
    def CreateGpuCollisionStackSizeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:gpuCollisionStackSize")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:gpuCollisionStackSize", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetInvertCollisionGroupFilterAttr(self):
        return self._prim.GetAttribute("physxScene:invertCollisionGroupFilter")
    def CreateInvertCollisionGroupFilterAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:invertCollisionGroupFilter")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:invertCollisionGroupFilter", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetReportKinematicKinematicPairsAttr(self):
        return self._prim.GetAttribute("physxScene:reportKinematicKinematicPairs")
    def CreateReportKinematicKinematicPairsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:reportKinematicKinematicPairs")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:reportKinematicKinematicPairs", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetReportKinematicStaticPairsAttr(self):
        return self._prim.GetAttribute("physxScene:reportKinematicStaticPairs")
    def CreateReportKinematicStaticPairsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:reportKinematicStaticPairs")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:reportKinematicStaticPairs", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinPositionIterationCountAttr(self):
        return self._prim.GetAttribute("physxScene:minPositionIterationCount")
    def CreateMinPositionIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:minPositionIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:minPositionIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxPositionIterationCountAttr(self):
        return self._prim.GetAttribute("physxScene:maxPositionIterationCount")
    def CreateMaxPositionIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:maxPositionIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:maxPositionIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinVelocityIterationCountAttr(self):
        return self._prim.GetAttribute("physxScene:minVelocityIterationCount")
    def CreateMinVelocityIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:minVelocityIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:minVelocityIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxVelocityIterationCountAttr(self):
        return self._prim.GetAttribute("physxScene:maxVelocityIterationCount")
    def CreateMaxVelocityIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:maxVelocityIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:maxVelocityIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDisableSleepingAttr(self):
        return self._prim.GetAttribute("physxScene:disableSleeping")
    def CreateDisableSleepingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxScene:disableSleeping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxScene:disableSleeping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxRigidBodyAPI(_Base):
    _schemaName = "PhysxRigidBodyAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxRigidBody:linearDamping', 'physxRigidBody:angularDamping', 'physxRigidBody:maxLinearVelocity', 'physxRigidBody:maxAngularVelocity', 'physxRigidBody:sleepThreshold', 'physxRigidBody:stabilizationThreshold', 'physxRigidBody:maxDepenetrationVelocity', 'physxRigidBody:maxContactImpulse', 'physxRigidBody:solverPositionIterationCount', 'physxRigidBody:solverVelocityIterationCount', 'physxRigidBody:enableCCD', 'physxRigidBody:enableSpeculativeCCD', 'physxRigidBody:retainAccelerations', 'physxRigidBody:enableGyroscopicForces', 'physxRigidBody:disableGravity', 'physxRigidBody:solveContact', 'physxRigidBody:lockedPosAxis', 'physxRigidBody:lockedRotAxis', 'physxRigidBody:contactSlopCoefficient', 'physxRigidBody:cfmScale']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetLinearDampingAttr(self):
        return self._prim.GetAttribute("physxRigidBody:linearDamping")
    def CreateLinearDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:linearDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:linearDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAngularDampingAttr(self):
        return self._prim.GetAttribute("physxRigidBody:angularDamping")
    def CreateAngularDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:angularDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:angularDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxLinearVelocityAttr(self):
        return self._prim.GetAttribute("physxRigidBody:maxLinearVelocity")
    def CreateMaxLinearVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:maxLinearVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:maxLinearVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxAngularVelocityAttr(self):
        return self._prim.GetAttribute("physxRigidBody:maxAngularVelocity")
    def CreateMaxAngularVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:maxAngularVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:maxAngularVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSleepThresholdAttr(self):
        return self._prim.GetAttribute("physxRigidBody:sleepThreshold")
    def CreateSleepThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:sleepThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:sleepThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStabilizationThresholdAttr(self):
        return self._prim.GetAttribute("physxRigidBody:stabilizationThreshold")
    def CreateStabilizationThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:stabilizationThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:stabilizationThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxDepenetrationVelocityAttr(self):
        return self._prim.GetAttribute("physxRigidBody:maxDepenetrationVelocity")
    def CreateMaxDepenetrationVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:maxDepenetrationVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:maxDepenetrationVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxContactImpulseAttr(self):
        return self._prim.GetAttribute("physxRigidBody:maxContactImpulse")
    def CreateMaxContactImpulseAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:maxContactImpulse")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:maxContactImpulse", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolverPositionIterationCountAttr(self):
        return self._prim.GetAttribute("physxRigidBody:solverPositionIterationCount")
    def CreateSolverPositionIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:solverPositionIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:solverPositionIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolverVelocityIterationCountAttr(self):
        return self._prim.GetAttribute("physxRigidBody:solverVelocityIterationCount")
    def CreateSolverVelocityIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:solverVelocityIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:solverVelocityIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableCCDAttr(self):
        return self._prim.GetAttribute("physxRigidBody:enableCCD")
    def CreateEnableCCDAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:enableCCD")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:enableCCD", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableSpeculativeCCDAttr(self):
        return self._prim.GetAttribute("physxRigidBody:enableSpeculativeCCD")
    def CreateEnableSpeculativeCCDAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:enableSpeculativeCCD")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:enableSpeculativeCCD", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRetainAccelerationsAttr(self):
        return self._prim.GetAttribute("physxRigidBody:retainAccelerations")
    def CreateRetainAccelerationsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:retainAccelerations")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:retainAccelerations", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableGyroscopicForcesAttr(self):
        return self._prim.GetAttribute("physxRigidBody:enableGyroscopicForces")
    def CreateEnableGyroscopicForcesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:enableGyroscopicForces")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:enableGyroscopicForces", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDisableGravityAttr(self):
        return self._prim.GetAttribute("physxRigidBody:disableGravity")
    def CreateDisableGravityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:disableGravity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:disableGravity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolveContactAttr(self):
        return self._prim.GetAttribute("physxRigidBody:solveContact")
    def CreateSolveContactAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:solveContact")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:solveContact", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLockedPosAxisAttr(self):
        return self._prim.GetAttribute("physxRigidBody:lockedPosAxis")
    def CreateLockedPosAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:lockedPosAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:lockedPosAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLockedRotAxisAttr(self):
        return self._prim.GetAttribute("physxRigidBody:lockedRotAxis")
    def CreateLockedRotAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:lockedRotAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:lockedRotAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetContactSlopCoefficientAttr(self):
        return self._prim.GetAttribute("physxRigidBody:contactSlopCoefficient")
    def CreateContactSlopCoefficientAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:contactSlopCoefficient")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:contactSlopCoefficient", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCfmScaleAttr(self):
        return self._prim.GetAttribute("physxRigidBody:cfmScale")
    def CreateCfmScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxRigidBody:cfmScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxRigidBody:cfmScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxContactReportAPI(_Base):
    _schemaName = "PhysxContactReportAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxContactReport:threshold']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetThresholdAttr(self):
        return self._prim.GetAttribute("physxContactReport:threshold")
    def CreateThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxContactReport:threshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxContactReport:threshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetReportPairsRel(self):
        return self._prim.GetRelationship("physxContactReport:reportPairs")
    def CreateReportPairsRel(self):
        return self._prim.CreateRelationship("physxContactReport:reportPairs", False)

class PhysxCollisionAPI(_Base):
    _schemaName = "PhysxCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxCollision:contactOffset', 'physxCollision:restOffset', 'physxCollision:torsionalPatchRadius', 'physxCollision:minTorsionalPatchRadius']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetContactOffsetAttr(self):
        return self._prim.GetAttribute("physxCollision:contactOffset")
    def CreateContactOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCollision:contactOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCollision:contactOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRestOffsetAttr(self):
        return self._prim.GetAttribute("physxCollision:restOffset")
    def CreateRestOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCollision:restOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCollision:restOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTorsionalPatchRadiusAttr(self):
        return self._prim.GetAttribute("physxCollision:torsionalPatchRadius")
    def CreateTorsionalPatchRadiusAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCollision:torsionalPatchRadius")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCollision:torsionalPatchRadius", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinTorsionalPatchRadiusAttr(self):
        return self._prim.GetAttribute("physxCollision:minTorsionalPatchRadius")
    def CreateMinTorsionalPatchRadiusAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCollision:minTorsionalPatchRadius")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCollision:minTorsionalPatchRadius", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxConvexHullCollisionAPI(_Base):
    _schemaName = "PhysxConvexHullCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxConvexHullCollision:hullVertexLimit', 'physxConvexHullCollision:minThickness']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetHullVertexLimitAttr(self):
        return self._prim.GetAttribute("physxConvexHullCollision:hullVertexLimit")
    def CreateHullVertexLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexHullCollision:hullVertexLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexHullCollision:hullVertexLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinThicknessAttr(self):
        return self._prim.GetAttribute("physxConvexHullCollision:minThickness")
    def CreateMinThicknessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexHullCollision:minThickness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexHullCollision:minThickness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxConvexDecompositionCollisionAPI(_Base):
    _schemaName = "PhysxConvexDecompositionCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxConvexDecompositionCollision:hullVertexLimit', 'physxConvexDecompositionCollision:maxConvexHulls', 'physxConvexDecompositionCollision:minThickness', 'physxConvexDecompositionCollision:voxelResolution', 'physxConvexDecompositionCollision:errorPercentage', 'physxConvexDecompositionCollision:shrinkWrap']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetHullVertexLimitAttr(self):
        return self._prim.GetAttribute("physxConvexDecompositionCollision:hullVertexLimit")
    def CreateHullVertexLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexDecompositionCollision:hullVertexLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexDecompositionCollision:hullVertexLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxConvexHullsAttr(self):
        return self._prim.GetAttribute("physxConvexDecompositionCollision:maxConvexHulls")
    def CreateMaxConvexHullsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexDecompositionCollision:maxConvexHulls")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexDecompositionCollision:maxConvexHulls", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinThicknessAttr(self):
        return self._prim.GetAttribute("physxConvexDecompositionCollision:minThickness")
    def CreateMinThicknessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexDecompositionCollision:minThickness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexDecompositionCollision:minThickness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVoxelResolutionAttr(self):
        return self._prim.GetAttribute("physxConvexDecompositionCollision:voxelResolution")
    def CreateVoxelResolutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexDecompositionCollision:voxelResolution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexDecompositionCollision:voxelResolution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetErrorPercentageAttr(self):
        return self._prim.GetAttribute("physxConvexDecompositionCollision:errorPercentage")
    def CreateErrorPercentageAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexDecompositionCollision:errorPercentage")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexDecompositionCollision:errorPercentage", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetShrinkWrapAttr(self):
        return self._prim.GetAttribute("physxConvexDecompositionCollision:shrinkWrap")
    def CreateShrinkWrapAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxConvexDecompositionCollision:shrinkWrap")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxConvexDecompositionCollision:shrinkWrap", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxSphereFillCollisionAPI(_Base):
    _schemaName = "PhysxSphereFillCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxSphereFillCollision:maxSpheres', 'physxSphereFillCollision:voxelResolution', 'physxSphereFillCollision:seedCount', 'physxSphereFillCollision:fillMode']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetMaxSpheresAttr(self):
        return self._prim.GetAttribute("physxSphereFillCollision:maxSpheres")
    def CreateMaxSpheresAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSphereFillCollision:maxSpheres")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSphereFillCollision:maxSpheres", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVoxelResolutionAttr(self):
        return self._prim.GetAttribute("physxSphereFillCollision:voxelResolution")
    def CreateVoxelResolutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSphereFillCollision:voxelResolution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSphereFillCollision:voxelResolution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSeedCountAttr(self):
        return self._prim.GetAttribute("physxSphereFillCollision:seedCount")
    def CreateSeedCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSphereFillCollision:seedCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSphereFillCollision:seedCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFillModeAttr(self):
        return self._prim.GetAttribute("physxSphereFillCollision:fillMode")
    def CreateFillModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSphereFillCollision:fillMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSphereFillCollision:fillMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxTriangleMeshSimplificationCollisionAPI(_Base):
    _schemaName = "PhysxTriangleMeshSimplificationCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxTriangleMeshSimplificationCollision:metric', 'physxTriangleMeshSimplificationCollision:weldTolerance']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSimplificationMetricAttr(self):
        return self._prim.GetAttribute("physxTriangleMeshSimplificationCollision:metric")
    def CreateSimplificationMetricAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTriangleMeshSimplificationCollision:metric")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTriangleMeshSimplificationCollision:metric", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWeldToleranceAttr(self):
        return self._prim.GetAttribute("physxTriangleMeshSimplificationCollision:weldTolerance")
    def CreateWeldToleranceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTriangleMeshSimplificationCollision:weldTolerance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTriangleMeshSimplificationCollision:weldTolerance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxTriangleMeshCollisionAPI(_Base):
    _schemaName = "PhysxTriangleMeshCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxTriangleMeshCollision:weldTolerance']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetWeldToleranceAttr(self):
        return self._prim.GetAttribute("physxTriangleMeshCollision:weldTolerance")
    def CreateWeldToleranceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTriangleMeshCollision:weldTolerance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTriangleMeshCollision:weldTolerance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxSDFMeshCollisionAPI(_Base):
    _schemaName = "PhysxSDFMeshCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxSDFMeshCollision:sdfResolution', 'physxSDFMeshCollision:sdfSubgridResolution', 'physxSDFMeshCollision:sdfBitsPerSubgridPixel', 'physxSDFMeshCollision:sdfNarrowBandThickness', 'physxSDFMeshCollision:sdfMargin', 'physxSDFMeshCollision:sdfEnableRemeshing', 'physxSDFMeshCollision:sdfTriangleCountReductionFactor']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSdfResolutionAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfResolution")
    def CreateSdfResolutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfResolution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfResolution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSdfSubgridResolutionAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfSubgridResolution")
    def CreateSdfSubgridResolutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfSubgridResolution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfSubgridResolution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSdfBitsPerSubgridPixelAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfBitsPerSubgridPixel")
    def CreateSdfBitsPerSubgridPixelAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfBitsPerSubgridPixel")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfBitsPerSubgridPixel", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSdfNarrowBandThicknessAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfNarrowBandThickness")
    def CreateSdfNarrowBandThicknessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfNarrowBandThickness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfNarrowBandThickness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSdfMarginAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfMargin")
    def CreateSdfMarginAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfMargin")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfMargin", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSdfEnableRemeshingAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfEnableRemeshing")
    def CreateSdfEnableRemeshingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfEnableRemeshing")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfEnableRemeshing", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSdfTriangleCountReductionFactorAttr(self):
        return self._prim.GetAttribute("physxSDFMeshCollision:sdfTriangleCountReductionFactor")
    def CreateSdfTriangleCountReductionFactorAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSDFMeshCollision:sdfTriangleCountReductionFactor")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSDFMeshCollision:sdfTriangleCountReductionFactor", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxSurfaceVelocityAPI(_Base):
    _schemaName = "PhysxSurfaceVelocityAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxSurfaceVelocity:surfaceVelocityEnabled', 'physxSurfaceVelocity:surfaceVelocityLocalSpace', 'physxSurfaceVelocity:surfaceVelocity', 'physxSurfaceVelocity:surfaceAngularVelocity']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSurfaceVelocityEnabledAttr(self):
        return self._prim.GetAttribute("physxSurfaceVelocity:surfaceVelocityEnabled")
    def CreateSurfaceVelocityEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSurfaceVelocity:surfaceVelocityEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSurfaceVelocity:surfaceVelocityEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceVelocityLocalSpaceAttr(self):
        return self._prim.GetAttribute("physxSurfaceVelocity:surfaceVelocityLocalSpace")
    def CreateSurfaceVelocityLocalSpaceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSurfaceVelocity:surfaceVelocityLocalSpace")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSurfaceVelocity:surfaceVelocityLocalSpace", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceVelocityAttr(self):
        return self._prim.GetAttribute("physxSurfaceVelocity:surfaceVelocity")
    def CreateSurfaceVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Vector3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSurfaceVelocity:surfaceVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSurfaceVelocity:surfaceVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceAngularVelocityAttr(self):
        return self._prim.GetAttribute("physxSurfaceVelocity:surfaceAngularVelocity")
    def CreateSurfaceAngularVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Vector3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSurfaceVelocity:surfaceAngularVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSurfaceVelocity:surfaceAngularVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxMeshMergeCollisionAPI(_Base):
    _schemaName = "PhysxMeshMergeCollisionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = []
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetCollisionMeshesCollectionAPI(self):
        return Usd.CollectionAPI(self._prim, "collisionmeshes")

class PhysxSceneQuasistaticAPI(_Base):
    _schemaName = "PhysxSceneQuasistaticAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxSceneQuasistatic:enableQuasistatic']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetEnableQuasistaticAttr(self):
        return self._prim.GetAttribute("physxSceneQuasistatic:enableQuasistatic")
    def CreateEnableQuasistaticAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSceneQuasistatic:enableQuasistatic")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSceneQuasistatic:enableQuasistatic", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetQuasistaticActorsCollectionAPI(self):
        return Usd.CollectionAPI(self._prim, "quasistaticactors")

class PhysxMaterialAPI(_Base):
    _schemaName = "PhysxMaterialAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxMaterial:frictionCombineMode', 'physxMaterial:restitutionCombineMode', 'physxMaterial:dampingCombineMode', 'physxMaterial:compliantContactAccelerationSpring', 'physxMaterial:compliantContactStiffness', 'physxMaterial:compliantContactDamping']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetFrictionCombineModeAttr(self):
        return self._prim.GetAttribute("physxMaterial:frictionCombineMode")
    def CreateFrictionCombineModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMaterial:frictionCombineMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMaterial:frictionCombineMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRestitutionCombineModeAttr(self):
        return self._prim.GetAttribute("physxMaterial:restitutionCombineMode")
    def CreateRestitutionCombineModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMaterial:restitutionCombineMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMaterial:restitutionCombineMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingCombineModeAttr(self):
        return self._prim.GetAttribute("physxMaterial:dampingCombineMode")
    def CreateDampingCombineModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMaterial:dampingCombineMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMaterial:dampingCombineMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCompliantContactAccelerationSpringAttr(self):
        return self._prim.GetAttribute("physxMaterial:compliantContactAccelerationSpring")
    def CreateCompliantContactAccelerationSpringAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMaterial:compliantContactAccelerationSpring")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMaterial:compliantContactAccelerationSpring", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCompliantContactStiffnessAttr(self):
        return self._prim.GetAttribute("physxMaterial:compliantContactStiffness")
    def CreateCompliantContactStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMaterial:compliantContactStiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMaterial:compliantContactStiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCompliantContactDampingAttr(self):
        return self._prim.GetAttribute("physxMaterial:compliantContactDamping")
    def CreateCompliantContactDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMaterial:compliantContactDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMaterial:compliantContactDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxPhysicsInstancer(_Base):
    _schemaName = "PhysxPhysicsInstancer"
    schemaKind = 'typed'
    _schemaAttributeNames = ['physics:protoIndices']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def Define(cls, stage, path): return cls(stage.DefinePrim(path, cls._schemaName))
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        t = self._GetStaticTfType()
        return bool(t.typeName) and self._prim.IsA(t)
    def GetPhysicsProtoIndicesAttr(self):
        return self._prim.GetAttribute("physics:protoIndices")
    def CreatePhysicsProtoIndicesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:protoIndices")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:protoIndices", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsPrototypesRel(self):
        return self._prim.GetRelationship("physics:prototypes")
    def CreatePhysicsPrototypesRel(self):
        return self._prim.CreateRelationship("physics:prototypes", False)

class PhysxPhysicsJointInstancer(PhysxPhysicsInstancer):
    _schemaName = "PhysxPhysicsJointInstancer"
    schemaKind = 'typed'
    _schemaAttributeNames = ['physics:body0Indices', 'physics:body1Indices', 'physics:localPos0s', 'physics:localRot0s', 'physics:localPos1s', 'physics:localRot1s']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def Define(cls, stage, path): return cls(stage.DefinePrim(path, cls._schemaName))
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        t = self._GetStaticTfType()
        return bool(t.typeName) and self._prim.IsA(t)
    def GetPhysicsBody0IndicesAttr(self):
        return self._prim.GetAttribute("physics:body0Indices")
    def CreatePhysicsBody0IndicesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:body0Indices")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:body0Indices", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsBody1IndicesAttr(self):
        return self._prim.GetAttribute("physics:body1Indices")
    def CreatePhysicsBody1IndicesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:body1Indices")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:body1Indices", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsLocalPos0sAttr(self):
        return self._prim.GetAttribute("physics:localPos0s")
    def CreatePhysicsLocalPos0sAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Point3fArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:localPos0s")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:localPos0s", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsLocalRot0sAttr(self):
        return self._prim.GetAttribute("physics:localRot0s")
    def CreatePhysicsLocalRot0sAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.QuathArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:localRot0s")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:localRot0s", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsLocalPos1sAttr(self):
        return self._prim.GetAttribute("physics:localPos1s")
    def CreatePhysicsLocalPos1sAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Point3fArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:localPos1s")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:localPos1s", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsLocalRot1sAttr(self):
        return self._prim.GetAttribute("physics:localRot1s")
    def CreatePhysicsLocalRot1sAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.QuathArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:localRot1s")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:localRot1s", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysicsBody0sRel(self):
        return self._prim.GetRelationship("physics:body0s")
    def CreatePhysicsBody0sRel(self):
        return self._prim.CreateRelationship("physics:body0s", False)
    def GetPhysicsBody1sRel(self):
        return self._prim.GetRelationship("physics:body1s")
    def CreatePhysicsBody1sRel(self):
        return self._prim.CreateRelationship("physics:body1s", False)

class PhysxPhysicsGearJoint(_Base):
    _schemaName = "PhysxPhysicsGearJoint"
    schemaKind = 'typed'
    _schemaAttributeNames = ['physics:gearRatio']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def Define(cls, stage, path): return cls(stage.DefinePrim(path, cls._schemaName))
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        t = self._GetStaticTfType()
        return bool(t.typeName) and self._prim.IsA(t)
    def GetGearRatioAttr(self):
        return self._prim.GetAttribute("physics:gearRatio")
    def CreateGearRatioAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:gearRatio")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:gearRatio", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetHinge0Rel(self):
        return self._prim.GetRelationship("physics:hinge0")
    def CreateHinge0Rel(self):
        return self._prim.CreateRelationship("physics:hinge0", False)
    def GetHinge1Rel(self):
        return self._prim.GetRelationship("physics:hinge1")
    def CreateHinge1Rel(self):
        return self._prim.CreateRelationship("physics:hinge1", False)

class PhysxPhysicsRackAndPinionJoint(_Base):
    _schemaName = "PhysxPhysicsRackAndPinionJoint"
    schemaKind = 'typed'
    _schemaAttributeNames = ['physics:ratio']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def Define(cls, stage, path): return cls(stage.DefinePrim(path, cls._schemaName))
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        t = self._GetStaticTfType()
        return bool(t.typeName) and self._prim.IsA(t)
    def GetRatioAttr(self):
        return self._prim.GetAttribute("physics:ratio")
    def CreateRatioAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physics:ratio")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physics:ratio", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetHingeRel(self):
        return self._prim.GetRelationship("physics:hinge")
    def CreateHingeRel(self):
        return self._prim.CreateRelationship("physics:hinge", False)
    def GetPrismaticRel(self):
        return self._prim.GetRelationship("physics:prismatic")
    def CreatePrismaticRel(self):
        return self._prim.CreateRelationship("physics:prismatic", False)

class PhysxJointAPI(_Base):
    _schemaName = "PhysxJointAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxJoint:jointFriction', 'physxJoint:maxJointVelocity', 'physxJoint:armature']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetJointFrictionAttr(self):
        return self._prim.GetAttribute("physxJoint:jointFriction")
    def CreateJointFrictionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJoint:jointFriction")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJoint:jointFriction", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxJointVelocityAttr(self):
        return self._prim.GetAttribute("physxJoint:maxJointVelocity")
    def CreateMaxJointVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJoint:maxJointVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJoint:maxJointVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetArmatureAttr(self):
        return self._prim.GetAttribute("physxJoint:armature")
    def CreateArmatureAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJoint:armature")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJoint:armature", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxPhysicsDistanceJointAPI(_Base):
    _schemaName = "PhysxPhysicsDistanceJointAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxPhysicsDistanceJoint:springEnabled', 'physxPhysicsDistanceJoint:springStiffness', 'physxPhysicsDistanceJoint:springDamping']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSpringEnabledAttr(self):
        return self._prim.GetAttribute("physxPhysicsDistanceJoint:springEnabled")
    def CreateSpringEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPhysicsDistanceJoint:springEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPhysicsDistanceJoint:springEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSpringStiffnessAttr(self):
        return self._prim.GetAttribute("physxPhysicsDistanceJoint:springStiffness")
    def CreateSpringStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPhysicsDistanceJoint:springStiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPhysicsDistanceJoint:springStiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSpringDampingAttr(self):
        return self._prim.GetAttribute("physxPhysicsDistanceJoint:springDamping")
    def CreateSpringDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPhysicsDistanceJoint:springDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPhysicsDistanceJoint:springDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxLimitAPI(_Base):
    _schemaName = "PhysxLimitAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxLimit:__INSTANCE_NAME__:restitution', 'physxLimit:__INSTANCE_NAME__:bounceThreshold', 'physxLimit:__INSTANCE_NAME__:stiffness', 'physxLimit:__INSTANCE_NAME__:damping']
    _propertyNamespacePrefix = 'physxLimit'
    _schemaPropertyBaseNames = ['bounceThreshold', 'damping', 'restitution', 'stiffness']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxLimitAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetRestitutionAttr(self):
        return self._prim.GetAttribute("physxLimit:" + self._name + ":restitution")
    def CreateRestitutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxLimit:" + self._name + ":restitution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxLimit:" + self._name + ":restitution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBounceThresholdAttr(self):
        return self._prim.GetAttribute("physxLimit:" + self._name + ":bounceThreshold")
    def CreateBounceThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxLimit:" + self._name + ":bounceThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxLimit:" + self._name + ":bounceThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStiffnessAttr(self):
        return self._prim.GetAttribute("physxLimit:" + self._name + ":stiffness")
    def CreateStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxLimit:" + self._name + ":stiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxLimit:" + self._name + ":stiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingAttr(self):
        return self._prim.GetAttribute("physxLimit:" + self._name + ":damping")
    def CreateDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxLimit:" + self._name + ":damping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxLimit:" + self._name + ":damping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class JointStateAPI(_Base):
    _schemaName = "PhysicsJointStateAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['state:__INSTANCE_NAME__:physics:position', 'state:__INSTANCE_NAME__:physics:velocity']
    _propertyNamespacePrefix = 'state'
    _schemaPropertyBaseNames = ['position', 'velocity']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysicsJointStateAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetPositionAttr(self):
        return self._prim.GetAttribute("state:" + self._name + ":physics:position")
    def CreatePositionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("state:" + self._name + ":physics:position")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("state:" + self._name + ":physics:position", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVelocityAttr(self):
        return self._prim.GetAttribute("state:" + self._name + ":physics:velocity")
    def CreateVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("state:" + self._name + ":physics:velocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("state:" + self._name + ":physics:velocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxArticulationAPI(_Base):
    _schemaName = "PhysxArticulationAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxArticulation:articulationEnabled', 'physxArticulation:solverPositionIterationCount', 'physxArticulation:solverVelocityIterationCount', 'physxArticulation:sleepThreshold', 'physxArticulation:stabilizationThreshold', 'physxArticulation:enabledSelfCollisions']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetArticulationEnabledAttr(self):
        return self._prim.GetAttribute("physxArticulation:articulationEnabled")
    def CreateArticulationEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxArticulation:articulationEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxArticulation:articulationEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolverPositionIterationCountAttr(self):
        return self._prim.GetAttribute("physxArticulation:solverPositionIterationCount")
    def CreateSolverPositionIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxArticulation:solverPositionIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxArticulation:solverPositionIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolverVelocityIterationCountAttr(self):
        return self._prim.GetAttribute("physxArticulation:solverVelocityIterationCount")
    def CreateSolverVelocityIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxArticulation:solverVelocityIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxArticulation:solverVelocityIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSleepThresholdAttr(self):
        return self._prim.GetAttribute("physxArticulation:sleepThreshold")
    def CreateSleepThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxArticulation:sleepThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxArticulation:sleepThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStabilizationThresholdAttr(self):
        return self._prim.GetAttribute("physxArticulation:stabilizationThreshold")
    def CreateStabilizationThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxArticulation:stabilizationThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxArticulation:stabilizationThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnabledSelfCollisionsAttr(self):
        return self._prim.GetAttribute("physxArticulation:enabledSelfCollisions")
    def CreateEnabledSelfCollisionsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxArticulation:enabledSelfCollisions")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxArticulation:enabledSelfCollisions", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxForceAPI(_Base):
    _schemaName = "PhysxForceAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxForce:forceEnabled', 'physxForce:worldFrameEnabled', 'physxForce:mode', 'physxForce:force', 'physxForce:torque']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetForceEnabledAttr(self):
        return self._prim.GetAttribute("physxForce:forceEnabled")
    def CreateForceEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxForce:forceEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxForce:forceEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWorldFrameEnabledAttr(self):
        return self._prim.GetAttribute("physxForce:worldFrameEnabled")
    def CreateWorldFrameEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxForce:worldFrameEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxForce:worldFrameEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetModeAttr(self):
        return self._prim.GetAttribute("physxForce:mode")
    def CreateModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxForce:mode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxForce:mode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetForceAttr(self):
        return self._prim.GetAttribute("physxForce:force")
    def CreateForceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Vector3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxForce:force")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxForce:force", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTorqueAttr(self):
        return self._prim.GetAttribute("physxForce:torque")
    def CreateTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Vector3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxForce:torque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxForce:torque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxCharacterControllerAPI(_Base):
    _schemaName = "PhysxCharacterControllerAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxCharacterController:slopeLimit', 'physxCharacterController:moveTarget', 'physxCharacterController:upAxis', 'physxCharacterController:nonWalkableMode', 'physxCharacterController:climbingMode', 'physxCharacterController:invisibleWallHeight', 'physxCharacterController:maxJumpHeight', 'physxCharacterController:contactOffset', 'physxCharacterController:stepOffset', 'physxCharacterController:scaleCoeff', 'physxCharacterController:volumeGrowth']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSlopeLimitAttr(self):
        return self._prim.GetAttribute("physxCharacterController:slopeLimit")
    def CreateSlopeLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:slopeLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:slopeLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMoveTargetAttr(self):
        return self._prim.GetAttribute("physxCharacterController:moveTarget")
    def CreateMoveTargetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Vector3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:moveTarget")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:moveTarget", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpAxisAttr(self):
        return self._prim.GetAttribute("physxCharacterController:upAxis")
    def CreateUpAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:upAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:upAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetNonWalkableModeAttr(self):
        return self._prim.GetAttribute("physxCharacterController:nonWalkableMode")
    def CreateNonWalkableModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:nonWalkableMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:nonWalkableMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetClimbingModeAttr(self):
        return self._prim.GetAttribute("physxCharacterController:climbingMode")
    def CreateClimbingModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:climbingMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:climbingMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetInvisibleWallHeightAttr(self):
        return self._prim.GetAttribute("physxCharacterController:invisibleWallHeight")
    def CreateInvisibleWallHeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:invisibleWallHeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:invisibleWallHeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxJumpHeightAttr(self):
        return self._prim.GetAttribute("physxCharacterController:maxJumpHeight")
    def CreateMaxJumpHeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:maxJumpHeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:maxJumpHeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetContactOffsetAttr(self):
        return self._prim.GetAttribute("physxCharacterController:contactOffset")
    def CreateContactOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:contactOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:contactOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStepOffsetAttr(self):
        return self._prim.GetAttribute("physxCharacterController:stepOffset")
    def CreateStepOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:stepOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:stepOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetScaleCoeffAttr(self):
        return self._prim.GetAttribute("physxCharacterController:scaleCoeff")
    def CreateScaleCoeffAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:scaleCoeff")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:scaleCoeff", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVolumeGrowthAttr(self):
        return self._prim.GetAttribute("physxCharacterController:volumeGrowth")
    def CreateVolumeGrowthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCharacterController:volumeGrowth")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCharacterController:volumeGrowth", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSimulationOwnerRel(self):
        return self._prim.GetRelationship("physxCharacterController:simulationOwner")
    def CreateSimulationOwnerRel(self):
        return self._prim.CreateRelationship("physxCharacterController:simulationOwner", False)

class PhysxTriggerAPI(_Base):
    _schemaName = "PhysxTriggerAPI"
    schemaKind = 'single'
    _schemaAttributeNames = []
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)

class PhysxTriggerStateAPI(_Base):
    _schemaName = "PhysxTriggerStateAPI"
    schemaKind = 'single'
    _schemaAttributeNames = []
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetTriggeredCollisionsRel(self):
        return self._prim.GetRelationship("physxTrigger:triggeredCollisions")
    def CreateTriggeredCollisionsRel(self):
        return self._prim.CreateRelationship("physxTrigger:triggeredCollisions", False)

class PhysxCookedDataAPI(_Base):
    _schemaName = "PhysxCookedDataAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxCookedData:__INSTANCE_NAME__:buffer']
    _propertyNamespacePrefix = 'physxCookedData'
    _schemaPropertyBaseNames = ['buffer']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxCookedDataAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetBufferAttr(self):
        return self._prim.GetAttribute("physxCookedData:" + self._name + ":buffer")
    def CreateBufferAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UCharArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxCookedData:" + self._name + ":buffer")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxCookedData:" + self._name + ":buffer", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxTendonAxisAPI(_Base):
    _schemaName = "PhysxTendonAxisAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxTendon:__INSTANCE_NAME__:gearing', 'physxTendon:__INSTANCE_NAME__:forceCoefficient', 'physxTendon:__INSTANCE_NAME__:jointAxis']
    _propertyNamespacePrefix = 'physxTendon'
    _schemaPropertyBaseNames = ['forceCoefficient', 'gearing', 'jointAxis']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxTendonAxisAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetGearingAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":gearing")
    def CreateGearingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":gearing")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":gearing", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetForceCoefficientAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":forceCoefficient")
    def CreateForceCoefficientAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":forceCoefficient")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":forceCoefficient", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetJointAxisAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":jointAxis")
    def CreateJointAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.TokenArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":jointAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":jointAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxTendonAxisRootAPI(_Base):
    _schemaName = "PhysxTendonAxisRootAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxTendon:__INSTANCE_NAME__:stiffness', 'physxTendon:__INSTANCE_NAME__:damping', 'physxTendon:__INSTANCE_NAME__:limitStiffness', 'physxTendon:__INSTANCE_NAME__:offset', 'physxTendon:__INSTANCE_NAME__:restLength', 'physxTendon:__INSTANCE_NAME__:lowerLimit', 'physxTendon:__INSTANCE_NAME__:upperLimit', 'physxTendon:__INSTANCE_NAME__:tendonEnabled']
    _propertyNamespacePrefix = 'physxTendon'
    _schemaPropertyBaseNames = ['damping', 'limitStiffness', 'lowerLimit', 'offset', 'restLength', 'stiffness', 'tendonEnabled', 'upperLimit']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxTendonAxisRootAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetStiffnessAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":stiffness")
    def CreateStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":stiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":stiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":damping")
    def CreateDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":damping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":damping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLimitStiffnessAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":limitStiffness")
    def CreateLimitStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":limitStiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":limitStiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetOffsetAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":offset")
    def CreateOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":offset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":offset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRestLengthAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":restLength")
    def CreateRestLengthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":restLength")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":restLength", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLowerLimitAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":lowerLimit")
    def CreateLowerLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":lowerLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":lowerLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpperLimitAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":upperLimit")
    def CreateUpperLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":upperLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":upperLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTendonEnabledAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":tendonEnabled")
    def CreateTendonEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":tendonEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":tendonEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxTendonAttachmentAPI(_Base):
    _schemaName = "PhysxTendonAttachmentAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxTendon:__INSTANCE_NAME__:gearing', 'physxTendon:__INSTANCE_NAME__:localPos', 'physxTendon:__INSTANCE_NAME__:parentAttachment']
    _propertyNamespacePrefix = 'physxTendon'
    _schemaPropertyBaseNames = ['gearing', 'localPos', 'parentAttachment', 'parentLink']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxTendonAttachmentAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetGearingAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":gearing")
    def CreateGearingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":gearing")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":gearing", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLocalPosAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":localPos")
    def CreateLocalPosAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Point3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":localPos")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":localPos", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParentAttachmentAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":parentAttachment")
    def CreateParentAttachmentAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":parentAttachment")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":parentAttachment", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParentLinkRel(self):
        return self._prim.GetRelationship("physxTendon:" + self._name + ":parentLink")
    def CreateParentLinkRel(self):
        return self._prim.CreateRelationship("physxTendon:" + self._name + ":parentLink", False)
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxTendonAttachmentRootAPI(_Base):
    _schemaName = "PhysxTendonAttachmentRootAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxTendon:__INSTANCE_NAME__:tendonEnabled', 'physxTendon:__INSTANCE_NAME__:stiffness', 'physxTendon:__INSTANCE_NAME__:damping', 'physxTendon:__INSTANCE_NAME__:limitStiffness', 'physxTendon:__INSTANCE_NAME__:offset']
    _propertyNamespacePrefix = 'physxTendon'
    _schemaPropertyBaseNames = ['damping', 'limitStiffness', 'offset', 'stiffness', 'tendonEnabled']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxTendonAttachmentRootAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetTendonEnabledAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":tendonEnabled")
    def CreateTendonEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":tendonEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":tendonEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStiffnessAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":stiffness")
    def CreateStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":stiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":stiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":damping")
    def CreateDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":damping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":damping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLimitStiffnessAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":limitStiffness")
    def CreateLimitStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":limitStiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":limitStiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetOffsetAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":offset")
    def CreateOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":offset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":offset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxTendonAttachmentLeafAPI(_Base):
    _schemaName = "PhysxTendonAttachmentLeafAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxTendon:__INSTANCE_NAME__:restLength', 'physxTendon:__INSTANCE_NAME__:lowerLimit', 'physxTendon:__INSTANCE_NAME__:upperLimit']
    _propertyNamespacePrefix = 'physxTendon'
    _schemaPropertyBaseNames = ['lowerLimit', 'restLength', 'upperLimit']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxTendonAttachmentLeafAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetRestLengthAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":restLength")
    def CreateRestLengthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":restLength")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":restLength", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLowerLimitAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":lowerLimit")
    def CreateLowerLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":lowerLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":lowerLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpperLimitAttr(self):
        return self._prim.GetAttribute("physxTendon:" + self._name + ":upperLimit")
    def CreateUpperLimitAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxTendon:" + self._name + ":upperLimit")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxTendon:" + self._name + ":upperLimit", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxMimicJointAPI(_Base):
    _schemaName = "PhysxMimicJointAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxMimicJoint:__INSTANCE_NAME__:referenceJointAxis', 'physxMimicJoint:__INSTANCE_NAME__:gearing', 'physxMimicJoint:__INSTANCE_NAME__:offset', 'physxMimicJoint:__INSTANCE_NAME__:naturalFrequency', 'physxMimicJoint:__INSTANCE_NAME__:dampingRatio']
    _propertyNamespacePrefix = 'physxMimicJoint'
    _schemaPropertyBaseNames = ['dampingRatio', 'gearing', 'naturalFrequency', 'offset', 'referenceJoint', 'referenceJointAxis']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxMimicJointAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetReferenceJointAxisAttr(self):
        return self._prim.GetAttribute("physxMimicJoint:" + self._name + ":referenceJointAxis")
    def CreateReferenceJointAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMimicJoint:" + self._name + ":referenceJointAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMimicJoint:" + self._name + ":referenceJointAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGearingAttr(self):
        return self._prim.GetAttribute("physxMimicJoint:" + self._name + ":gearing")
    def CreateGearingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMimicJoint:" + self._name + ":gearing")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMimicJoint:" + self._name + ":gearing", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetOffsetAttr(self):
        return self._prim.GetAttribute("physxMimicJoint:" + self._name + ":offset")
    def CreateOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMimicJoint:" + self._name + ":offset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMimicJoint:" + self._name + ":offset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetNaturalFrequencyAttr(self):
        return self._prim.GetAttribute("physxMimicJoint:" + self._name + ":naturalFrequency")
    def CreateNaturalFrequencyAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMimicJoint:" + self._name + ":naturalFrequency")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMimicJoint:" + self._name + ":naturalFrequency", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingRatioAttr(self):
        return self._prim.GetAttribute("physxMimicJoint:" + self._name + ":dampingRatio")
    def CreateDampingRatioAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxMimicJoint:" + self._name + ":dampingRatio")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxMimicJoint:" + self._name + ":dampingRatio", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetReferenceJointRel(self):
        return self._prim.GetRelationship("physxMimicJoint:" + self._name + ":referenceJoint")
    def CreateReferenceJointRel(self):
        return self._prim.CreateRelationship("physxMimicJoint:" + self._name + ":referenceJoint", False)
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxVehicleContextAPI(_Base):
    _schemaName = "PhysxVehicleContextAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleContext:updateMode', 'physxVehicleContext:upAxis', 'physxVehicleContext:forwardAxis', 'physxVehicleContext:verticalAxis', 'physxVehicleContext:longitudinalAxis']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetUpdateModeAttr(self):
        return self._prim.GetAttribute("physxVehicleContext:updateMode")
    def CreateUpdateModeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleContext:updateMode")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleContext:updateMode", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpAxisAttr(self):
        return self._prim.GetAttribute("physxVehicleContext:upAxis")
    def CreateUpAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleContext:upAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleContext:upAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetForwardAxisAttr(self):
        return self._prim.GetAttribute("physxVehicleContext:forwardAxis")
    def CreateForwardAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleContext:forwardAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleContext:forwardAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVerticalAxisAttr(self):
        return self._prim.GetAttribute("physxVehicleContext:verticalAxis")
    def CreateVerticalAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleContext:verticalAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleContext:verticalAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLongitudinalAxisAttr(self):
        return self._prim.GetAttribute("physxVehicleContext:longitudinalAxis")
    def CreateLongitudinalAxisAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleContext:longitudinalAxis")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleContext:longitudinalAxis", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleTireFrictionTable(_Base):
    _schemaName = "PhysxVehicleTireFrictionTable"
    schemaKind = 'typed'
    _schemaAttributeNames = ['frictionValues', 'defaultFrictionValue']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def Define(cls, stage, path): return cls(stage.DefinePrim(path, cls._schemaName))
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        t = self._GetStaticTfType()
        return bool(t.typeName) and self._prim.IsA(t)
    def GetFrictionValuesAttr(self):
        return self._prim.GetAttribute("frictionValues")
    def CreateFrictionValuesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("frictionValues")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("frictionValues", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDefaultFrictionValueAttr(self):
        return self._prim.GetAttribute("defaultFrictionValue")
    def CreateDefaultFrictionValueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("defaultFrictionValue")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("defaultFrictionValue", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGroundMaterialsRel(self):
        return self._prim.GetRelationship("groundMaterials")
    def CreateGroundMaterialsRel(self):
        return self._prim.CreateRelationship("groundMaterials", False)

class PhysxVehicleWheelAPI(_Base):
    _schemaName = "PhysxVehicleWheelAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleWheel:radius', 'physxVehicleWheel:width', 'physxVehicleWheel:mass', 'physxVehicleWheel:moi', 'physxVehicleWheel:dampingRate', 'physxVehicleWheel:maxBrakeTorque', 'physxVehicleWheel:maxHandBrakeTorque', 'physxVehicleWheel:maxSteerAngle', 'physxVehicleWheel:toeAngle']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetRadiusAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:radius")
    def CreateRadiusAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:radius")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:radius", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWidthAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:width")
    def CreateWidthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:width")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:width", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMassAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:mass")
    def CreateMassAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:mass")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:mass", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMoiAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:moi")
    def CreateMoiAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:moi")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:moi", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingRateAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:dampingRate")
    def CreateDampingRateAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:dampingRate")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:dampingRate", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxBrakeTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:maxBrakeTorque")
    def CreateMaxBrakeTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:maxBrakeTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:maxBrakeTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxHandBrakeTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:maxHandBrakeTorque")
    def CreateMaxHandBrakeTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:maxHandBrakeTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:maxHandBrakeTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxSteerAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:maxSteerAngle")
    def CreateMaxSteerAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:maxSteerAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:maxSteerAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetToeAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleWheel:toeAngle")
    def CreateToeAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheel:toeAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheel:toeAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleTireAPI(_Base):
    _schemaName = "PhysxVehicleTireAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleTire:latStiffX', 'physxVehicleTire:latStiffY', 'physxVehicleTire:lateralStiffnessGraph', 'physxVehicleTire:longitudinalStiffnessPerUnitGravity', 'physxVehicleTire:longitudinalStiffness', 'physxVehicleTire:camberStiffnessPerUnitGravity', 'physxVehicleTire:camberStiffness', 'physxVehicleTire:frictionVsSlipGraph', 'physxVehicleTire:restLoad']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetLatStiffXAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:latStiffX")
    def CreateLatStiffXAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:latStiffX")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:latStiffX", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLatStiffYAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:latStiffY")
    def CreateLatStiffYAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:latStiffY")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:latStiffY", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLateralStiffnessGraphAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:lateralStiffnessGraph")
    def CreateLateralStiffnessGraphAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float2
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:lateralStiffnessGraph")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:lateralStiffnessGraph", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLongitudinalStiffnessPerUnitGravityAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:longitudinalStiffnessPerUnitGravity")
    def CreateLongitudinalStiffnessPerUnitGravityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:longitudinalStiffnessPerUnitGravity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:longitudinalStiffnessPerUnitGravity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLongitudinalStiffnessAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:longitudinalStiffness")
    def CreateLongitudinalStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:longitudinalStiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:longitudinalStiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCamberStiffnessPerUnitGravityAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:camberStiffnessPerUnitGravity")
    def CreateCamberStiffnessPerUnitGravityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:camberStiffnessPerUnitGravity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:camberStiffnessPerUnitGravity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCamberStiffnessAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:camberStiffness")
    def CreateCamberStiffnessAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:camberStiffness")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:camberStiffness", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFrictionVsSlipGraphAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:frictionVsSlipGraph")
    def CreateFrictionVsSlipGraphAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float2Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:frictionVsSlipGraph")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:frictionVsSlipGraph", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRestLoadAttr(self):
        return self._prim.GetAttribute("physxVehicleTire:restLoad")
    def CreateRestLoadAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTire:restLoad")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTire:restLoad", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFrictionTableRel(self):
        return self._prim.GetRelationship("physxVehicleTire:frictionTable")
    def CreateFrictionTableRel(self):
        return self._prim.CreateRelationship("physxVehicleTire:frictionTable", False)

class PhysxVehicleSuspensionAPI(_Base):
    _schemaName = "PhysxVehicleSuspensionAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleSuspension:springStrength', 'physxVehicleSuspension:springDamperRate', 'physxVehicleSuspension:travelDistance', 'physxVehicleSuspension:maxCompression', 'physxVehicleSuspension:maxDroop', 'physxVehicleSuspension:sprungMass', 'physxVehicleSuspension:camberAtRest', 'physxVehicleSuspension:camberAtMaxCompression', 'physxVehicleSuspension:camberAtMaxDroop']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSpringStrengthAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:springStrength")
    def CreateSpringStrengthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:springStrength")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:springStrength", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSpringDamperRateAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:springDamperRate")
    def CreateSpringDamperRateAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:springDamperRate")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:springDamperRate", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTravelDistanceAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:travelDistance")
    def CreateTravelDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:travelDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:travelDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxCompressionAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:maxCompression")
    def CreateMaxCompressionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:maxCompression")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:maxCompression", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxDroopAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:maxDroop")
    def CreateMaxDroopAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:maxDroop")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:maxDroop", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSprungMassAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:sprungMass")
    def CreateSprungMassAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:sprungMass")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:sprungMass", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCamberAtRestAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:camberAtRest")
    def CreateCamberAtRestAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:camberAtRest")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:camberAtRest", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCamberAtMaxCompressionAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:camberAtMaxCompression")
    def CreateCamberAtMaxCompressionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:camberAtMaxCompression")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:camberAtMaxCompression", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCamberAtMaxDroopAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspension:camberAtMaxDroop")
    def CreateCamberAtMaxDroopAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspension:camberAtMaxDroop")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspension:camberAtMaxDroop", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleWheelAttachmentAPI(_Base):
    _schemaName = "PhysxVehicleWheelAttachmentAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleWheelAttachment:index', 'physxVehicleWheelAttachment:suspensionTravelDirection', 'physxVehicleWheelAttachment:suspensionFramePosition', 'physxVehicleWheelAttachment:suspensionFrameOrientation', 'physxVehicleWheelAttachment:suspensionForceAppPointOffset', 'physxVehicleWheelAttachment:wheelCenterOfMassOffset', 'physxVehicleWheelAttachment:tireForceAppPointOffset', 'physxVehicleWheelAttachment:wheelFramePosition', 'physxVehicleWheelAttachment:wheelFrameOrientation', 'physxVehicleWheelAttachment:driven']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetIndexAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:index")
    def CreateIndexAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:index")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:index", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSuspensionTravelDirectionAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionTravelDirection")
    def CreateSuspensionTravelDirectionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Vector3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionTravelDirection")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:suspensionTravelDirection", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSuspensionFramePositionAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionFramePosition")
    def CreateSuspensionFramePositionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Point3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionFramePosition")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:suspensionFramePosition", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSuspensionFrameOrientationAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionFrameOrientation")
    def CreateSuspensionFrameOrientationAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Quatf
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionFrameOrientation")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:suspensionFrameOrientation", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSuspensionForceAppPointOffsetAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionForceAppPointOffset")
    def CreateSuspensionForceAppPointOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:suspensionForceAppPointOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:suspensionForceAppPointOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelCenterOfMassOffsetAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:wheelCenterOfMassOffset")
    def CreateWheelCenterOfMassOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:wheelCenterOfMassOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:wheelCenterOfMassOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTireForceAppPointOffsetAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:tireForceAppPointOffset")
    def CreateTireForceAppPointOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:tireForceAppPointOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:tireForceAppPointOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelFramePositionAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:wheelFramePosition")
    def CreateWheelFramePositionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Point3f
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:wheelFramePosition")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:wheelFramePosition", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelFrameOrientationAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:wheelFrameOrientation")
    def CreateWheelFrameOrientationAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Quatf
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:wheelFrameOrientation")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:wheelFrameOrientation", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDrivenAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelAttachment:driven")
    def CreateDrivenAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelAttachment:driven")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelAttachment:driven", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelRel(self):
        return self._prim.GetRelationship("physxVehicleWheelAttachment:wheel")
    def CreateWheelRel(self):
        return self._prim.CreateRelationship("physxVehicleWheelAttachment:wheel", False)
    def GetTireRel(self):
        return self._prim.GetRelationship("physxVehicleWheelAttachment:tire")
    def CreateTireRel(self):
        return self._prim.CreateRelationship("physxVehicleWheelAttachment:tire", False)
    def GetSuspensionRel(self):
        return self._prim.GetRelationship("physxVehicleWheelAttachment:suspension")
    def CreateSuspensionRel(self):
        return self._prim.CreateRelationship("physxVehicleWheelAttachment:suspension", False)
    def GetCollisionGroupRel(self):
        return self._prim.GetRelationship("physxVehicleWheelAttachment:collisionGroup")
    def CreateCollisionGroupRel(self):
        return self._prim.CreateRelationship("physxVehicleWheelAttachment:collisionGroup", False)

class PhysxVehicleSuspensionComplianceAPI(_Base):
    _schemaName = "PhysxVehicleSuspensionComplianceAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleSuspensionCompliance:wheelToeAngle', 'physxVehicleSuspensionCompliance:wheelCamberAngle', 'physxVehicleSuspensionCompliance:suspensionForceAppPoint', 'physxVehicleSuspensionCompliance:tireForceAppPoint']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetWheelToeAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspensionCompliance:wheelToeAngle")
    def CreateWheelToeAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float2Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspensionCompliance:wheelToeAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspensionCompliance:wheelToeAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelCamberAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspensionCompliance:wheelCamberAngle")
    def CreateWheelCamberAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float2Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspensionCompliance:wheelCamberAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspensionCompliance:wheelCamberAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSuspensionForceAppPointAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspensionCompliance:suspensionForceAppPoint")
    def CreateSuspensionForceAppPointAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float4Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspensionCompliance:suspensionForceAppPoint")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspensionCompliance:suspensionForceAppPoint", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTireForceAppPointAttr(self):
        return self._prim.GetAttribute("physxVehicleSuspensionCompliance:tireForceAppPoint")
    def CreateTireForceAppPointAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float4Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSuspensionCompliance:tireForceAppPoint")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSuspensionCompliance:tireForceAppPoint", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleEngineAPI(_Base):
    _schemaName = "PhysxVehicleEngineAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleEngine:moi', 'physxVehicleEngine:peakTorque', 'physxVehicleEngine:maxRotationSpeed', 'physxVehicleEngine:idleRotationSpeed', 'physxVehicleEngine:torqueCurve', 'physxVehicleEngine:dampingRateFullThrottle', 'physxVehicleEngine:dampingRateZeroThrottleClutchEngaged', 'physxVehicleEngine:dampingRateZeroThrottleClutchDisengaged']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetMoiAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:moi")
    def CreateMoiAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:moi")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:moi", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPeakTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:peakTorque")
    def CreatePeakTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:peakTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:peakTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxRotationSpeedAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:maxRotationSpeed")
    def CreateMaxRotationSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:maxRotationSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:maxRotationSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetIdleRotationSpeedAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:idleRotationSpeed")
    def CreateIdleRotationSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:idleRotationSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:idleRotationSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTorqueCurveAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:torqueCurve")
    def CreateTorqueCurveAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float2Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:torqueCurve")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:torqueCurve", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingRateFullThrottleAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:dampingRateFullThrottle")
    def CreateDampingRateFullThrottleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:dampingRateFullThrottle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:dampingRateFullThrottle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingRateZeroThrottleClutchEngagedAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:dampingRateZeroThrottleClutchEngaged")
    def CreateDampingRateZeroThrottleClutchEngagedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:dampingRateZeroThrottleClutchEngaged")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:dampingRateZeroThrottleClutchEngaged", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingRateZeroThrottleClutchDisengagedAttr(self):
        return self._prim.GetAttribute("physxVehicleEngine:dampingRateZeroThrottleClutchDisengaged")
    def CreateDampingRateZeroThrottleClutchDisengagedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleEngine:dampingRateZeroThrottleClutchDisengaged")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleEngine:dampingRateZeroThrottleClutchDisengaged", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleGearsAPI(_Base):
    _schemaName = "PhysxVehicleGearsAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleGears:ratios', 'physxVehicleGears:ratioScale', 'physxVehicleGears:switchTime']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetRatiosAttr(self):
        return self._prim.GetAttribute("physxVehicleGears:ratios")
    def CreateRatiosAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleGears:ratios")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleGears:ratios", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRatioScaleAttr(self):
        return self._prim.GetAttribute("physxVehicleGears:ratioScale")
    def CreateRatioScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleGears:ratioScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleGears:ratioScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSwitchTimeAttr(self):
        return self._prim.GetAttribute("physxVehicleGears:switchTime")
    def CreateSwitchTimeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleGears:switchTime")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleGears:switchTime", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleAutoGearBoxAPI(_Base):
    _schemaName = "PhysxVehicleAutoGearBoxAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleAutoGearBox:upRatios', 'physxVehicleAutoGearBox:downRatios', 'physxVehicleAutoGearBox:latency']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetUpRatiosAttr(self):
        return self._prim.GetAttribute("physxVehicleAutoGearBox:upRatios")
    def CreateUpRatiosAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAutoGearBox:upRatios")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAutoGearBox:upRatios", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDownRatiosAttr(self):
        return self._prim.GetAttribute("physxVehicleAutoGearBox:downRatios")
    def CreateDownRatiosAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAutoGearBox:downRatios")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAutoGearBox:downRatios", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLatencyAttr(self):
        return self._prim.GetAttribute("physxVehicleAutoGearBox:latency")
    def CreateLatencyAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAutoGearBox:latency")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAutoGearBox:latency", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleClutchAPI(_Base):
    _schemaName = "PhysxVehicleClutchAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleClutch:strength']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetStrengthAttr(self):
        return self._prim.GetAttribute("physxVehicleClutch:strength")
    def CreateStrengthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleClutch:strength")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleClutch:strength", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleDriveBasicAPI(_Base):
    _schemaName = "PhysxVehicleDriveBasicAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleDriveBasic:peakTorque']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetPeakTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleDriveBasic:peakTorque")
    def CreatePeakTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleDriveBasic:peakTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleDriveBasic:peakTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleDriveStandardAPI(_Base):
    _schemaName = "PhysxVehicleDriveStandardAPI"
    schemaKind = 'single'
    _schemaAttributeNames = []
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetEngineRel(self):
        return self._prim.GetRelationship("physxVehicleDriveStandard:engine")
    def CreateEngineRel(self):
        return self._prim.CreateRelationship("physxVehicleDriveStandard:engine", False)
    def GetGearsRel(self):
        return self._prim.GetRelationship("physxVehicleDriveStandard:gears")
    def CreateGearsRel(self):
        return self._prim.CreateRelationship("physxVehicleDriveStandard:gears", False)
    def GetAutoGearBoxRel(self):
        return self._prim.GetRelationship("physxVehicleDriveStandard:autoGearBox")
    def CreateAutoGearBoxRel(self):
        return self._prim.CreateRelationship("physxVehicleDriveStandard:autoGearBox", False)
    def GetClutchRel(self):
        return self._prim.GetRelationship("physxVehicleDriveStandard:clutch")
    def CreateClutchRel(self):
        return self._prim.CreateRelationship("physxVehicleDriveStandard:clutch", False)

class PhysxVehicleMultiWheelDifferentialAPI(_Base):
    _schemaName = "PhysxVehicleMultiWheelDifferentialAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleMultiWheelDifferential:wheels', 'physxVehicleMultiWheelDifferential:torqueRatios', 'physxVehicleMultiWheelDifferential:averageWheelSpeedRatios']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetWheelsAttr(self):
        return self._prim.GetAttribute("physxVehicleMultiWheelDifferential:wheels")
    def CreateWheelsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleMultiWheelDifferential:wheels")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleMultiWheelDifferential:wheels", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTorqueRatiosAttr(self):
        return self._prim.GetAttribute("physxVehicleMultiWheelDifferential:torqueRatios")
    def CreateTorqueRatiosAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleMultiWheelDifferential:torqueRatios")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleMultiWheelDifferential:torqueRatios", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAverageWheelSpeedRatiosAttr(self):
        return self._prim.GetAttribute("physxVehicleMultiWheelDifferential:averageWheelSpeedRatios")
    def CreateAverageWheelSpeedRatiosAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleMultiWheelDifferential:averageWheelSpeedRatios")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleMultiWheelDifferential:averageWheelSpeedRatios", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleTankDifferentialAPI(_Base):
    _schemaName = "PhysxVehicleTankDifferentialAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleTankDifferential:numberOfWheelsPerTrack', 'physxVehicleTankDifferential:thrustIndexPerTrack', 'physxVehicleTankDifferential:trackToWheelIndices', 'physxVehicleTankDifferential:wheelIndicesInTrackOrder']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetNumberOfWheelsPerTrackAttr(self):
        return self._prim.GetAttribute("physxVehicleTankDifferential:numberOfWheelsPerTrack")
    def CreateNumberOfWheelsPerTrackAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTankDifferential:numberOfWheelsPerTrack")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTankDifferential:numberOfWheelsPerTrack", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetThrustIndexPerTrackAttr(self):
        return self._prim.GetAttribute("physxVehicleTankDifferential:thrustIndexPerTrack")
    def CreateThrustIndexPerTrackAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTankDifferential:thrustIndexPerTrack")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTankDifferential:thrustIndexPerTrack", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTrackToWheelIndicesAttr(self):
        return self._prim.GetAttribute("physxVehicleTankDifferential:trackToWheelIndices")
    def CreateTrackToWheelIndicesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTankDifferential:trackToWheelIndices")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTankDifferential:trackToWheelIndices", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelIndicesInTrackOrderAttr(self):
        return self._prim.GetAttribute("physxVehicleTankDifferential:wheelIndicesInTrackOrder")
    def CreateWheelIndicesInTrackOrderAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTankDifferential:wheelIndicesInTrackOrder")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTankDifferential:wheelIndicesInTrackOrder", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleBrakesAPI(_Base):
    _schemaName = "PhysxVehicleBrakesAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxVehicleBrakes:__INSTANCE_NAME__:wheels', 'physxVehicleBrakes:__INSTANCE_NAME__:maxBrakeTorque', 'physxVehicleBrakes:__INSTANCE_NAME__:torqueMultipliers']
    _propertyNamespacePrefix = 'physxVehicleBrakes'
    _schemaPropertyBaseNames = ['maxBrakeTorque', 'torqueMultipliers', 'wheels']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxVehicleBrakesAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetWheelsAttr(self):
        return self._prim.GetAttribute("physxVehicleBrakes:" + self._name + ":wheels")
    def CreateWheelsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleBrakes:" + self._name + ":wheels")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleBrakes:" + self._name + ":wheels", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxBrakeTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleBrakes:" + self._name + ":maxBrakeTorque")
    def CreateMaxBrakeTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleBrakes:" + self._name + ":maxBrakeTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleBrakes:" + self._name + ":maxBrakeTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTorqueMultipliersAttr(self):
        return self._prim.GetAttribute("physxVehicleBrakes:" + self._name + ":torqueMultipliers")
    def CreateTorqueMultipliersAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleBrakes:" + self._name + ":torqueMultipliers")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleBrakes:" + self._name + ":torqueMultipliers", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxVehicleSteeringAPI(_Base):
    _schemaName = "PhysxVehicleSteeringAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleSteering:wheels', 'physxVehicleSteering:maxSteerAngle', 'physxVehicleSteering:angleMultipliers']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetWheelsAttr(self):
        return self._prim.GetAttribute("physxVehicleSteering:wheels")
    def CreateWheelsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSteering:wheels")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSteering:wheels", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxSteerAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleSteering:maxSteerAngle")
    def CreateMaxSteerAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSteering:maxSteerAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSteering:maxSteerAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAngleMultipliersAttr(self):
        return self._prim.GetAttribute("physxVehicleSteering:angleMultipliers")
    def CreateAngleMultipliersAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleSteering:angleMultipliers")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleSteering:angleMultipliers", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleAckermannSteeringAPI(_Base):
    _schemaName = "PhysxVehicleAckermannSteeringAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleAckermannSteering:wheel0', 'physxVehicleAckermannSteering:wheel1', 'physxVehicleAckermannSteering:maxSteerAngle', 'physxVehicleAckermannSteering:wheelBase', 'physxVehicleAckermannSteering:trackWidth', 'physxVehicleAckermannSteering:strength']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetWheel0Attr(self):
        return self._prim.GetAttribute("physxVehicleAckermannSteering:wheel0")
    def CreateWheel0Attr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAckermannSteering:wheel0")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAckermannSteering:wheel0", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheel1Attr(self):
        return self._prim.GetAttribute("physxVehicleAckermannSteering:wheel1")
    def CreateWheel1Attr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAckermannSteering:wheel1")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAckermannSteering:wheel1", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxSteerAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleAckermannSteering:maxSteerAngle")
    def CreateMaxSteerAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAckermannSteering:maxSteerAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAckermannSteering:maxSteerAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWheelBaseAttr(self):
        return self._prim.GetAttribute("physxVehicleAckermannSteering:wheelBase")
    def CreateWheelBaseAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAckermannSteering:wheelBase")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAckermannSteering:wheelBase", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTrackWidthAttr(self):
        return self._prim.GetAttribute("physxVehicleAckermannSteering:trackWidth")
    def CreateTrackWidthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAckermannSteering:trackWidth")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAckermannSteering:trackWidth", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStrengthAttr(self):
        return self._prim.GetAttribute("physxVehicleAckermannSteering:strength")
    def CreateStrengthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleAckermannSteering:strength")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleAckermannSteering:strength", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleAPI(_Base):
    _schemaName = "PhysxVehicleAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicle:vehicleEnabled', 'physxVehicle:suspensionLineQueryType', 'physxVehicle:limitSuspensionExpansionVelocity', 'physxVehicle:subStepThresholdLongitudinalSpeed', 'physxVehicle:lowForwardSpeedSubStepCount', 'physxVehicle:highForwardSpeedSubStepCount', 'physxVehicle:minLongitudinalSlipDenominator', 'physxVehicle:minPassiveLongitudinalSlipDenominator', 'physxVehicle:minActiveLongitudinalSlipDenominator', 'physxVehicle:minLateralSlipDenominator', 'physxVehicle:longitudinalStickyTireThresholdSpeed', 'physxVehicle:longitudinalStickyTireThresholdTime', 'physxVehicle:longitudinalStickyTireDamping', 'physxVehicle:lateralStickyTireThresholdSpeed', 'physxVehicle:lateralStickyTireThresholdTime', 'physxVehicle:lateralStickyTireDamping']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetVehicleEnabledAttr(self):
        return self._prim.GetAttribute("physxVehicle:vehicleEnabled")
    def CreateVehicleEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:vehicleEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:vehicleEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSuspensionLineQueryTypeAttr(self):
        return self._prim.GetAttribute("physxVehicle:suspensionLineQueryType")
    def CreateSuspensionLineQueryTypeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Token
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:suspensionLineQueryType")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:suspensionLineQueryType", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLimitSuspensionExpansionVelocityAttr(self):
        return self._prim.GetAttribute("physxVehicle:limitSuspensionExpansionVelocity")
    def CreateLimitSuspensionExpansionVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:limitSuspensionExpansionVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:limitSuspensionExpansionVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSubStepThresholdLongitudinalSpeedAttr(self):
        return self._prim.GetAttribute("physxVehicle:subStepThresholdLongitudinalSpeed")
    def CreateSubStepThresholdLongitudinalSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:subStepThresholdLongitudinalSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:subStepThresholdLongitudinalSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLowForwardSpeedSubStepCountAttr(self):
        return self._prim.GetAttribute("physxVehicle:lowForwardSpeedSubStepCount")
    def CreateLowForwardSpeedSubStepCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:lowForwardSpeedSubStepCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:lowForwardSpeedSubStepCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetHighForwardSpeedSubStepCountAttr(self):
        return self._prim.GetAttribute("physxVehicle:highForwardSpeedSubStepCount")
    def CreateHighForwardSpeedSubStepCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:highForwardSpeedSubStepCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:highForwardSpeedSubStepCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinLongitudinalSlipDenominatorAttr(self):
        return self._prim.GetAttribute("physxVehicle:minLongitudinalSlipDenominator")
    def CreateMinLongitudinalSlipDenominatorAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:minLongitudinalSlipDenominator")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:minLongitudinalSlipDenominator", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinPassiveLongitudinalSlipDenominatorAttr(self):
        return self._prim.GetAttribute("physxVehicle:minPassiveLongitudinalSlipDenominator")
    def CreateMinPassiveLongitudinalSlipDenominatorAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:minPassiveLongitudinalSlipDenominator")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:minPassiveLongitudinalSlipDenominator", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinActiveLongitudinalSlipDenominatorAttr(self):
        return self._prim.GetAttribute("physxVehicle:minActiveLongitudinalSlipDenominator")
    def CreateMinActiveLongitudinalSlipDenominatorAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:minActiveLongitudinalSlipDenominator")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:minActiveLongitudinalSlipDenominator", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinLateralSlipDenominatorAttr(self):
        return self._prim.GetAttribute("physxVehicle:minLateralSlipDenominator")
    def CreateMinLateralSlipDenominatorAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:minLateralSlipDenominator")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:minLateralSlipDenominator", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLongitudinalStickyTireThresholdSpeedAttr(self):
        return self._prim.GetAttribute("physxVehicle:longitudinalStickyTireThresholdSpeed")
    def CreateLongitudinalStickyTireThresholdSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:longitudinalStickyTireThresholdSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:longitudinalStickyTireThresholdSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLongitudinalStickyTireThresholdTimeAttr(self):
        return self._prim.GetAttribute("physxVehicle:longitudinalStickyTireThresholdTime")
    def CreateLongitudinalStickyTireThresholdTimeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:longitudinalStickyTireThresholdTime")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:longitudinalStickyTireThresholdTime", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLongitudinalStickyTireDampingAttr(self):
        return self._prim.GetAttribute("physxVehicle:longitudinalStickyTireDamping")
    def CreateLongitudinalStickyTireDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:longitudinalStickyTireDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:longitudinalStickyTireDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLateralStickyTireThresholdSpeedAttr(self):
        return self._prim.GetAttribute("physxVehicle:lateralStickyTireThresholdSpeed")
    def CreateLateralStickyTireThresholdSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:lateralStickyTireThresholdSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:lateralStickyTireThresholdSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLateralStickyTireThresholdTimeAttr(self):
        return self._prim.GetAttribute("physxVehicle:lateralStickyTireThresholdTime")
    def CreateLateralStickyTireThresholdTimeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:lateralStickyTireThresholdTime")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:lateralStickyTireThresholdTime", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLateralStickyTireDampingAttr(self):
        return self._prim.GetAttribute("physxVehicle:lateralStickyTireDamping")
    def CreateLateralStickyTireDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicle:lateralStickyTireDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicle:lateralStickyTireDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDriveRel(self):
        return self._prim.GetRelationship("physxVehicle:drive")
    def CreateDriveRel(self):
        return self._prim.CreateRelationship("physxVehicle:drive", False)

class PhysxVehicleControllerAPI(_Base):
    _schemaName = "PhysxVehicleControllerAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleController:accelerator', 'physxVehicleController:brake0', 'physxVehicleController:brake1', 'physxVehicleController:brake', 'physxVehicleController:handbrake', 'physxVehicleController:steer', 'physxVehicleController:steerLeft', 'physxVehicleController:steerRight', 'physxVehicleController:targetGear']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetAcceleratorAttr(self):
        return self._prim.GetAttribute("physxVehicleController:accelerator")
    def CreateAcceleratorAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:accelerator")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:accelerator", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBrake0Attr(self):
        return self._prim.GetAttribute("physxVehicleController:brake0")
    def CreateBrake0Attr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:brake0")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:brake0", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBrake1Attr(self):
        return self._prim.GetAttribute("physxVehicleController:brake1")
    def CreateBrake1Attr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:brake1")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:brake1", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBrakeAttr(self):
        return self._prim.GetAttribute("physxVehicleController:brake")
    def CreateBrakeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:brake")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:brake", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetHandbrakeAttr(self):
        return self._prim.GetAttribute("physxVehicleController:handbrake")
    def CreateHandbrakeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:handbrake")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:handbrake", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSteerAttr(self):
        return self._prim.GetAttribute("physxVehicleController:steer")
    def CreateSteerAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:steer")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:steer", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSteerLeftAttr(self):
        return self._prim.GetAttribute("physxVehicleController:steerLeft")
    def CreateSteerLeftAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:steerLeft")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:steerLeft", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSteerRightAttr(self):
        return self._prim.GetAttribute("physxVehicleController:steerRight")
    def CreateSteerRightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:steerRight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:steerRight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTargetGearAttr(self):
        return self._prim.GetAttribute("physxVehicleController:targetGear")
    def CreateTargetGearAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleController:targetGear")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleController:targetGear", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleTankControllerAPI(_Base):
    _schemaName = "PhysxVehicleTankControllerAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleTankController:thrust0', 'physxVehicleTankController:thrust1']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetThrust0Attr(self):
        return self._prim.GetAttribute("physxVehicleTankController:thrust0")
    def CreateThrust0Attr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTankController:thrust0")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTankController:thrust0", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetThrust1Attr(self):
        return self._prim.GetAttribute("physxVehicleTankController:thrust1")
    def CreateThrust1Attr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleTankController:thrust1")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleTankController:thrust1", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleWheelControllerAPI(_Base):
    _schemaName = "PhysxVehicleWheelControllerAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxVehicleWheelController:driveTorque', 'physxVehicleWheelController:brakeTorque', 'physxVehicleWheelController:steerAngle']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetDriveTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelController:driveTorque")
    def CreateDriveTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelController:driveTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelController:driveTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBrakeTorqueAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelController:brakeTorque")
    def CreateBrakeTorqueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelController:brakeTorque")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelController:brakeTorque", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSteerAngleAttr(self):
        return self._prim.GetAttribute("physxVehicleWheelController:steerAngle")
    def CreateSteerAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleWheelController:steerAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleWheelController:steerAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxVehicleNonlinearCommandResponseAPI(_Base):
    _schemaName = "PhysxVehicleNonlinearCommandResponseAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxVehicleNCR:__INSTANCE_NAME__:commandValues', 'physxVehicleNCR:__INSTANCE_NAME__:speedResponsesPerCommandValue', 'physxVehicleNCR:__INSTANCE_NAME__:speedResponses']
    _propertyNamespacePrefix = 'physxVehicleNCR'
    _schemaPropertyBaseNames = ['commandValues', 'speedResponses', 'speedResponsesPerCommandValue']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxVehicleNonlinearCommandResponseAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetCommandValuesAttr(self):
        return self._prim.GetAttribute("physxVehicleNCR:" + self._name + ":commandValues")
    def CreateCommandValuesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.FloatArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleNCR:" + self._name + ":commandValues")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleNCR:" + self._name + ":commandValues", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSpeedResponsesPerCommandValueAttr(self):
        return self._prim.GetAttribute("physxVehicleNCR:" + self._name + ":speedResponsesPerCommandValue")
    def CreateSpeedResponsesPerCommandValueAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.IntArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleNCR:" + self._name + ":speedResponsesPerCommandValue")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleNCR:" + self._name + ":speedResponsesPerCommandValue", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSpeedResponsesAttr(self):
        return self._prim.GetAttribute("physxVehicleNCR:" + self._name + ":speedResponses")
    def CreateSpeedResponsesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float2Array
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxVehicleNCR:" + self._name + ":speedResponses")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxVehicleNCR:" + self._name + ":speedResponses", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxCameraAPI(_Base):
    _schemaName = "PhysxCameraAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['alwaysUpdateEnabled']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetAlwaysUpdateEnabledAttr(self):
        return self._prim.GetAttribute("alwaysUpdateEnabled")
    def CreateAlwaysUpdateEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("alwaysUpdateEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("alwaysUpdateEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPhysxCameraSubjectRel(self):
        return self._prim.GetRelationship("physxCamera:subject")
    def CreatePhysxCameraSubjectRel(self):
        return self._prim.CreateRelationship("physxCamera:subject", False)

class PhysxCameraFollowAPI(_Base):
    _schemaName = "PhysxCameraFollowAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxFollowCamera:yawAngle', 'physxFollowCamera:pitchAngle', 'physxFollowCamera:pitchAngleTimeConstant', 'physxFollowCamera:slowSpeedPitchAngleScale', 'physxFollowCamera:slowPitchAngleSpeed', 'physxFollowCamera:velocityNormalMinSpeed', 'physxFollowCamera:followMinSpeed', 'physxFollowCamera:followMinDistance', 'physxFollowCamera:followMaxSpeed', 'physxFollowCamera:followMaxDistance', 'physxFollowCamera:yawRateTimeConstant', 'physxFollowCamera:followTurnRateGain', 'physxFollowCamera:cameraPositionTimeConstant', 'physxFollowCamera:positionOffset', 'physxFollowCamera:lookAheadMinSpeed', 'physxFollowCamera:lookAheadMinDistance', 'physxFollowCamera:lookAheadMaxSpeed', 'physxFollowFollowCamera:lookAheadMaxDistance', 'physxFollowCamera:lookAheadTurnRateGain', 'physxFollowCamera:lookPositionHeight', 'physxFollowCamera:lookPositionTimeConstant']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetYawAngleAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:yawAngle")
    def CreateYawAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:yawAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:yawAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPitchAngleAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:pitchAngle")
    def CreatePitchAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:pitchAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:pitchAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPitchAngleTimeConstantAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:pitchAngleTimeConstant")
    def CreatePitchAngleTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:pitchAngleTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:pitchAngleTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSlowSpeedPitchAngleScaleAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:slowSpeedPitchAngleScale")
    def CreateSlowSpeedPitchAngleScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:slowSpeedPitchAngleScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:slowSpeedPitchAngleScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSlowPitchAngleSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:slowPitchAngleSpeed")
    def CreateSlowPitchAngleSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:slowPitchAngleSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:slowPitchAngleSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVelocityNormalMinSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:velocityNormalMinSpeed")
    def CreateVelocityNormalMinSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:velocityNormalMinSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:velocityNormalMinSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowMinSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:followMinSpeed")
    def CreateFollowMinSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:followMinSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:followMinSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowMinDistanceAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:followMinDistance")
    def CreateFollowMinDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:followMinDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:followMinDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowMaxSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:followMaxSpeed")
    def CreateFollowMaxSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:followMaxSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:followMaxSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowMaxDistanceAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:followMaxDistance")
    def CreateFollowMaxDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:followMaxDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:followMaxDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetYawRateTimeConstantAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:yawRateTimeConstant")
    def CreateYawRateTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:yawRateTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:yawRateTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowTurnRateGainAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:followTurnRateGain")
    def CreateFollowTurnRateGainAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:followTurnRateGain")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:followTurnRateGain", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCameraPositionTimeConstantAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:cameraPositionTimeConstant")
    def CreateCameraPositionTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:cameraPositionTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:cameraPositionTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPositionOffsetAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:positionOffset")
    def CreatePositionOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:positionOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:positionOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookAheadMinSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:lookAheadMinSpeed")
    def CreateLookAheadMinSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:lookAheadMinSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:lookAheadMinSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookAheadMinDistanceAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:lookAheadMinDistance")
    def CreateLookAheadMinDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:lookAheadMinDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:lookAheadMinDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookAheadMaxSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:lookAheadMaxSpeed")
    def CreateLookAheadMaxSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:lookAheadMaxSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:lookAheadMaxSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookAheadMaxDistanceAttr(self):
        return self._prim.GetAttribute("physxFollowFollowCamera:lookAheadMaxDistance")
    def CreateLookAheadMaxDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowFollowCamera:lookAheadMaxDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowFollowCamera:lookAheadMaxDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookAheadTurnRateGainAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:lookAheadTurnRateGain")
    def CreateLookAheadTurnRateGainAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:lookAheadTurnRateGain")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:lookAheadTurnRateGain", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookPositionHeightAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:lookPositionHeight")
    def CreateLookPositionHeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:lookPositionHeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:lookPositionHeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLookPositionTimeConstantAttr(self):
        return self._prim.GetAttribute("physxFollowCamera:lookPositionTimeConstant")
    def CreateLookPositionTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowCamera:lookPositionTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowCamera:lookPositionTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxCameraFollowLookAPI(_Base):
    _schemaName = "PhysxCameraFollowLookAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxFollowLookCamera:downHillGroundAngle', 'physxFollowLookCamera:downHillGroundPitch', 'physxFollowLookCamera:upHillGroundAngle', 'physxFollowLookCamera:upHillGroundPitch', 'physxFollowLookCamera:velocityBlendTimeConstant', 'physxFollowLookCamera:followReverseSpeed', 'physxFollowLookCamera:followReverseDistance']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetDownHillGroundAngleAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:downHillGroundAngle")
    def CreateDownHillGroundAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:downHillGroundAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:downHillGroundAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDownHillGroundPitchAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:downHillGroundPitch")
    def CreateDownHillGroundPitchAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:downHillGroundPitch")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:downHillGroundPitch", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpHillGroundAngleAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:upHillGroundAngle")
    def CreateUpHillGroundAngleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:upHillGroundAngle")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:upHillGroundAngle", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUpHillGroundPitchAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:upHillGroundPitch")
    def CreateUpHillGroundPitchAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:upHillGroundPitch")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:upHillGroundPitch", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVelocityBlendTimeConstantAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:velocityBlendTimeConstant")
    def CreateVelocityBlendTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:velocityBlendTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:velocityBlendTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowReverseSpeedAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:followReverseSpeed")
    def CreateFollowReverseSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:followReverseSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:followReverseSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowReverseDistanceAttr(self):
        return self._prim.GetAttribute("physxFollowLookCamera:followReverseDistance")
    def CreateFollowReverseDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxFollowLookCamera:followReverseDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxFollowLookCamera:followReverseDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxCameraFollowVelocityAPI(_Base):
    _schemaName = "PhysxCameraFollowVelocityAPI"
    schemaKind = 'single'
    _schemaAttributeNames = []
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)

class PhysxCameraDroneAPI(_Base):
    _schemaName = "PhysxCameraDroneAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDroneCamera:followHeight', 'physxDroneCamera:followDistance', 'physxDroneCamera:maxDistance', 'physxDroneCamera:maxSpeed', 'physxDroneCamera:horizontalVelocityGain', 'physxDroneCamera:verticalVelocityGain', 'physxDroneCamera:feedForwardVelocityGain', 'physxDroneCamera:velocityFilterTimeConstant', 'physxDroneCamera:rotationFilterTimeConstant', 'physxDroneCamera:positionOffset']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetFollowHeightAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:followHeight")
    def CreateFollowHeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:followHeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:followHeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFollowDistanceAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:followDistance")
    def CreateFollowDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:followDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:followDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxDistanceAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:maxDistance")
    def CreateMaxDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:maxDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:maxDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxSpeedAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:maxSpeed")
    def CreateMaxSpeedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:maxSpeed")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:maxSpeed", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetHorizontalVelocityGainAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:horizontalVelocityGain")
    def CreateHorizontalVelocityGainAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:horizontalVelocityGain")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:horizontalVelocityGain", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVerticalVelocityGainAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:verticalVelocityGain")
    def CreateVerticalVelocityGainAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:verticalVelocityGain")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:verticalVelocityGain", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFeedForwardVelocityGainAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:feedForwardVelocityGain")
    def CreateFeedForwardVelocityGainAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:feedForwardVelocityGain")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:feedForwardVelocityGain", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVelocityFilterTimeConstantAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:velocityFilterTimeConstant")
    def CreateVelocityFilterTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:velocityFilterTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:velocityFilterTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRotationFilterTimeConstantAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:rotationFilterTimeConstant")
    def CreateRotationFilterTimeConstantAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:rotationFilterTimeConstant")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:rotationFilterTimeConstant", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPositionOffsetAttr(self):
        return self._prim.GetAttribute("physxDroneCamera:positionOffset")
    def CreatePositionOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDroneCamera:positionOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDroneCamera:positionOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxParticleSystem(_Base):
    _schemaName = "PhysxParticleSystem"
    schemaKind = 'typed'
    _schemaAttributeNames = ['particleSystemEnabled', 'contactOffset', 'restOffset', 'particleContactOffset', 'solidRestOffset', 'fluidRestOffset', 'enableCCD', 'solverPositionIterationCount', 'maxDepenetrationVelocity', 'wind', 'maxNeighborhood', 'neighborhoodScale', 'maxVelocity', 'globalSelfCollisionEnabled', 'nonParticleCollisionEnabled']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def Define(cls, stage, path): return cls(stage.DefinePrim(path, cls._schemaName))
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        t = self._GetStaticTfType()
        return bool(t.typeName) and self._prim.IsA(t)
    def GetParticleSystemEnabledAttr(self):
        return self._prim.GetAttribute("particleSystemEnabled")
    def CreateParticleSystemEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("particleSystemEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("particleSystemEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetContactOffsetAttr(self):
        return self._prim.GetAttribute("contactOffset")
    def CreateContactOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("contactOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("contactOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRestOffsetAttr(self):
        return self._prim.GetAttribute("restOffset")
    def CreateRestOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("restOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("restOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParticleContactOffsetAttr(self):
        return self._prim.GetAttribute("particleContactOffset")
    def CreateParticleContactOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("particleContactOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("particleContactOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolidRestOffsetAttr(self):
        return self._prim.GetAttribute("solidRestOffset")
    def CreateSolidRestOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("solidRestOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("solidRestOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetFluidRestOffsetAttr(self):
        return self._prim.GetAttribute("fluidRestOffset")
    def CreateFluidRestOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("fluidRestOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("fluidRestOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableCCDAttr(self):
        return self._prim.GetAttribute("enableCCD")
    def CreateEnableCCDAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("enableCCD")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("enableCCD", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSolverPositionIterationCountAttr(self):
        return self._prim.GetAttribute("solverPositionIterationCount")
    def CreateSolverPositionIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("solverPositionIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("solverPositionIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxDepenetrationVelocityAttr(self):
        return self._prim.GetAttribute("maxDepenetrationVelocity")
    def CreateMaxDepenetrationVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("maxDepenetrationVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("maxDepenetrationVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetWindAttr(self):
        return self._prim.GetAttribute("wind")
    def CreateWindAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float3
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("wind")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("wind", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxNeighborhoodAttr(self):
        return self._prim.GetAttribute("maxNeighborhood")
    def CreateMaxNeighborhoodAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("maxNeighborhood")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("maxNeighborhood", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetNeighborhoodScaleAttr(self):
        return self._prim.GetAttribute("neighborhoodScale")
    def CreateNeighborhoodScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("neighborhoodScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("neighborhoodScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxVelocityAttr(self):
        return self._prim.GetAttribute("maxVelocity")
    def CreateMaxVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("maxVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("maxVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGlobalSelfCollisionEnabledAttr(self):
        return self._prim.GetAttribute("globalSelfCollisionEnabled")
    def CreateGlobalSelfCollisionEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("globalSelfCollisionEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("globalSelfCollisionEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetNonParticleCollisionEnabledAttr(self):
        return self._prim.GetAttribute("nonParticleCollisionEnabled")
    def CreateNonParticleCollisionEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("nonParticleCollisionEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("nonParticleCollisionEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSimulationOwnerRel(self):
        return self._prim.GetRelationship("simulationOwner")
    def CreateSimulationOwnerRel(self):
        return self._prim.CreateRelationship("simulationOwner", False)

class PhysxParticleIsosurfaceAPI(_Base):
    _schemaName = "PhysxParticleIsosurfaceAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxParticleIsosurface:isosurfaceEnabled', 'physxParticleIsosurface:maxVertices', 'physxParticleIsosurface:maxTriangles', 'physxParticleIsosurface:maxSubgrids', 'physxParticleIsosurface:gridSpacing', 'physxParticleIsosurface:surfaceDistance', 'physxParticleIsosurface:gridFilteringPasses', 'physxParticleIsosurface:gridSmoothingRadius', 'physxParticleIsosurface:numMeshSmoothingPasses', 'physxParticleIsosurface:numMeshNormalSmoothingPasses']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetIsosurfaceEnabledAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:isosurfaceEnabled")
    def CreateIsosurfaceEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:isosurfaceEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:isosurfaceEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxVerticesAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:maxVertices")
    def CreateMaxVerticesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:maxVertices")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:maxVertices", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxTrianglesAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:maxTriangles")
    def CreateMaxTrianglesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:maxTriangles")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:maxTriangles", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxSubgridsAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:maxSubgrids")
    def CreateMaxSubgridsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:maxSubgrids")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:maxSubgrids", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGridSpacingAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:gridSpacing")
    def CreateGridSpacingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:gridSpacing")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:gridSpacing", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceDistanceAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:surfaceDistance")
    def CreateSurfaceDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:surfaceDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:surfaceDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGridFilteringPassesAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:gridFilteringPasses")
    def CreateGridFilteringPassesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.String
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:gridFilteringPasses")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:gridFilteringPasses", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGridSmoothingRadiusAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:gridSmoothingRadius")
    def CreateGridSmoothingRadiusAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:gridSmoothingRadius")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:gridSmoothingRadius", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetNumMeshSmoothingPassesAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:numMeshSmoothingPasses")
    def CreateNumMeshSmoothingPassesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:numMeshSmoothingPasses")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:numMeshSmoothingPasses", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetNumMeshNormalSmoothingPassesAttr(self):
        return self._prim.GetAttribute("physxParticleIsosurface:numMeshNormalSmoothingPasses")
    def CreateNumMeshNormalSmoothingPassesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleIsosurface:numMeshNormalSmoothingPasses")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleIsosurface:numMeshNormalSmoothingPasses", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxParticleSmoothingAPI(_Base):
    _schemaName = "PhysxParticleSmoothingAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxParticleSmoothing:particleSmoothingEnabled', 'physxParticleSmoothing:strength']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetParticleSmoothingEnabledAttr(self):
        return self._prim.GetAttribute("physxParticleSmoothing:particleSmoothingEnabled")
    def CreateParticleSmoothingEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleSmoothing:particleSmoothingEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleSmoothing:particleSmoothingEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStrengthAttr(self):
        return self._prim.GetAttribute("physxParticleSmoothing:strength")
    def CreateStrengthAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleSmoothing:strength")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleSmoothing:strength", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxParticleAnisotropyAPI(_Base):
    _schemaName = "PhysxParticleAnisotropyAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxParticleAnisotropy:particleAnisotropyEnabled', 'physxParticleAnisotropy:scale', 'physxParticleAnisotropy:min', 'physxParticleAnisotropy:max']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetParticleAnisotropyEnabledAttr(self):
        return self._prim.GetAttribute("physxParticleAnisotropy:particleAnisotropyEnabled")
    def CreateParticleAnisotropyEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleAnisotropy:particleAnisotropyEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleAnisotropy:particleAnisotropyEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetScaleAttr(self):
        return self._prim.GetAttribute("physxParticleAnisotropy:scale")
    def CreateScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleAnisotropy:scale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleAnisotropy:scale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMinAttr(self):
        return self._prim.GetAttribute("physxParticleAnisotropy:min")
    def CreateMinAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleAnisotropy:min")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleAnisotropy:min", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxAttr(self):
        return self._prim.GetAttribute("physxParticleAnisotropy:max")
    def CreateMaxAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleAnisotropy:max")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleAnisotropy:max", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxDiffuseParticlesAPI(_Base):
    _schemaName = "PhysxDiffuseParticlesAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDiffuseParticles:diffuseParticlesEnabled', 'physxDiffuseParticles:maxDiffuseParticleMultiplier', 'physxDiffuseParticles:threshold', 'physxDiffuseParticles:lifetime', 'physxDiffuseParticles:airDrag', 'physxDiffuseParticles:bubbleDrag', 'physxDiffuseParticles:buoyancy', 'physxDiffuseParticles:kineticEnergyWeight', 'physxDiffuseParticles:pressureWeight', 'physxDiffuseParticles:divergenceWeight', 'physxDiffuseParticles:collisionDecay', 'physxDiffuseParticles:useAccurateVelocity']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetDiffuseParticlesEnabledAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:diffuseParticlesEnabled")
    def CreateDiffuseParticlesEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:diffuseParticlesEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:diffuseParticlesEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxDiffuseParticleMultiplierAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:maxDiffuseParticleMultiplier")
    def CreateMaxDiffuseParticleMultiplierAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:maxDiffuseParticleMultiplier")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:maxDiffuseParticleMultiplier", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetThresholdAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:threshold")
    def CreateThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:threshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:threshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLifetimeAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:lifetime")
    def CreateLifetimeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:lifetime")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:lifetime", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAirDragAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:airDrag")
    def CreateAirDragAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:airDrag")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:airDrag", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBubbleDragAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:bubbleDrag")
    def CreateBubbleDragAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:bubbleDrag")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:bubbleDrag", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetBuoyancyAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:buoyancy")
    def CreateBuoyancyAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:buoyancy")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:buoyancy", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetKineticEnergyWeightAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:kineticEnergyWeight")
    def CreateKineticEnergyWeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:kineticEnergyWeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:kineticEnergyWeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetPressureWeightAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:pressureWeight")
    def CreatePressureWeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:pressureWeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:pressureWeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDivergenceWeightAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:divergenceWeight")
    def CreateDivergenceWeightAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:divergenceWeight")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:divergenceWeight", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCollisionDecayAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:collisionDecay")
    def CreateCollisionDecayAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:collisionDecay")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:collisionDecay", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetUseAccurateVelocityAttr(self):
        return self._prim.GetAttribute("physxDiffuseParticles:useAccurateVelocity")
    def CreateUseAccurateVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDiffuseParticles:useAccurateVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDiffuseParticles:useAccurateVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxPBDMaterialAPI(_Base):
    _schemaName = "PhysxPBDMaterialAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxPBDMaterial:friction', 'physxPBDMaterial:particleFrictionScale', 'physxPBDMaterial:damping', 'physxPBDMaterial:viscosity', 'physxPBDMaterial:vorticityConfinement', 'physxPBDMaterial:surfaceTension', 'physxPBDMaterial:cohesion', 'physxPBDMaterial:adhesion', 'physxPBDMaterial:particleAdhesionScale', 'physxPBDMaterial:adhesionOffsetScale', 'physxPBDMaterial:gravityScale', 'physxPBDMaterial:density', 'physxPBDMaterial:cflCoefficient']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetFrictionAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:friction")
    def CreateFrictionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:friction")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:friction", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParticleFrictionScaleAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:particleFrictionScale")
    def CreateParticleFrictionScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:particleFrictionScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:particleFrictionScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDampingAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:damping")
    def CreateDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:damping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:damping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetViscosityAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:viscosity")
    def CreateViscosityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:viscosity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:viscosity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVorticityConfinementAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:vorticityConfinement")
    def CreateVorticityConfinementAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:vorticityConfinement")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:vorticityConfinement", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceTensionAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:surfaceTension")
    def CreateSurfaceTensionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:surfaceTension")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:surfaceTension", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCohesionAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:cohesion")
    def CreateCohesionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:cohesion")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:cohesion", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAdhesionAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:adhesion")
    def CreateAdhesionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:adhesion")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:adhesion", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParticleAdhesionScaleAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:particleAdhesionScale")
    def CreateParticleAdhesionScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:particleAdhesionScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:particleAdhesionScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAdhesionOffsetScaleAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:adhesionOffsetScale")
    def CreateAdhesionOffsetScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:adhesionOffsetScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:adhesionOffsetScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetGravityScaleAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:gravityScale")
    def CreateGravityScaleAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:gravityScale")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:gravityScale", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDensityAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:density")
    def CreateDensityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:density")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:density", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCflCoefficientAttr(self):
        return self._prim.GetAttribute("physxPBDMaterial:cflCoefficient")
    def CreateCflCoefficientAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxPBDMaterial:cflCoefficient")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxPBDMaterial:cflCoefficient", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxParticleAPI(_Base):
    _schemaName = "PhysxParticleAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxParticle:particleEnabled', 'physxParticle:selfCollision', 'physxParticle:particleGroup']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetParticleEnabledAttr(self):
        return self._prim.GetAttribute("physxParticle:particleEnabled")
    def CreateParticleEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticle:particleEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticle:particleEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSelfCollisionAttr(self):
        return self._prim.GetAttribute("physxParticle:selfCollision")
    def CreateSelfCollisionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticle:selfCollision")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticle:selfCollision", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParticleGroupAttr(self):
        return self._prim.GetAttribute("physxParticle:particleGroup")
    def CreateParticleGroupAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticle:particleGroup")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticle:particleGroup", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParticleSystemRel(self):
        return self._prim.GetRelationship("physxParticle:particleSystem")
    def CreateParticleSystemRel(self):
        return self._prim.CreateRelationship("physxParticle:particleSystem", False)

class PhysxParticleSetAPI(_Base):
    _schemaName = "PhysxParticleSetAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxParticle:fluid', 'physxParticle:simulationPoints']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetFluidAttr(self):
        return self._prim.GetAttribute("physxParticle:fluid")
    def CreateFluidAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticle:fluid")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticle:fluid", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSimulationPointsAttr(self):
        return self._prim.GetAttribute("physxParticle:simulationPoints")
    def CreateSimulationPointsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Point3fArray
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticle:simulationPoints")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticle:simulationPoints", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxParticleSamplingAPI(_Base):
    _schemaName = "PhysxParticleSamplingAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxParticleSampling:samplingDistance', 'physxParticleSampling:volume', 'physxParticleSampling:maxSamples']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSamplingDistanceAttr(self):
        return self._prim.GetAttribute("physxParticleSampling:samplingDistance")
    def CreateSamplingDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleSampling:samplingDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleSampling:samplingDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVolumeAttr(self):
        return self._prim.GetAttribute("physxParticleSampling:volume")
    def CreateVolumeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleSampling:volume")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleSampling:volume", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxSamplesAttr(self):
        return self._prim.GetAttribute("physxParticleSampling:maxSamples")
    def CreateMaxSamplesAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Int
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxParticleSampling:maxSamples")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxParticleSampling:maxSamples", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetParticlesRel(self):
        return self._prim.GetRelationship("physxParticleSampling:particles")
    def CreateParticlesRel(self):
        return self._prim.CreateRelationship("physxParticleSampling:particles", False)

class PhysxJointAxisAPI(_Base):
    _schemaName = "PhysxJointAxisAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxJointAxis:__INSTANCE_NAME__:maxJointVelocity', 'physxJointAxis:__INSTANCE_NAME__:armature', 'physxJointAxis:__INSTANCE_NAME__:staticFrictionEffort', 'physxJointAxis:__INSTANCE_NAME__:dynamicFrictionEffort', 'physxJointAxis:__INSTANCE_NAME__:viscousFrictionCoefficient']
    _propertyNamespacePrefix = 'physxJointAxis'
    _schemaPropertyBaseNames = ['armature', 'dynamicFrictionEffort', 'maxJointVelocity', 'staticFrictionEffort', 'viscousFrictionCoefficient']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxJointAxisAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetMaxJointVelocityAttr(self):
        return self._prim.GetAttribute("physxJointAxis:" + self._name + ":maxJointVelocity")
    def CreateMaxJointVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJointAxis:" + self._name + ":maxJointVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJointAxis:" + self._name + ":maxJointVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetArmatureAttr(self):
        return self._prim.GetAttribute("physxJointAxis:" + self._name + ":armature")
    def CreateArmatureAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJointAxis:" + self._name + ":armature")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJointAxis:" + self._name + ":armature", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetStaticFrictionEffortAttr(self):
        return self._prim.GetAttribute("physxJointAxis:" + self._name + ":staticFrictionEffort")
    def CreateStaticFrictionEffortAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJointAxis:" + self._name + ":staticFrictionEffort")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJointAxis:" + self._name + ":staticFrictionEffort", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDynamicFrictionEffortAttr(self):
        return self._prim.GetAttribute("physxJointAxis:" + self._name + ":dynamicFrictionEffort")
    def CreateDynamicFrictionEffortAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJointAxis:" + self._name + ":dynamicFrictionEffort")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJointAxis:" + self._name + ":dynamicFrictionEffort", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetViscousFrictionCoefficientAttr(self):
        return self._prim.GetAttribute("physxJointAxis:" + self._name + ":viscousFrictionCoefficient")
    def CreateViscousFrictionCoefficientAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxJointAxis:" + self._name + ":viscousFrictionCoefficient")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxJointAxis:" + self._name + ":viscousFrictionCoefficient", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxDrivePerformanceEnvelopeAPI(_Base):
    _schemaName = "PhysxDrivePerformanceEnvelopeAPI"
    schemaKind = 'multiple'
    _schemaAttributeNames = ['physxDrivePerformanceEnvelope:__INSTANCE_NAME__:maxActuatorVelocity', 'physxDrivePerformanceEnvelope:__INSTANCE_NAME__:velocityDependentResistance', 'physxDrivePerformanceEnvelope:__INSTANCE_NAME__:speedEffortGradient']
    _propertyNamespacePrefix = 'physxDrivePerformanceEnvelope'
    _schemaPropertyBaseNames = ['maxActuatorVelocity', 'speedEffortGradient', 'velocityDependentResistance']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def _GetMultipleApplyInstanceNameFromPath(cls, path):
        if not path.IsPropertyPath():
            return ''
        property_name = path.name
        tokens = property_name.split(':')
        if len(tokens) < 2 or tokens[-1] in cls._schemaPropertyBaseNames:
            return ''
        prefix = cls._propertyNamespacePrefix
        if property_name.startswith(prefix + ':'):
            return property_name[len(prefix) + 1:]
        return ''
    @classmethod
    def IsPhysxDrivePerformanceEnvelopeAPIPath(cls, path):
        return bool(cls._GetMultipleApplyInstanceNameFromPath(path))
    @classmethod
    def CanApply(cls, prim, name):
        return prim.CanApplyAPI(cls._schemaName, name)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim, name):
        if prim.ApplyAPI(cls._schemaName, name):
            return cls(prim, name)
        return cls()
    @classmethod
    def Get(cls, prim_or_stage, name_or_path):
        if isinstance(name_or_path, Sdf.Path):
            instance_name = cls._GetMultipleApplyInstanceNameFromPath(name_or_path)
            if not instance_name:
                return cls()
            return cls(prim_or_stage.GetPrimAtPath(name_or_path.GetPrimPath()), instance_name)
        return cls(prim_or_stage, name_or_path)
    def _IsCompatible(self):
        return bool(self._name) and self._prim.HasAPI(self._schemaName, self._name)
    def GetMaxActuatorVelocityAttr(self):
        return self._prim.GetAttribute("physxDrivePerformanceEnvelope:" + self._name + ":maxActuatorVelocity")
    def CreateMaxActuatorVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDrivePerformanceEnvelope:" + self._name + ":maxActuatorVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDrivePerformanceEnvelope:" + self._name + ":maxActuatorVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetVelocityDependentResistanceAttr(self):
        return self._prim.GetAttribute("physxDrivePerformanceEnvelope:" + self._name + ":velocityDependentResistance")
    def CreateVelocityDependentResistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDrivePerformanceEnvelope:" + self._name + ":velocityDependentResistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDrivePerformanceEnvelope:" + self._name + ":velocityDependentResistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSpeedEffortGradientAttr(self):
        return self._prim.GetAttribute("physxDrivePerformanceEnvelope:" + self._name + ":speedEffortGradient")
    def CreateSpeedEffortGradientAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDrivePerformanceEnvelope:" + self._name + ":speedEffortGradient")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDrivePerformanceEnvelope:" + self._name + ":speedEffortGradient", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    @classmethod
    def GetAll(cls, prim):
        prefix = cls._schemaName + ":"
        out = []
        for s in prim.GetAppliedSchemas():
            if s == cls._schemaName:
                out.append(cls(prim, ''))
            elif s.startswith(prefix):
                out.append(cls(prim, s[len(prefix):]))
        return out

class PhysxSplinesSurfaceVelocityAPI(_Base):
    _schemaName = "PhysxSplinesSurfaceVelocityAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxSplinesSurfaceVelocity:surfaceVelocityEnabled', 'physxSplinesSurfaceVelocity:surfaceVelocityForceBased', 'physxSplinesSurfaceVelocity:surfaceVelocityMagnitude']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSurfaceVelocityEnabledAttr(self):
        return self._prim.GetAttribute("physxSplinesSurfaceVelocity:surfaceVelocityEnabled")
    def CreateSurfaceVelocityEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSplinesSurfaceVelocity:surfaceVelocityEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSplinesSurfaceVelocity:surfaceVelocityEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceVelocityForceBasedAttr(self):
        return self._prim.GetAttribute("physxSplinesSurfaceVelocity:surfaceVelocityForceBased")
    def CreateSurfaceVelocityForceBasedAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSplinesSurfaceVelocity:surfaceVelocityForceBased")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSplinesSurfaceVelocity:surfaceVelocityForceBased", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceVelocityMagnitudeAttr(self):
        return self._prim.GetAttribute("physxSplinesSurfaceVelocity:surfaceVelocityMagnitude")
    def CreateSurfaceVelocityMagnitudeAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxSplinesSurfaceVelocity:surfaceVelocityMagnitude")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxSplinesSurfaceVelocity:surfaceVelocityMagnitude", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSurfaceVelocityCurveRel(self):
        return self._prim.GetRelationship("physxSplinesSurfaceVelocity:surfaceVelocityCurve")
    def CreateSurfaceVelocityCurveRel(self):
        return self._prim.CreateRelationship("physxSplinesSurfaceVelocity:surfaceVelocityCurve", False)

class PhysxBaseDeformableBodyAPI(_Base):
    _schemaName = "PhysxBaseDeformableBodyAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableBody:solverPositionIterationCount', 'physxDeformableBody:linearDamping', 'physxDeformableBody:maxLinearVelocity', 'physxDeformableBody:settlingDamping', 'physxDeformableBody:settlingThreshold', 'physxDeformableBody:sleepThreshold', 'physxDeformableBody:maxDepenetrationVelocity', 'physxDeformableBody:selfCollision', 'physxDeformableBody:selfCollisionFilterDistance', 'physxDeformableBody:enableSpeculativeCCD', 'physxDeformableBody:disableGravity']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetSolverPositionIterationCountAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:solverPositionIterationCount")
    def CreateSolverPositionIterationCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:solverPositionIterationCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:solverPositionIterationCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetLinearDampingAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:linearDamping")
    def CreateLinearDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:linearDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:linearDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxLinearVelocityAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:maxLinearVelocity")
    def CreateMaxLinearVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:maxLinearVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:maxLinearVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSettlingDampingAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:settlingDamping")
    def CreateSettlingDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:settlingDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:settlingDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSettlingThresholdAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:settlingThreshold")
    def CreateSettlingThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:settlingThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:settlingThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSleepThresholdAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:sleepThreshold")
    def CreateSleepThresholdAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:sleepThreshold")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:sleepThreshold", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetMaxDepenetrationVelocityAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:maxDepenetrationVelocity")
    def CreateMaxDepenetrationVelocityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:maxDepenetrationVelocity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:maxDepenetrationVelocity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSelfCollisionAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:selfCollision")
    def CreateSelfCollisionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:selfCollision")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:selfCollision", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetSelfCollisionFilterDistanceAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:selfCollisionFilterDistance")
    def CreateSelfCollisionFilterDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:selfCollisionFilterDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:selfCollisionFilterDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableSpeculativeCCDAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:enableSpeculativeCCD")
    def CreateEnableSpeculativeCCDAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:enableSpeculativeCCD")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:enableSpeculativeCCD", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDisableGravityAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:disableGravity")
    def CreateDisableGravityAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:disableGravity")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:disableGravity", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxSurfaceDeformableBodyAPI(_Base):
    _schemaName = "PhysxSurfaceDeformableBodyAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableBody:collisionPairUpdateFrequency', 'physxDeformableBody:collisionIterationMultiplier']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetCollisionPairUpdateFrequencyAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:collisionPairUpdateFrequency")
    def CreateCollisionPairUpdateFrequencyAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:collisionPairUpdateFrequency")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:collisionPairUpdateFrequency", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCollisionIterationMultiplierAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:collisionIterationMultiplier")
    def CreateCollisionIterationMultiplierAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:collisionIterationMultiplier")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:collisionIterationMultiplier", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxDeformableMaterialAPI(_Base):
    _schemaName = "PhysxDeformableMaterialAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableMaterial:elasticityDamping']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetElasticityDampingAttr(self):
        return self._prim.GetAttribute("physxDeformableMaterial:elasticityDamping")
    def CreateElasticityDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableMaterial:elasticityDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableMaterial:elasticityDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxSurfaceDeformableMaterialAPI(_Base):
    _schemaName = "PhysxSurfaceDeformableMaterialAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableMaterial:bendDamping']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetBendDampingAttr(self):
        return self._prim.GetAttribute("physxDeformableMaterial:bendDamping")
    def CreateBendDampingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableMaterial:bendDamping")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableMaterial:bendDamping", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxAutoDeformableBodyAPI(_Base):
    _schemaName = "PhysxAutoDeformableBodyAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableBody:autoDeformableBodyEnabled']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetAutoDeformableBodyEnabledAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:autoDeformableBodyEnabled")
    def CreateAutoDeformableBodyEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:autoDeformableBodyEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:autoDeformableBodyEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCookingSourceMeshRel(self):
        return self._prim.GetRelationship("physxDeformableBody:cookingSourceMesh")
    def CreateCookingSourceMeshRel(self):
        return self._prim.CreateRelationship("physxDeformableBody:cookingSourceMesh", False)

class PhysxAutoDeformableHexahedralMeshAPI(_Base):
    _schemaName = "PhysxAutoDeformableHexahedralMeshAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableBody:resolution']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetResolutionAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:resolution")
    def CreateResolutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:resolution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:resolution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxAutoDeformableMeshSimplificationAPI(_Base):
    _schemaName = "PhysxAutoDeformableMeshSimplificationAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxDeformableBody:autoDeformableMeshSimplificationEnabled', 'physxDeformableBody:remeshingEnabled', 'physxDeformableBody:remeshingResolution', 'physxDeformableBody:targetTriangleCount', 'physxDeformableBody:forceConforming']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetAutoDeformableMeshSimplificationEnabledAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:autoDeformableMeshSimplificationEnabled")
    def CreateAutoDeformableMeshSimplificationEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:autoDeformableMeshSimplificationEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:autoDeformableMeshSimplificationEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRemeshingEnabledAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:remeshingEnabled")
    def CreateRemeshingEnabledAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:remeshingEnabled")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:remeshingEnabled", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRemeshingResolutionAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:remeshingResolution")
    def CreateRemeshingResolutionAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:remeshingResolution")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:remeshingResolution", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetTargetTriangleCountAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:targetTriangleCount")
    def CreateTargetTriangleCountAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.UInt
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:targetTriangleCount")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:targetTriangleCount", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetForceConformingAttr(self):
        return self._prim.GetAttribute("physxDeformableBody:forceConforming")
    def CreateForceConformingAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxDeformableBody:forceConforming")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxDeformableBody:forceConforming", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr

class PhysxAutoDeformableAttachmentAPI(_Base):
    _schemaName = "PhysxAutoDeformableAttachmentAPI"
    schemaKind = 'single'
    _schemaAttributeNames = ['physxAutoDeformableAttachment:enableDeformableVertexAttachments', 'physxAutoDeformableAttachment:deformableVertexOverlapOffset', 'physxAutoDeformableAttachment:enableRigidSurfaceAttachments', 'physxAutoDeformableAttachment:rigidSurfaceSamplingDistance', 'physxAutoDeformableAttachment:enableCollisionFiltering', 'physxAutoDeformableAttachment:collisionFilteringOffset', 'physxAutoDeformableAttachment:enableDeformableFilteringPairs']
    @classmethod
    def _GetStaticTfType(cls):
        return Usd.SchemaRegistry.GetTypeFromSchemaTypeName(cls._schemaName)
    @classmethod
    def GetSchemaAttributeNames(cls, includeInherited=True, instanceName=None):
        names = list(cls._schemaAttributeNames)
        base = cls.__bases__[0]
        if includeInherited and base is not _Base and hasattr(base, 'GetSchemaAttributeNames'):
            names = list(base.GetSchemaAttributeNames(True)) + names
        if instanceName is not None:
            instanceName = str(instanceName)
            if instanceName:
                names = [name.replace(':__INSTANCE_NAME__:', ':' + instanceName + ':') for name in names]
        return names
    @classmethod
    def CanApply(cls, prim):
        return prim.CanApplyAPI(cls._schemaName)  # native _CanApplyAPIResult (bool + .whyNot)
    @classmethod
    def Apply(cls, prim):
        if prim.ApplyAPI(cls._schemaName):
            return cls(prim)
        return cls()
    @classmethod
    def Get(cls, stage, path): return cls(stage.GetPrimAtPath(path))
    def _IsCompatible(self):
        return self._prim.HasAPI(self._schemaName)
    def GetEnableDeformableVertexAttachmentsAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:enableDeformableVertexAttachments")
    def CreateEnableDeformableVertexAttachmentsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:enableDeformableVertexAttachments")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:enableDeformableVertexAttachments", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetDeformableVertexOverlapOffsetAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:deformableVertexOverlapOffset")
    def CreateDeformableVertexOverlapOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:deformableVertexOverlapOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:deformableVertexOverlapOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableRigidSurfaceAttachmentsAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:enableRigidSurfaceAttachments")
    def CreateEnableRigidSurfaceAttachmentsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:enableRigidSurfaceAttachments")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:enableRigidSurfaceAttachments", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetRigidSurfaceSamplingDistanceAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:rigidSurfaceSamplingDistance")
    def CreateRigidSurfaceSamplingDistanceAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:rigidSurfaceSamplingDistance")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:rigidSurfaceSamplingDistance", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableCollisionFilteringAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:enableCollisionFiltering")
    def CreateEnableCollisionFilteringAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:enableCollisionFiltering")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:enableCollisionFiltering", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetCollisionFilteringOffsetAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:collisionFilteringOffset")
    def CreateCollisionFilteringOffsetAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Float
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:collisionFilteringOffset")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:collisionFilteringOffset", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetEnableDeformableFilteringPairsAttr(self):
        return self._prim.GetAttribute("physxAutoDeformableAttachment:enableDeformableFilteringPairs")
    def CreateEnableDeformableFilteringPairsAttr(self, defaultValue=None, writeSparsely=False):
        _t = Sdf.ValueTypeNames.Bool
        if writeSparsely and defaultValue is not None:
            attr = self._prim.GetAttribute("physxAutoDeformableAttachment:enableDeformableFilteringPairs")
            if attr and not attr.HasAuthoredValue() and attr.Get() == _coerce_to_type(defaultValue, _t):
                return attr
        attr = self._prim.CreateAttribute("physxAutoDeformableAttachment:enableDeformableFilteringPairs", _t, False)
        if defaultValue is not None: attr.Set(defaultValue)
        return attr
    def GetAttachable0Rel(self):
        return self._prim.GetRelationship("physxAutoDeformableAttachment:attachable0")
    def CreateAttachable0Rel(self):
        return self._prim.CreateRelationship("physxAutoDeformableAttachment:attachable0", False)
    def GetAttachable1Rel(self):
        return self._prim.GetRelationship("physxAutoDeformableAttachment:attachable1")
    def CreateAttachable1Rel(self):
        return self._prim.CreateRelationship("physxAutoDeformableAttachment:attachable1", False)
    def GetMaskShapesRel(self):
        return self._prim.GetRelationship("physxAutoDeformableAttachment:maskShapes")
    def CreateMaskShapesRel(self):
        return self._prim.CreateRelationship("physxAutoDeformableAttachment:maskShapes", False)
