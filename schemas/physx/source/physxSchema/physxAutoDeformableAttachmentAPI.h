// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxAutoDeformableAttachmentAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXAUTODEFORMABLEATTACHMENTAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXAUTODEFORMABLEATTACHMENTAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxAutoDeformableAttachmentAPI
///
/// PhysxAutoDeformableAttachmentAPI is used to control automatic attachment and element collision filter
/// generation and is applied to UsdGeomImageable prim. The necessary low level attachment and filter primitives,
/// e.g. OmniPhysicsVtxVtxAttachment and OmniPhysicsElementCollisionFilter are provided by the application and are
/// assumed to be direct children of the prim this API is applied to.
/// Either one of the attachable primitives needs to have a UsdPhysicsDeformableBodyAPI. One attachable primitive can
/// be an UsdGeomXformable.
///
class PhysxSchemaPhysxAutoDeformableAttachmentAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxAutoDeformableAttachmentAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxAutoDeformableAttachmentAPI on UsdPrim \p prim .
    /// Equivalent to PhysxSchemaPhysxAutoDeformableAttachmentAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxAutoDeformableAttachmentAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a PhysxSchemaPhysxAutoDeformableAttachmentAPI on the prim held by \p schemaObj .
    /// Should be preferred over PhysxSchemaPhysxAutoDeformableAttachmentAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit PhysxSchemaPhysxAutoDeformableAttachmentAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~PhysxSchemaPhysxAutoDeformableAttachmentAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxAutoDeformableAttachmentEnableDeformableVertexAttachments,
            PhysxSchemaTokens->physxAutoDeformableAttachmentDeformableVertexOverlapOffset,
            PhysxSchemaTokens->physxAutoDeformableAttachmentEnableRigidSurfaceAttachments,
            PhysxSchemaTokens->physxAutoDeformableAttachmentRigidSurfaceSamplingDistance,
            PhysxSchemaTokens->physxAutoDeformableAttachmentEnableCollisionFiltering,
            PhysxSchemaTokens->physxAutoDeformableAttachmentCollisionFilteringOffset,
            PhysxSchemaTokens->physxAutoDeformableAttachmentEnableDeformableFilteringPairs,
        };
        return localNames;
    }

    /// Return a PhysxSchemaPhysxAutoDeformableAttachmentAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// PhysxSchemaPhysxAutoDeformableAttachmentAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static PhysxSchemaPhysxAutoDeformableAttachmentAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return PhysxSchemaPhysxAutoDeformableAttachmentAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxAutoDeformableAttachmentAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "PhysxAutoDeformableAttachmentAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid PhysxSchemaPhysxAutoDeformableAttachmentAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxAutoDeformableAttachmentAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxAutoDeformableAttachmentAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxAutoDeformableAttachmentAPI)) { return PhysxSchemaPhysxAutoDeformableAttachmentAPI(prim); }
        return PhysxSchemaPhysxAutoDeformableAttachmentAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // ENABLEDEFORMABLEVERTEXATTACHMENTS
    // --------------------------------------------------------------------- //
    /// Enables attaching deformable mesh vertices which overlap with the other actor's volume.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxAutoDeformableAttachment:enableDeformableVertexAttachments = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetEnableDeformableVertexAttachmentsAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableDeformableVertexAttachments); }

    /// See GetEnableDeformableVertexAttachmentsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateEnableDeformableVertexAttachmentsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableDeformableVertexAttachments,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // DEFORMABLEVERTEXOVERLAPOFFSET
    // --------------------------------------------------------------------- //
    /// Deformable mesh vertices are attached if their distance to the other actor's volume is smaller than the offset.
    /// Range: [0, inf)
    /// Units: distance
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxAutoDeformableAttachment:deformableVertexOverlapOffset = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetDeformableVertexOverlapOffsetAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentDeformableVertexOverlapOffset); }

    /// See GetDeformableVertexOverlapOffsetAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateDeformableVertexOverlapOffsetAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentDeformableVertexOverlapOffset,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // ENABLERIGIDSURFACEATTACHMENTS
    // --------------------------------------------------------------------- //
    /// Enables sampling attachment points on the rigid actor's surface.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxAutoDeformableAttachment:enableRigidSurfaceAttachments = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetEnableRigidSurfaceAttachmentsAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableRigidSurfaceAttachments); }

    /// See GetEnableRigidSurfaceAttachmentsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateEnableRigidSurfaceAttachmentsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableRigidSurfaceAttachments,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // RIGIDSURFACESAMPLINGDISTANCE
    // --------------------------------------------------------------------- //
    /// Attachment points on the rigid surface are positioned such that their typical neighbor distance matches the sampling distance. Use heuristic by default.
    /// Range: [0, inf)
    /// Units: distance
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxAutoDeformableAttachment:rigidSurfaceSamplingDistance = -inf` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetRigidSurfaceSamplingDistanceAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentRigidSurfaceSamplingDistance); }

    /// See GetRigidSurfaceSamplingDistanceAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRigidSurfaceSamplingDistanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentRigidSurfaceSamplingDistance,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // ENABLECOLLISIONFILTERING
    // --------------------------------------------------------------------- //
    /// Enables adding collision filtering in the vicinity of the attachment points.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxAutoDeformableAttachment:enableCollisionFiltering = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetEnableCollisionFilteringAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableCollisionFiltering); }

    /// See GetEnableCollisionFilteringAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateEnableCollisionFilteringAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableCollisionFiltering,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // COLLISIONFILTERINGOFFSET
    // --------------------------------------------------------------------- //
    /// Filtering ids are generated for deformable mesh vertices if their distance to the other actor's volume
    /// is smaller than the offset. Use heuristic by default.
    /// Range: [0, inf)
    /// Units: distance
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxAutoDeformableAttachment:collisionFilteringOffset = -inf` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetCollisionFilteringOffsetAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentCollisionFilteringOffset); }

    /// See GetCollisionFilteringOffsetAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateCollisionFilteringOffsetAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentCollisionFilteringOffset,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // ENABLEDEFORMABLEFILTERINGPAIRS
    // --------------------------------------------------------------------- //
    /// Enables geometry pair based collision filtering for deformable-deformable attachments.
    /// By default a filtered geometry of one actor is filtered against all geometries of the other actor.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxAutoDeformableAttachment:enableDeformableFilteringPairs = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetEnableDeformableFilteringPairsAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableDeformableFilteringPairs); }

    /// See GetEnableDeformableFilteringPairsAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateEnableDeformableFilteringPairsAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxAutoDeformableAttachmentEnableDeformableFilteringPairs,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // ATTACHABLE0
    // --------------------------------------------------------------------- //
    /// Relationship to a prim which is either a UsdGeomXformable or has a UsdPhysicsDeformableBodyAPI.
    UsdRelationship GetAttachable0Rel() const
    { return GetPrim().GetRelationship(PhysxSchemaTokens->physxAutoDeformableAttachmentAttachable0); }

    /// See GetAttachable0Rel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateAttachable0Rel() const
    { return GetPrim().CreateRelationship(PhysxSchemaTokens->physxAutoDeformableAttachmentAttachable0, /* custom = */ false); }

public:
    // --------------------------------------------------------------------- //
    // ATTACHABLE1
    // --------------------------------------------------------------------- //
    /// Relationship to a prim which is either a UsdGeomXformable or has a UsdPhysicsDeformableBodyAPI.
    UsdRelationship GetAttachable1Rel() const
    { return GetPrim().GetRelationship(PhysxSchemaTokens->physxAutoDeformableAttachmentAttachable1); }

    /// See GetAttachable1Rel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateAttachable1Rel() const
    { return GetPrim().CreateRelationship(PhysxSchemaTokens->physxAutoDeformableAttachmentAttachable1, /* custom = */ false); }

public:
    // --------------------------------------------------------------------- //
    // MASKSHAPES
    // --------------------------------------------------------------------- //
    /// Relationship to UsdGeomSphere, UsdGeomCube or UsdGeomCapsule geometries. The union of
    /// the shapes defines a mask volume within which attachment points are generated.
    UsdRelationship GetMaskShapesRel() const
    { return GetPrim().GetRelationship(PhysxSchemaTokens->physxAutoDeformableAttachmentMaskShapes); }

    /// See GetMaskShapesRel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateMaskShapesRel() const
    { return GetPrim().CreateRelationship(PhysxSchemaTokens->physxAutoDeformableAttachmentMaskShapes, /* custom = */ false); }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
