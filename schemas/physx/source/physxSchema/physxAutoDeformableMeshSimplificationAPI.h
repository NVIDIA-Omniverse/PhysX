// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxAutoDeformableMeshSimplificationAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXAUTODEFORMABLEMESHSIMPLIFICATIONAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXAUTODEFORMABLEMESHSIMPLIFICATIONAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI
///
/// PhysxAutoDeformableMeshSimplificationAPI specifies parameters related to mesh geometry simplification. The
/// simplification process is applied to the cooking source mesh specified by the PhysxAutoDeformableBodyAPI.
///
class PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxAutoDeformableMeshSimplificationAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI on UsdPrim \p prim .
    /// Equivalent to PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI on the prim held by \p schemaObj .
    /// Should be preferred over PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxDeformableBodyAutoDeformableMeshSimplificationEnabled,
            PhysxSchemaTokens->physxDeformableBodyRemeshingEnabled,
            PhysxSchemaTokens->physxDeformableBodyRemeshingResolution,
            PhysxSchemaTokens->physxDeformableBodyTargetTriangleCount,
            PhysxSchemaTokens->physxDeformableBodyForceConforming,
        };
        return localNames;
    }

    /// Return a PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxAutoDeformableMeshSimplificationAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "PhysxAutoDeformableMeshSimplificationAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxAutoDeformableMeshSimplificationAPI)) { return PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI(prim); }
        return PhysxSchemaPhysxAutoDeformableMeshSimplificationAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // AUTODEFORMABLEMESHSIMPLIFICATIONENABLED
    // --------------------------------------------------------------------- //
    /// Determines whether mesh simplification is enabled
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxDeformableBody:autoDeformableMeshSimplificationEnabled = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetAutoDeformableMeshSimplificationEnabledAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyAutoDeformableMeshSimplificationEnabled); }

    /// See GetAutoDeformableMeshSimplificationEnabledAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateAutoDeformableMeshSimplificationEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyAutoDeformableMeshSimplificationEnabled,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // REMESHINGENABLED
    // --------------------------------------------------------------------- //
    /// Determines whether remeshing is enabled
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxDeformableBody:remeshingEnabled = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetRemeshingEnabledAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyRemeshingEnabled); }

    /// See GetRemeshingEnabledAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRemeshingEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyRemeshingEnabled,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // REMESHINGRESOLUTION
    // --------------------------------------------------------------------- //
    /// Remeshing Resolution, 0 indicates that the resolution is automatically determined
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint physxDeformableBody:remeshingResolution = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    UsdAttribute GetRemeshingResolutionAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyRemeshingResolution); }

    /// See GetRemeshingResolutionAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateRemeshingResolutionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyRemeshingResolution,
                   SdfValueTypeNames->UInt, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // TARGETTRIANGLECOUNT
    // --------------------------------------------------------------------- //
    /// Target Triangle Count, 0 indicates that the target count is automatically determined
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint physxDeformableBody:targetTriangleCount = 0` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    UsdAttribute GetTargetTriangleCountAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyTargetTriangleCount); }

    /// See GetTargetTriangleCountAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateTargetTriangleCountAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyTargetTriangleCount,
                   SdfValueTypeNames->UInt, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // FORCECONFORMING
    // --------------------------------------------------------------------- //
    /// Determines whether to force conformation with triangle surface. This parameter only affects the mesh
    /// geometry generation for volume deformable bodies.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxDeformableBody:forceConforming = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetForceConformingAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyForceConforming); }

    /// See GetForceConformingAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateForceConformingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyForceConforming,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
