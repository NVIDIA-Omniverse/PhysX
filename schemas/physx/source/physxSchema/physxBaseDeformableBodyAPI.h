// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxBaseDeformableBodyAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXBASEDEFORMABLEBODYAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXBASEDEFORMABLEBODYAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxBaseDeformableBodyAPI
///
/// PhysxBaseDeformableBodyAPI provides PhysX specific parameters for simulating volume and surface deformables.
/// It can be applied to a prim with OmniPhysicsDeformableBodyAPI.
///
class PhysxSchemaPhysxBaseDeformableBodyAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxBaseDeformableBodyAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxBaseDeformableBodyAPI on UsdPrim \p prim .
    /// Equivalent to PhysxSchemaPhysxBaseDeformableBodyAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxBaseDeformableBodyAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a PhysxSchemaPhysxBaseDeformableBodyAPI on the prim held by \p schemaObj .
    /// Should be preferred over PhysxSchemaPhysxBaseDeformableBodyAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit PhysxSchemaPhysxBaseDeformableBodyAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~PhysxSchemaPhysxBaseDeformableBodyAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxDeformableBodySolverPositionIterationCount,
            PhysxSchemaTokens->physxDeformableBodyLinearDamping,
            PhysxSchemaTokens->physxDeformableBodyMaxLinearVelocity,
            PhysxSchemaTokens->physxDeformableBodySettlingDamping,
            PhysxSchemaTokens->physxDeformableBodySettlingThreshold,
            PhysxSchemaTokens->physxDeformableBodySleepThreshold,
            PhysxSchemaTokens->physxDeformableBodyMaxDepenetrationVelocity,
            PhysxSchemaTokens->physxDeformableBodySelfCollision,
            PhysxSchemaTokens->physxDeformableBodySelfCollisionFilterDistance,
            PhysxSchemaTokens->physxDeformableBodyEnableSpeculativeCCD,
            PhysxSchemaTokens->physxDeformableBodyDisableGravity,
        };
        return localNames;
    }

    /// Return a PhysxSchemaPhysxBaseDeformableBodyAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// PhysxSchemaPhysxBaseDeformableBodyAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static PhysxSchemaPhysxBaseDeformableBodyAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return PhysxSchemaPhysxBaseDeformableBodyAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxBaseDeformableBodyAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "PhysxBaseDeformableBodyAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid PhysxSchemaPhysxBaseDeformableBodyAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxBaseDeformableBodyAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxBaseDeformableBodyAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxBaseDeformableBodyAPI)) { return PhysxSchemaPhysxBaseDeformableBodyAPI(prim); }
        return PhysxSchemaPhysxBaseDeformableBodyAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // SOLVERPOSITIONITERATIONCOUNT
    // --------------------------------------------------------------------- //
    /// Number of solver position iterations per time step.
    /// Range: [1, 255]
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint physxDeformableBody:solverPositionIterationCount = 16` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    UsdAttribute GetSolverPositionIterationCountAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodySolverPositionIterationCount); }

    /// See GetSolverPositionIterationCountAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSolverPositionIterationCountAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodySolverPositionIterationCount,
                   SdfValueTypeNames->UInt, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // LINEARDAMPING
    // --------------------------------------------------------------------- //
    /// Linear damping coefficient.
    /// Range: [0, inf)
    /// Units: 1 / seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:linearDamping = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetLinearDampingAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyLinearDamping); }

    /// See GetLinearDampingAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateLinearDampingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyLinearDamping,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // MAXLINEARVELOCITY
    // --------------------------------------------------------------------- //
    /// Maximum allowable linear velocity for the deformable body.
    /// For surface deformables: If set to a negative value, the simulation determines a default value.
    /// Range: [0, inf)
    /// Units: distance / seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:maxLinearVelocity = inf` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetMaxLinearVelocityAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyMaxLinearVelocity); }

    /// See GetMaxLinearVelocityAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateMaxLinearVelocityAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyMaxLinearVelocity,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SETTLINGDAMPING
    // --------------------------------------------------------------------- //
    /// Additional damping term if vertex velocity drops below settlingThreshold.
    /// Range: [0, inf)
    /// Units: 1 / seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:settlingDamping = 10` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetSettlingDampingAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodySettlingDamping); }

    /// See GetSettlingDampingAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSettlingDampingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodySettlingDamping,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SETTLINGTHRESHOLD
    // --------------------------------------------------------------------- //
    /// Threshold vertex velocity under which sleep damping is applied in addition to velocity damping.
    /// Range: [0, inf]
    /// Units: distance / seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:settlingThreshold = 0.1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetSettlingThresholdAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodySettlingThreshold); }

    /// See GetSettlingThresholdAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSettlingThresholdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodySettlingThreshold,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SLEEPTHRESHOLD
    // --------------------------------------------------------------------- //
    /// Velocity threshold under which the vertex becomes a candidate for sleeping.
    /// Range: [0, inf)
    /// Units: distance / seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:sleepThreshold = 0.05` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetSleepThresholdAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodySleepThreshold); }

    /// See GetSleepThresholdAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSleepThresholdAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodySleepThreshold,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // MAXDEPENETRATIONVELOCITY
    // --------------------------------------------------------------------- //
    /// The maximum velocity permitted to be introduced by the solver to depenetrate intersections.
    /// Range: [0, inf)
    /// Units: distance / seconds
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:maxDepenetrationVelocity = inf` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetMaxDepenetrationVelocityAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyMaxDepenetrationVelocity); }

    /// See GetMaxDepenetrationVelocityAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateMaxDepenetrationVelocityAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyMaxDepenetrationVelocity,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SELFCOLLISION
    // --------------------------------------------------------------------- //
    /// Enables self collisions on the deformable, preventing self intersections.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxDeformableBody:selfCollision = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetSelfCollisionAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodySelfCollision); }

    /// See GetSelfCollisionAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSelfCollisionAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodySelfCollision,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SELFCOLLISIONFILTERDISTANCE
    // --------------------------------------------------------------------- //
    /// Distance under which self-collisions are disabled. Default value -inf means default is picked by the simulation.
    /// Range: [2*physxCollision:restOffset, max_float]
    /// Units: distance
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxDeformableBody:selfCollisionFilterDistance = -inf` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetSelfCollisionFilterDistanceAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodySelfCollisionFilterDistance); }

    /// See GetSelfCollisionFilterDistanceAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSelfCollisionFilterDistanceAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodySelfCollisionFilterDistance,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // ENABLESPECULATIVECCD
    // --------------------------------------------------------------------- //
    /// Register a deformable body to dynamically adjust contact offset based on velocity.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxDeformableBody:enableSpeculativeCCD = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetEnableSpeculativeCCDAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyEnableSpeculativeCCD); }

    /// See GetEnableSpeculativeCCDAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateEnableSpeculativeCCDAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyEnableSpeculativeCCD,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // DISABLEGRAVITY
    // --------------------------------------------------------------------- //
    /// Disable gravity for the deformable body.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxDeformableBody:disableGravity = 0` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetDisableGravityAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyDisableGravity); }

    /// See GetDisableGravityAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateDisableGravityAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyDisableGravity,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
