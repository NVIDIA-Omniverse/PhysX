// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxMimicJointAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXMIMICJOINTAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXMIMICJOINTAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxMimicJointAPI
///
/// DEPRECATED: use NewtonMimicAPI (single-apply) from the Newton schema instead. PhysxMimicJointAPI
/// is kept for backward compatibility with existing assets; new content should author NewtonMimicAPI.
/// The NewtonMimicMigrationChecker validator in omni.physx.asset_validator can auto-migrate eligible
/// single-DOF usages. See the Newton schema documentation for the mapping and coefficient conventions.
///
/// Applied to a Physics Joint that must be part of an articulation. Supported joint types are:
/// PhysicsRevoluteJoint (with a limit set), PhysicsPrismaticJoint as well as the more generic
/// PhysicsJoint if all linear degrees of freedom are locked.
///
/// This schema allows to interconnect the degrees of freedom of two joints according to the relationship:
/// jointPosition + (gearing * referenceJointPosition) + offset = 0. This can be used to have, for example,
/// one joint mimic the motion of another joint. The term "position" does not necessarily mean a translational
/// unit, in the case of an angular degree of freedom it is an angle. The mimic joint operates on a single
/// degree of freedom which is specified through the instance name of this multiple apply schema. The allowed
/// instance name tokens are "rotX", "rotY" and "rotZ". See the documentation of the attribute referenceJointAxis
/// for more info related to specifying the degree of freedom to operate on. Notes: 1) the two joints have to
/// be part of the same articulation. 2) once the simulation has started, a significant performance penalty will
/// be incurred if a mimic joint is added or removed, if the referenceJoint relationship is changed or if the
/// referenceJointAxis attributes is modified. 3) the mimic joint relationship is interpreted as a two-way
/// interaction and an impulse will get applied to the reference joint as well. Mimic joints may be confiugred as
/// hard constraints that will apply the forces necessary to maintain the mimic joint equation.
/// Alternatively, mimic joints may be configured with compliance using natural frequency and damping attributes.
///
/// For any described attribute \em Fallback \em Value or \em Allowed \em Values below
/// that are text/tokens, the actual token is published and defined in \ref PhysxSchemaTokens.
/// So to set an attribute to the value "rightHanded", use PhysxSchemaTokens->rightHanded
/// as the value.
///
class PhysxSchemaPhysxMimicJointAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::MultipleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxMimicJointAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxMimicJointAPI on UsdPrim \p prim with
    /// name \p name . Equivalent to
    /// PhysxSchemaPhysxMimicJointAPI::Get(
    ///    prim.GetStage(),
    ///    prim.GetPath().AppendProperty(
    ///        "physxMimicJoint:name"));
    ///
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxMimicJointAPI(const UsdPrim& prim=UsdPrim(), const TfToken &name=TfToken())
        : UsdAPISchemaBase(prim, name) {}

    /// Construct a PhysxSchemaPhysxMimicJointAPI on the prim held by \p schemaObj with
    /// name \p name.  Should be preferred over
    /// PhysxSchemaPhysxMimicJointAPI(schemaObj.GetPrim(), name), as it preserves
    /// SchemaBase state.
    explicit PhysxSchemaPhysxMimicJointAPI(const UsdSchemaBase& schemaObj, const TfToken &name)
        : UsdAPISchemaBase(schemaObj, name) {}

    /// Returns the name of this multiple-apply schema instance
    TfToken GetName() const { return _GetInstanceName(); }

    /// Destructor.
    virtual ~PhysxSchemaPhysxMimicJointAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJointAxis,
            PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Gearing,
            PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Offset,
            PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_NaturalFrequency,
            PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_DampingRatio,
        };
        return localNames;
    }

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes for a given instance name.  Does not
    /// include attributes that may be authored by custom/extended methods of
    /// the schemas involved. The names returned will have the proper namespace
    /// prefix.
    static TfTokenVector GetSchemaAttributeNames(bool includeInherited, const TfToken &instanceName)
    {
        const TfTokenVector &attrNames = GetSchemaAttributeNames(includeInherited);
        if (instanceName.IsEmpty()) {
            return attrNames;
        }
        TfTokenVector result;
        result.reserve(attrNames.size());
        for (const TfToken &attrName : attrNames) {
            result.push_back(UsdSchemaRegistry::MakeMultipleApplyNameInstance(attrName, instanceName));
        }
        return result;
    }

    /// Checks if the given name \p baseName is the base name of a property
    /// of PhysxMimicJointAPI.
    static bool IsSchemaPropertyBaseName(const TfToken &baseName)
    {
        static TfTokenVector attrsAndRels = {
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJointAxis),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Gearing),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Offset),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_NaturalFrequency),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_DampingRatio),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJoint),
        };
        return std::find(attrsAndRels.begin(), attrsAndRels.end(), baseName) != attrsAndRels.end();
    }

    /// Checks if the given path \p path is of an API schema of type
    /// PhysxMimicJointAPI. If so, it stores the instance name of
    /// the schema in \p name and returns true. Otherwise, it returns false.
    static bool IsPhysxMimicJointAPIPath(const SdfPath &path, TfToken *name)
    {
        if (!path.IsPropertyPath()) {
            return false;
        }
        const std::string propertyName = path.GetName();
        const TfTokenVector tokens = SdfPath::TokenizeIdentifierAsTokens(propertyName);
        if (tokens.empty()) {
            return false;
        }
        const TfToken &baseName = *tokens.rbegin();
        if (IsSchemaPropertyBaseName(baseName)) {
            return false;
        }
        if (tokens.size() >= 2
            && tokens[0] == PhysxSchemaTokens->physxMimicJoint) {
            if (name) {
                *name = TfToken(propertyName.substr(PhysxSchemaTokens->physxMimicJoint.GetString().size() + 1));
            }
            return true;
        }
        return false;
    }

    /// Return a PhysxSchemaPhysxMimicJointAPI with name \p name holding the
    /// prim \p prim. Shorthand for PhysxSchemaPhysxMimicJointAPI(prim, name);
    static PhysxSchemaPhysxMimicJointAPI Get(const UsdPrim &prim, const TfToken &name)
    { return PhysxSchemaPhysxMimicJointAPI(prim, name); }

    /// Return a PhysxSchemaPhysxMimicJointAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  \p path must be of the format
    /// <path>.physxMimicJoint:name .
    ///
    /// This is shorthand for the following:
    ///
    /// \code
    /// TfToken name = SdfPath::StripNamespace(path.GetToken());
    /// PhysxSchemaPhysxMimicJointAPI(
    ///     stage->GetPrimAtPath(path.GetPrimPath()), name);
    /// \endcode
    ///
    static PhysxSchemaPhysxMimicJointAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    {
        if (!stage) {
            return PhysxSchemaPhysxMimicJointAPI();
        }
        TfToken name;
        if (!IsPhysxMimicJointAPIPath(path, &name)) {
            return PhysxSchemaPhysxMimicJointAPI();
        }
        return PhysxSchemaPhysxMimicJointAPI(stage->GetPrimAtPath(path.GetPrimPath()), name);
    }

    /// Return a vector of all named instances of PhysxSchemaPhysxMimicJointAPI on the
    /// given \p prim.
    static std::vector<PhysxSchemaPhysxMimicJointAPI> GetAll(const UsdPrim &prim)
    {
        std::vector<PhysxSchemaPhysxMimicJointAPI> schemas;
        const TfType &type = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxMimicJointAPI);
        for (const auto &n : _GetMultipleApplyInstanceNames(prim, type)) {
            schemas.emplace_back(prim, n);
        }
        return schemas;
    }

    /// Returns true if this <b>multiple-apply</b> API schema can be applied,
    /// with the given instance name, \p name, to the given \p prim. If this
    /// schema can not be a applied the prim, this returns false and, if
    /// provided, populates \p whyNot with the reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, const TfToken &name, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxMimicJointAPI, name, whyNot); }

    /// Applies this <b>multiple-apply</b> API schema to the given \p prim
    /// along with the given instance name, \p name.
    ///
    /// This information is stored by adding "PhysxMimicJointAPI:<i>name</i>"
    /// to the token-valued, listOp metadata \em apiSchemas on the prim.
    /// For example, if \p name is 'instance1', the token
    /// 'PhysxMimicJointAPI:instance1' is added to 'apiSchemas'.
    ///
    /// \return A valid PhysxSchemaPhysxMimicJointAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxMimicJointAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for
    /// conditions resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxMimicJointAPI Apply(const UsdPrim &prim, const TfToken &name)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxMimicJointAPI, name)) { return PhysxSchemaPhysxMimicJointAPI(prim, name); }
        return PhysxSchemaPhysxMimicJointAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // REFERENCEJOINTAXIS
    // --------------------------------------------------------------------- //
    /// The axis that marks the degree of freedom of the reference joint to mimic. Has to be a valid
    /// degree of freedom on the reference joint. For joint types with a single degree of freedom
    /// (like PhysicsRevoluteJoint or PhysicsPrismaticJoint), this attribute will be ignored since the
    /// axis is defined implicitly. The mimic joint connects <targetJoint, targetJointAxis> to
    /// <referenceJoint, referenceJointAxis> with targetJoint being the prim that has PhysxMimicJointAPI
    /// applied and targetJointAxis being defined through the instance name token of PhysxMimicJointAPI.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uniform token referenceJointAxis = "rotX"` |
    /// | C++ Type | TfToken |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Token |
    /// | \ref SdfVariability "Variability" | SdfVariabilityUniform |
    /// | \ref PhysxSchemaTokens "Allowed Values" | rotX, rotY, rotZ |
    UsdAttribute GetReferenceJointAxisAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJointAxis, _GetInstanceName())); }

    /// See GetReferenceJointAxisAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateReferenceJointAxisAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJointAxis, _GetInstanceName()),
                   SdfValueTypeNames->Token, /* custom = */ false,
                   SdfVariabilityUniform, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // GEARING
    // --------------------------------------------------------------------- //
    /// The gearing coefficient (see relationship formulation in the documentation part of PhysxMimicJointAPI).
    /// Range: (-inf, inf)
    /// Units:
    /// if linear to angular: degrees / distance
    /// if angular to linear: distance / degrees
    /// Otherwise the value is unitless
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float gearing = 1` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetGearingAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Gearing, _GetInstanceName())); }

    /// See GetGearingAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateGearingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Gearing, _GetInstanceName()),
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // OFFSET
    // --------------------------------------------------------------------- //
    /// The offset coefficient (see relationship formulation in the documentation part of PhysxMimicJointAPI).
    /// Range: (-inf, inf)
    /// Units: distance or degrees (depending on whether the mimic joint's degree of freedom is linear or angular)
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float offset = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetOffsetAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Offset, _GetInstanceName())); }

    /// See GetOffsetAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateOffsetAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_Offset, _GetInstanceName()),
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // NATURALFREQUENCY
    // --------------------------------------------------------------------- //
    /// The natural frequency of mimic joint compliance. A mimic joint with naturalFrequency <= 0 will behave as a hard constraint. Larger values of naturalFrequency and dampingRatio will make the mimic joint stiffer and more akin to a hard constraint.
    /// Range:
    /// (-inf, inf)
    /// Units: s^-1
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float naturalFrequency = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetNaturalFrequencyAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_NaturalFrequency, _GetInstanceName())); }

    /// See GetNaturalFrequencyAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateNaturalFrequencyAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_NaturalFrequency, _GetInstanceName()),
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // DAMPINGRATIO
    // --------------------------------------------------------------------- //
    /// The damping ratio of mimic joint compliance. A mimic joint with dampingRatio <= 0 will behave as a hard constraint. Larger values of naturalFrequency and dampingRatio will make the mimic joint stiffer and more akin to a hard constraint.
    /// Range: (-inf, inf)
    /// Units: dimensionless
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float dampingRatio = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetDampingRatioAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_DampingRatio, _GetInstanceName())); }

    /// See GetDampingRatioAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateDampingRatioAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_DampingRatio, _GetInstanceName()),
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // REFERENCEJOINT
    // --------------------------------------------------------------------- //
    /// The joint that shall be mimicked. Relationship to a prim that has the PhysicsJoint API schema
    /// applied. For the supported joint types, see the general documentation of PhysxMimicJointAPI. The
    /// reference joint as well as the joint that mimics the reference joint have to be part of the same
    /// articulation (see PhysicsArticulationRootAPI).
    UsdRelationship GetReferenceJointRel() const
    { return GetPrim().GetRelationship(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJoint, _GetInstanceName())); }

    /// See GetReferenceJointRel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateReferenceJointRel() const
    { return GetPrim().CreateRelationship(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxMimicJoint_MultipleApplyTemplate_ReferenceJoint, _GetInstanceName()), /* custom = */ false); }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
