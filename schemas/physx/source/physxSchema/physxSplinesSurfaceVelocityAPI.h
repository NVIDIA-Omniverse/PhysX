// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxSplinesSurfaceVelocityAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXSPLINESSURFACEVELOCITYAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXSPLINESSURFACEVELOCITYAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxSplinesSurfaceVelocityAPI
///
/// PhysxSplinesSurfaceVelocityAPI enables surface velocity simulation
/// that injects velocity to the solver through internal contact modify
/// callback. Surface velocity can be used typically for conveyer belt
/// simulation. It must be applied to a prim with UsdPhysicsRigidBodyAPI.
/// The motion is defined by a velocity magnitude and UsdGeomBasisCurves that
/// defines the motion direction.
///
class PhysxSchemaPhysxSplinesSurfaceVelocityAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxSplinesSurfaceVelocityAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxSplinesSurfaceVelocityAPI on UsdPrim \p prim .
    /// Equivalent to PhysxSchemaPhysxSplinesSurfaceVelocityAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxSplinesSurfaceVelocityAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a PhysxSchemaPhysxSplinesSurfaceVelocityAPI on the prim held by \p schemaObj .
    /// Should be preferred over PhysxSchemaPhysxSplinesSurfaceVelocityAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit PhysxSchemaPhysxSplinesSurfaceVelocityAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~PhysxSchemaPhysxSplinesSurfaceVelocityAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityEnabled,
            PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityForceBased,
            PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityMagnitude,
        };
        return localNames;
    }

    /// Return a PhysxSchemaPhysxSplinesSurfaceVelocityAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// PhysxSchemaPhysxSplinesSurfaceVelocityAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static PhysxSchemaPhysxSplinesSurfaceVelocityAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return PhysxSchemaPhysxSplinesSurfaceVelocityAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxSplinesSurfaceVelocityAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "PhysxSplinesSurfaceVelocityAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid PhysxSchemaPhysxSplinesSurfaceVelocityAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxSplinesSurfaceVelocityAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxSplinesSurfaceVelocityAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxSplinesSurfaceVelocityAPI)) { return PhysxSchemaPhysxSplinesSurfaceVelocityAPI(prim); }
        return PhysxSchemaPhysxSplinesSurfaceVelocityAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // SURFACEVELOCITYENABLED
    // --------------------------------------------------------------------- //
    /// Whether surface velocity is enabled or not.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxSplinesSurfaceVelocity:surfaceVelocityEnabled = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetSurfaceVelocityEnabledAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityEnabled); }

    /// See GetSurfaceVelocityEnabledAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSurfaceVelocityEnabledAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityEnabled,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SURFACEVELOCITYFORCEBASED
    // --------------------------------------------------------------------- //
    /// Whether surface velocity is applied as forces to the bodies.
    /// Doing so will introduce one step latency as contacts are gathered and
    /// forces are applied in the next simulation step. Otherwise computation
    /// happens during contact modify callback, which can have performance
    /// implications.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `bool physxSplinesSurfaceVelocity:surfaceVelocityForceBased = 1` |
    /// | C++ Type | bool |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Bool |
    UsdAttribute GetSurfaceVelocityForceBasedAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityForceBased); }

    /// See GetSurfaceVelocityForceBasedAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSurfaceVelocityForceBasedAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityForceBased,
                   SdfValueTypeNames->Bool, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SURFACEVELOCITYMAGNITUDE
    // --------------------------------------------------------------------- //
    /// Surface velocity magnitude applied through contact modify callback.
    /// Note that if objects are scaled, the surface velocity magnitude has to be
    /// adjusted.
    /// Units: distance/second.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float physxSplinesSurfaceVelocity:surfaceVelocityMagnitude = 0` |
    /// | C++ Type | float |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->Float |
    UsdAttribute GetSurfaceVelocityMagnitudeAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityMagnitude); }

    /// See GetSurfaceVelocityMagnitudeAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateSurfaceVelocityMagnitudeAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityMagnitude,
                   SdfValueTypeNames->Float, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // SURFACEVELOCITYCURVE
    // --------------------------------------------------------------------- //
    /// Relationship to UsdGeomBasisCurves that defines the motion direction.
    /// When a contact is found the closest point on the spline is found and the
    /// motion direction is derived as the tangent at that spline point.
    ///
    /// Note that only one spline is supported.
    /// Note that runtime changes to the curve are not supported.
    UsdRelationship GetSurfaceVelocityCurveRel() const
    { return GetPrim().GetRelationship(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityCurve); }

    /// See GetSurfaceVelocityCurveRel(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create
    UsdRelationship CreateSurfaceVelocityCurveRel() const
    { return GetPrim().CreateRelationship(PhysxSchemaTokens->physxSplinesSurfaceVelocitySurfaceVelocityCurve, /* custom = */ false); }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
