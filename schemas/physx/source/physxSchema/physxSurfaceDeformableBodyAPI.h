// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxSurfaceDeformableBodyAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXSURFACEDEFORMABLEBODYAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXSURFACEDEFORMABLEBODYAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxSurfaceDeformableBodyAPI
///
/// PhysxSurfaceDeformableBodyAPI provides PhysX specific parameters for simulating surface deformables. It can
/// be applied to a prim with OmniPhysicsDeformableBodyAPI.
///
class PhysxSchemaPhysxSurfaceDeformableBodyAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::SingleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxSurfaceDeformableBodyAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxSurfaceDeformableBodyAPI on UsdPrim \p prim .
    /// Equivalent to PhysxSchemaPhysxSurfaceDeformableBodyAPI::Get(prim.GetStage(), prim.GetPath())
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxSurfaceDeformableBodyAPI(const UsdPrim& prim=UsdPrim()) : UsdAPISchemaBase(prim) {}

    /// Construct a PhysxSchemaPhysxSurfaceDeformableBodyAPI on the prim held by \p schemaObj .
    /// Should be preferred over PhysxSchemaPhysxSurfaceDeformableBodyAPI(schemaObj.GetPrim()),
    /// as it preserves SchemaBase state.
    explicit PhysxSchemaPhysxSurfaceDeformableBodyAPI(const UsdSchemaBase& schemaObj) : UsdAPISchemaBase(schemaObj) {}

    /// Destructor.
    virtual ~PhysxSchemaPhysxSurfaceDeformableBodyAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxDeformableBodyCollisionPairUpdateFrequency,
            PhysxSchemaTokens->physxDeformableBodyCollisionIterationMultiplier,
        };
        return localNames;
    }

    /// Return a PhysxSchemaPhysxSurfaceDeformableBodyAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  This is shorthand for the following:
    ///
    /// \code
    /// PhysxSchemaPhysxSurfaceDeformableBodyAPI(stage->GetPrimAtPath(path));
    /// \endcode
    ///
    static PhysxSchemaPhysxSurfaceDeformableBodyAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    { return PhysxSchemaPhysxSurfaceDeformableBodyAPI(stage->GetPrimAtPath(path)); }

    /// Returns true if this <b>single-apply</b> API schema can be applied to
    /// the given \p prim. If this schema can not be a applied to the prim,
    /// this returns false and, if provided, populates \p whyNot with the
    /// reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxSurfaceDeformableBodyAPI, whyNot); }

    /// Applies this <b>single-apply</b> API schema to the given \p prim.
    /// This information is stored by adding "PhysxSurfaceDeformableBodyAPI" to the
    /// token-valued, listOp metadata \em apiSchemas on the prim.
    ///
    /// \return A valid PhysxSchemaPhysxSurfaceDeformableBodyAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxSurfaceDeformableBodyAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for conditions
    /// resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxSurfaceDeformableBodyAPI Apply(const UsdPrim &prim)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxSurfaceDeformableBodyAPI)) { return PhysxSchemaPhysxSurfaceDeformableBodyAPI(prim); }
        return PhysxSchemaPhysxSurfaceDeformableBodyAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // COLLISIONPAIRUPDATEFREQUENCY
    // --------------------------------------------------------------------- //
    /// Determines how often deformable surface collision pairs are updated during each time step.
    /// Increasing this value results in more frequent updates to the contact pairs, which provides better contact points.
    /// For example, a value of 2 means collision pairs are updated twice per time step:
    /// once at the beginning and once in the middle of the time step (i.e., during the middle solver iteration).
    /// If set to 0, the solver adaptively determines when to update the deformable surface contact pairs, instead of using a fixed frequency.
    /// Range [1, solverPositionIterationCount]
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint physxDeformableBody:collisionPairUpdateFrequency = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    UsdAttribute GetCollisionPairUpdateFrequencyAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyCollisionPairUpdateFrequency); }

    /// See GetCollisionPairUpdateFrequencyAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateCollisionPairUpdateFrequencyAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyCollisionPairUpdateFrequency,
                   SdfValueTypeNames->UInt, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // COLLISIONITERATIONMULTIPLIER
    // --------------------------------------------------------------------- //
    /// Determines how many collision subiterations are used in each solver iteration.
    /// By default, collision constraints are applied once per solver iteration.
    /// Increasing this value applies collision constraints more frequently within each solver iteration.
    /// For example, a value of 2 means collision constraints are applied twice per solver iteration
    /// (i.e., collision constraints are applied 2 x solverPositionIterationCount times per time step).
    /// Increasing this value does not update collision pairs more frequently; refer to collisionPairUpdateFrequency for that.
    /// Range [1, solverPositionIterationCount/2]
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uint physxDeformableBody:collisionIterationMultiplier = 1` |
    /// | C++ Type | unsigned int |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->UInt |
    UsdAttribute GetCollisionIterationMultiplierAttr() const
    { return GetPrim().GetAttribute(PhysxSchemaTokens->physxDeformableBodyCollisionIterationMultiplier); }

    /// See GetCollisionIterationMultiplierAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateCollisionIterationMultiplierAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(PhysxSchemaTokens->physxDeformableBodyCollisionIterationMultiplier,
                   SdfValueTypeNames->UInt, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
