// SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
// SPDX-License-Identifier: Apache-2.0
//
// GENERATED by tools/gen_codeless_api.py -- header-only codeless wrapper. DO NOT EDIT.
/// \file physxSchema/physxTendonAxisAPI.h
#ifndef PHYSXSCHEMA_GENERATED_PHYSXTENDONAXISAPI_API_H
#define PHYSXSCHEMA_GENERATED_PHYSXTENDONAXISAPI_API_H

#include "physxSchema/tokens.h"
#include <pxr/usd/usd/apiSchemaBase.h>
#include <pxr/usd/usd/typed.h>
#include <pxr/usd/usd/prim.h>
#include <pxr/usd/usd/stage.h>
#include <pxr/usd/usd/schemaRegistry.h>
#include <pxr/usd/usd/collectionAPI.h>
#include <pxr/usd/sdf/path.h>
#include <pxr/base/tf/type.h>
#include <algorithm>
#include <string>
#include <vector>

PXR_NAMESPACE_OPEN_SCOPE

/// \class PhysxSchemaPhysxTendonAxisAPI
///
/// WARNING: Draft API, this design is not fixed and may change in the future. At this point, we don't support
/// multi-axis joints (e.g. spherical, D6).
///
/// Applied to a Physics Joint that must be part of an articulation, e.g. PhysicsRevoluteJoint or PhysicsPrismaticJoint.
///
/// A tendon axis is part of a fixed tendon and contributes to the tendon length via the position of its associated
/// articulation joint axis and the gearing attribute. The tendon applies spring-damper forces to the articulation links,
/// scaled by the force coefficient, that aim to maintain constraints on the tendon lengths.
///
/// A joint may have multiple tendon axes that belong to distinct fixed tendons; therefore, the fixed-tendon APIs are
/// multi-apply and are grouped into tendons by their instance names.
///
/// Fixed tendons do not allow linking arbitrary joint axes of the articulation: The respective joints must all be
/// directly connected to each other in the articulation structure, i.e. each of the joints in the tendon must be
/// connected by a single articulation link to another joint in the same tendon. This implies that fixed tendons can
/// branch along with a branching articulation as well. In addition, the root tendon axis created by applying the
/// PhysxTendonAxisRootAPI must be applied to the articulation joint that is the common ancestor of all joint
/// axes in the fixed tendon.
///
/// In a future version, it will be possible to include multiple axes of a multi-axis joint (e.g. a spherical joint)
/// in the fixed tendon by adding them to the axis token array, and setting their gearing and forceCoefficient accordingly
/// in the respective float arrays (in the same order). Until then, the jointAxis token array is ignored, and only the first
/// element of the gearing array is considered.
///
class PhysxSchemaPhysxTendonAxisAPI : public UsdAPISchemaBase
{
public:
    /// Compile time constant representing what kind of schema this class is.
    ///
    /// \sa UsdSchemaKind
    static const UsdSchemaKind schemaKind = UsdSchemaKind::MultipleApplyAPI;

    static const TfType &_GetStaticTfType()
    {
        static TfType t = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxTendonAxisAPI);
        return t;
    }

    /// Construct a PhysxSchemaPhysxTendonAxisAPI on UsdPrim \p prim with
    /// name \p name . Equivalent to
    /// PhysxSchemaPhysxTendonAxisAPI::Get(
    ///    prim.GetStage(),
    ///    prim.GetPath().AppendProperty(
    ///        "physxTendon:name"));
    ///
    /// for a \em valid \p prim, but will not immediately throw an error for
    /// an invalid \p prim
    explicit PhysxSchemaPhysxTendonAxisAPI(const UsdPrim& prim=UsdPrim(), const TfToken &name=TfToken())
        : UsdAPISchemaBase(prim, name) {}

    /// Construct a PhysxSchemaPhysxTendonAxisAPI on the prim held by \p schemaObj with
    /// name \p name.  Should be preferred over
    /// PhysxSchemaPhysxTendonAxisAPI(schemaObj.GetPrim(), name), as it preserves
    /// SchemaBase state.
    explicit PhysxSchemaPhysxTendonAxisAPI(const UsdSchemaBase& schemaObj, const TfToken &name)
        : UsdAPISchemaBase(schemaObj, name) {}

    /// Returns the name of this multiple-apply schema instance
    TfToken GetName() const { return _GetInstanceName(); }

    /// Destructor.
    virtual ~PhysxSchemaPhysxTendonAxisAPI() {}

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes.  Does not include attributes that
    /// may be authored by custom/extended methods of the schemas involved.
    static const TfTokenVector &GetSchemaAttributeNames(bool includeInherited=true)
    {
        static TfTokenVector localNames = {
            PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_Gearing,
            PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_ForceCoefficient,
            PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_JointAxis,
        };
        return localNames;
    }

    /// Return a vector of names of all pre-declared attributes for this schema
    /// class and all its ancestor classes for a given instance name.  Does not
    /// include attributes that may be authored by custom/extended methods of
    /// the schemas involved. The names returned will have the proper namespace
    /// prefix.
    static TfTokenVector GetSchemaAttributeNames(bool includeInherited, const TfToken &instanceName)
    {
        const TfTokenVector &attrNames = GetSchemaAttributeNames(includeInherited);
        if (instanceName.IsEmpty()) {
            return attrNames;
        }
        TfTokenVector result;
        result.reserve(attrNames.size());
        for (const TfToken &attrName : attrNames) {
            result.push_back(UsdSchemaRegistry::MakeMultipleApplyNameInstance(attrName, instanceName));
        }
        return result;
    }

    /// Checks if the given name \p baseName is the base name of a property
    /// of PhysxTendonAxisAPI.
    static bool IsSchemaPropertyBaseName(const TfToken &baseName)
    {
        static TfTokenVector attrsAndRels = {
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_Gearing),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_ForceCoefficient),
            UsdSchemaRegistry::GetMultipleApplyNameTemplateBaseName(
                PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_JointAxis),
        };
        return std::find(attrsAndRels.begin(), attrsAndRels.end(), baseName) != attrsAndRels.end();
    }

    /// Checks if the given path \p path is of an API schema of type
    /// PhysxTendonAxisAPI. If so, it stores the instance name of
    /// the schema in \p name and returns true. Otherwise, it returns false.
    static bool IsPhysxTendonAxisAPIPath(const SdfPath &path, TfToken *name)
    {
        if (!path.IsPropertyPath()) {
            return false;
        }
        const std::string propertyName = path.GetName();
        const TfTokenVector tokens = SdfPath::TokenizeIdentifierAsTokens(propertyName);
        if (tokens.empty()) {
            return false;
        }
        const TfToken &baseName = *tokens.rbegin();
        if (IsSchemaPropertyBaseName(baseName)) {
            return false;
        }
        if (tokens.size() >= 2
            && tokens[0] == PhysxSchemaTokens->physxTendon) {
            if (name) {
                *name = TfToken(propertyName.substr(PhysxSchemaTokens->physxTendon.GetString().size() + 1));
            }
            return true;
        }
        return false;
    }

    /// Return a PhysxSchemaPhysxTendonAxisAPI with name \p name holding the
    /// prim \p prim. Shorthand for PhysxSchemaPhysxTendonAxisAPI(prim, name);
    static PhysxSchemaPhysxTendonAxisAPI Get(const UsdPrim &prim, const TfToken &name)
    { return PhysxSchemaPhysxTendonAxisAPI(prim, name); }

    /// Return a PhysxSchemaPhysxTendonAxisAPI holding the prim adhering to this
    /// schema at \p path on \p stage.  If no prim exists at \p path on
    /// \p stage, or if the prim at that path does not adhere to this schema,
    /// return an invalid schema object.  \p path must be of the format
    /// <path>.physxTendon:name .
    ///
    /// This is shorthand for the following:
    ///
    /// \code
    /// TfToken name = SdfPath::StripNamespace(path.GetToken());
    /// PhysxSchemaPhysxTendonAxisAPI(
    ///     stage->GetPrimAtPath(path.GetPrimPath()), name);
    /// \endcode
    ///
    static PhysxSchemaPhysxTendonAxisAPI Get(const UsdStagePtr &stage, const SdfPath &path)
    {
        if (!stage) {
            return PhysxSchemaPhysxTendonAxisAPI();
        }
        TfToken name;
        if (!IsPhysxTendonAxisAPIPath(path, &name)) {
            return PhysxSchemaPhysxTendonAxisAPI();
        }
        return PhysxSchemaPhysxTendonAxisAPI(stage->GetPrimAtPath(path.GetPrimPath()), name);
    }

    /// Return a vector of all named instances of PhysxSchemaPhysxTendonAxisAPI on the
    /// given \p prim.
    static std::vector<PhysxSchemaPhysxTendonAxisAPI> GetAll(const UsdPrim &prim)
    {
        std::vector<PhysxSchemaPhysxTendonAxisAPI> schemas;
        const TfType &type = UsdSchemaRegistry::GetTypeFromSchemaTypeName(PhysxSchemaTokens->PhysxTendonAxisAPI);
        for (const auto &n : _GetMultipleApplyInstanceNames(prim, type)) {
            schemas.emplace_back(prim, n);
        }
        return schemas;
    }

    /// Returns true if this <b>multiple-apply</b> API schema can be applied,
    /// with the given instance name, \p name, to the given \p prim. If this
    /// schema can not be a applied the prim, this returns false and, if
    /// provided, populates \p whyNot with the reason it can not be applied.
    ///
    /// Note that if CanApply returns false, that does not necessarily imply
    /// that calling Apply will fail. Callers are expected to call CanApply
    /// before calling Apply if they want to ensure that it is valid to
    /// apply a schema.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static bool CanApply(const UsdPrim &prim, const TfToken &name, std::string *whyNot=nullptr)
    { return prim.CanApplyAPI(PhysxSchemaTokens->PhysxTendonAxisAPI, name, whyNot); }

    /// Applies this <b>multiple-apply</b> API schema to the given \p prim
    /// along with the given instance name, \p name.
    ///
    /// This information is stored by adding "PhysxTendonAxisAPI:<i>name</i>"
    /// to the token-valued, listOp metadata \em apiSchemas on the prim.
    /// For example, if \p name is 'instance1', the token
    /// 'PhysxTendonAxisAPI:instance1' is added to 'apiSchemas'.
    ///
    /// \return A valid PhysxSchemaPhysxTendonAxisAPI object is returned upon success.
    /// An invalid (or empty) PhysxSchemaPhysxTendonAxisAPI object is returned upon
    /// failure. See \ref UsdPrim::ApplyAPI() for
    /// conditions resulting in failure.
    ///
    /// \sa UsdPrim::GetAppliedSchemas()
    /// \sa UsdPrim::HasAPI()
    /// \sa UsdPrim::CanApplyAPI()
    /// \sa UsdPrim::ApplyAPI()
    /// \sa UsdPrim::RemoveAPI()
    static PhysxSchemaPhysxTendonAxisAPI Apply(const UsdPrim &prim, const TfToken &name)
    {
        if (prim.ApplyAPI(PhysxSchemaTokens->PhysxTendonAxisAPI, name)) { return PhysxSchemaPhysxTendonAxisAPI(prim, name); }
        return PhysxSchemaPhysxTendonAxisAPI();
    }

public:
    // --------------------------------------------------------------------- //
    // GEARING
    // --------------------------------------------------------------------- //
    /// Joint gearing(s) per joint axis in axis token array, in the same order.
    /// Range: (-inf, inf)
    /// Units:
    /// if translational axis: unitless
    /// if rotational axis: distance/degrees
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] gearing = [1]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    UsdAttribute GetGearingAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_Gearing, _GetInstanceName())); }

    /// See GetGearingAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateGearingAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_Gearing, _GetInstanceName()),
                   SdfValueTypeNames->FloatArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // FORCECOEFFICIENT
    // --------------------------------------------------------------------- //
    /// Joint force coefficient(s) per joint axis in axis token array, in the same order.
    /// Range: (-inf, inf)
    /// Units:
    /// if translational axis: unitless
    /// if rotational axis: distance
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `float[] forceCoefficient = [1]` |
    /// | C++ Type | VtArray<float> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->FloatArray |
    UsdAttribute GetForceCoefficientAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_ForceCoefficient, _GetInstanceName())); }

    /// See GetForceCoefficientAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateForceCoefficientAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_ForceCoefficient, _GetInstanceName()),
                   SdfValueTypeNames->FloatArray, /* custom = */ false,
                   SdfVariabilityVarying, defaultValue, writeSparsely);
    }

public:
    // --------------------------------------------------------------------- //
    // JOINTAXIS
    // --------------------------------------------------------------------- //
    /// Specifies target joint axis/axes. Ignored when the joint only has a single axis, e.g. a revolute joint.
    ///
    /// | ||
    /// | -- | -- |
    /// | Declaration | `uniform token[] jointAxis` |
    /// | C++ Type | VtArray<TfToken> |
    /// | \ref Usd_Datatypes "Usd Type" | SdfValueTypeNames->TokenArray |
    /// | \ref SdfVariability "Variability" | SdfVariabilityUniform |
    /// | \ref PhysxSchemaTokens "Allowed Values" | transX, transY, transZ, rotX, rotY, rotZ |
    UsdAttribute GetJointAxisAttr() const
    { return GetPrim().GetAttribute(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_JointAxis, _GetInstanceName())); }

    /// See GetJointAxisAttr(), and also
    /// \ref Usd_Create_Or_Get_Property for when to use Get vs Create.
    /// If specified, author \p defaultValue as the attribute's default,
    /// sparsely (when it makes sense to do so) if \p writeSparsely is \c true -
    /// the default for \p writeSparsely is \c false.
    UsdAttribute CreateJointAxisAttr(VtValue const &defaultValue = VtValue(), bool writeSparsely=false) const
    {
        return UsdSchemaBase::_CreateAttr(UsdSchemaRegistry::MakeMultipleApplyNameInstance(PhysxSchemaTokens->physxTendon_MultipleApplyTemplate_JointAxis, _GetInstanceName()),
                   SdfValueTypeNames->TokenArray, /* custom = */ false,
                   SdfVariabilityUniform, defaultValue, writeSparsely);
    }

protected:
    /// Returns the kind of schema this class belongs to.
    ///
    /// \sa UsdSchemaKind
    UsdSchemaKind _GetSchemaKind() const override { return schemaKind; }
    const TfType &_GetTfType() const override { return _GetStaticTfType(); }
};

PXR_NAMESPACE_CLOSE_SCOPE

#endif
